# CLAUDE.md — Pixel Angler The Real

> 2D 픽셀 퍼펙트 한국 해양 낚시 시뮬레이터 + 생활 경영 RPG.
> TypeScript 5.8 (strict) · Phaser 3.90 · Vite 6 · Turborepo 2 + pnpm 9 · Tauri v2 · Node ≥ 20.

## 필수 선행 문서 (작업 전 반드시 읽기)

@.agents/AGENTS.md

- `.agents/AGENTS.md` — 아키텍처·코딩 규칙·금지 사항의 **단일 기준 문서** (위에 자동 임포트됨)
- `.agents/IMPLEMENTATION_PLAN.md` — 구현 단계 현황과 다음 작업 목록. 작업 완료 시 이 두 문서를 반드시 최신화할 것.
- `.agents/STORY_SPEC_v4.md` — **스토리·퀘스트 정본**(퀘 186·아크 23·법 5조·id 표·구현 현황). 퀘스트 작업 시 필독.
- **`docs/wiki/README.md` — 구조화 뷰(위키)**. 시스템별 현황·세부과제·잔여·위험을 한 눈에.
  작업 **착수 전** 해당 시스템 페이지(`docs/wiki/02-SYSTEMS/*.md`)를 읽고, **완료 후** 스킬 `work-log` 절차로 기록한다.
- `.agents/FIRE_COOKING_SPEC.md` · **`.agents/FIRE_COOKING_EXPANSION_SPEC.md`** — **불요리 작업 시 필독**
  (확장본 = 「레시피 ≠ 요리」 개체화 기획. ⚠ **§0.5(코드 정합)가 본문보다 우선** — 실 심볼·id 대조표).
- `.agents/CEPHALOPOD_BUTCHERY_SPEC.md` — **두족류 손질 작업 시 필독** (4종 트리·프리미티브·부산물·수율).
  ⚠ **§0.5(코드 정합 v3.1)가 v3 본문보다 우선** — 원문은 레포 접근 없이 작성돼 speciesId·심볼·프리미티브 실재 여부가 실제 코드와 다르다.

## 프로젝트 스킬 (.claude/skills/ — 해당 작업 시 반드시 해당 스킬 로드)

- **`verify-render`** — Playwright 실렌더 검증 하네스 (dev 전역 `__INV`/`__GS`·`.ts` URL 함정·측정 규칙)
- **`asset-pipeline`** — 이미지 에셋 투입/교체 ("구운 스냅샷 vs 직접 로드" 구분·생성기 목록·방향 규칙)
- **`add-species`** — 신규 어종 4계층 등록 (오라클/도감/텍스처/경락 — 매칭 테이블 순서 함정)
- **`ui-panel`** — 팝업/패널 작성·검수 (z-order 밴드·윈도우드 렌더 vs 마스크·커스텀 드래그·텍스트 오버플로)
- **`save-migration`** — 세이브 하위호환 (시드 백필·유저 상태 보존·폴백 3단계·오프라인 신선도 정지)
- **`deploy-ghpages`** — 테스트 빌드 배포 (worktree 절차·소스맵 제외·상대경로·라이브 검증)
- **`f9-guide-coords`** — 손질 가이드 좌표 실측 반영 (opts 보존·core 리빌드·cov 1.00 검증)
- **`add-region`** — 신규 지역 타일맵 (파이프라인·맵 그래프·4-연결 규칙·depthProfile 함정)
- **`add-tuning`** — 튜닝 값 추가 (TUNING/META 슬라이더·stale dist 함정·스냅샷 확정 흐름)
- **`scene-transition`** — 씬 전환 규칙 (SceneFade 안전망·pause/launch·재진입 가드)
- **`work-log`** — **작업 기록·문서 체계** (docs/wiki 4층·8절 양식·갱신 체크리스트). **모든 작업 완료 시 필수**
- **`doc-readability`** — **문서 가독성 규칙** (빈 줄·줄 길이 상한·블록인용 제한·차수 요약 양식). **문서 기록·갱신 시 `work-log`와 함께 로드**

## 현재 진행 상황 (2026-09-19) — 이어받기 요약

> 상세·우선순위·로드맵은 **`IMPLEMENTATION_PLAN.md` 최상단 "🚧 다음 착수"** 참고.

- **전체 위치**: Phase 6(게임플레이 심화) — 맵 인프라·해루질·통발 인-맵 1차 완료 후
  **성장·생존 스펙 P1~P9 완료**(P8 = 134차 스토리 S22) → ~~법 강제 판단·Ch1 실시스템~~ ✅135차 →
  ~~위판·총회~~ ✅147 → ~~인벤 스크롤·가방 사다리~~ ✅148 → ~~구멍치기~~ ✅149 → ~~불요리~~ ✅154 → 다음 = **농장**.
- **로드맵(사용자 확정 2026-09-17)**: ~~위판+총회~~ ✅147 → ~~인벤 스크롤+가방~~ ✅148 → ~~구멍치기~~ ✅149 →
  ~~불요리~~ ✅154 → ~~요리 개체화 1단계(매운탕 3종)~~ ✅156 → **나머지 7개 레시피 변형(사용자 확인 대기)** → **농장**
  (농사 스킬 배선·타입은 농장 단계 이연). 맵 확정(육안)·OSM land 14개는 사용자 동반 병행 트랙.
- **직전 작업**: 157차 — **전체 위키 확장·서더리/통생선 매운탕 분리·요리 별점·NPC 대화창 리디자인**
  `stew_red`는 서더리 전용, `stew_red_whole`은 통생선 전용으로 분리했다. 완성 회·모듬회·숙회는 `요리(회)`·`요리(숙회)` 하위 분류로 묶고, 상세는 `★/☆` 5칸 완성도로 표시한다. `tools/gen_game_wiki_data.mjs`와 `tools/game_wiki_template.html`, Claude 전달용 컨텍스트를 추가했다. 상세 `docs/wiki/03-WORKLOG/2026-09-19-157-wiki-cooking-dialogue.md`.
- **이전 작업**: 156차 — **요리 개체화(DishInstance) 1단계: 매운탕 어종별 변형 + 상세보기 + 요리 도감**
  (사용자 기획서 「불요리 확장 v1.0」): 명제는 **「레시피 ≠ 요리」** 였다 — 154차는 레시피 1개 = 아이템 1개라
  우럭을 넣든 광어를 넣든 결과가 늘 같은 `매운탕`이었고, **낚시 게임에서 조리가 어획을 지우고 있었다.**
  착수 범위도 사용자가 지정했다(*"매운탕 3종 → DishInstance → 상세보기 → 요리 도감까지만 먼저 완성하고,
  정상 작동을 확인한 뒤 나머지 7개를 확장"*) — **기획서 Stage 1~6**까지다.
  **core 5파일 신설** — `FishCookingProfiles`(지방감·식감·비린 향·풍미·국물) · `RecipeLore` · `FoodEffects`(8종) ·
  `DishDiscovery` · **`DishFactory.createDishInstance`**(프로필 × 중량 × 신선도 × 154차 품질 → 이름·회복치·효과·가격) +
  `TUNING.cook.{ingredientQuality, dishEffect, naming, pricing}`(**mockup**). ⚖ **154차 조리 엔진은 무수정**.
  실측 — 같은 900g에서 **감성돔 ₩31,800·허기 +29 / 우럭 ₩30,600·+27 / 광어 ₩29,100·+25**, 효과 지속 32/29/28분.
  ⚖ **개체는 `InvItem.dishInstance`로 저장**(구세이브 호환) · **실기전 효과는 포만감 하나**(나머지 7종 표시만) ·
  **수식어 최대 1개**(탄 → 일품 → 싱싱한 → 기름진) · **도감은 (레시피 × 주재료) 1건**.
  **요리 도감 탭(N)** 매운탕 3/50 · 전체 3/57 · 미발견 `??? 매운탕`.
  ⚠ 실측 결함 3건 — 발견 토스트가 **내부 id 노출**(`inv_dish_stew_red_1` · §8-9 → 카탈로그 밖은 알리지 않는다) ·
  **효과 지속 문장이 개체마다 달라 사전을 비껴감**(i18n **런타임 규칙** 신설) · 비린 향 라벨 4단계인데 프로브 3개.
  core 20항목 ALL PASS · 실렌더 전 항목 · EN 잔존 0 · pageerror 0 · 빌드 3/3 · typecheck 0.
  상세 `docs/wiki/03-WORKLOG/2026-09-18-156-dish-instance-cooking-codex.md`.
- **이전 작업**: 155차 — **인지 UX 전면 + 채비 고정 + 사시미 별 5개 + 3끼 생존 + 초상 정합 + 혼잣말 퀘 우호도 정정**
  (사용자 지시 10건 + 캡처 추가 3건): **우호도 이질** — 무발주 퀘(M1-01·M7-08)가 메인 톤 세트를 폴백해 `affinity['']`에 +0.03을
  쌓았다 → 혼잣말 전용 선택지(효과 없음) + `applyOutcome` 발주자 가드 + **검증기**(무발주 우호도 금지 · `affinityOther` ≠ 발주자)로
  데이터 오류 3건 적발·정정(N13-3·N05-5 이중 가산 · **N09-2 발주자 → 한봄**). **인지** — core `rules/QuestGuide.ts`가 목표마다
  「방법」 문장(일감 = `정옥선에게 [F] → 「일손이…」 → 일감 「얼음 나르기」`)을 내고 대화창·일지·**필드 추적기**가 공유 ·
  **점멸 화살표**(추적 → 활성 메인 → 수령 가능) + 일지 「이 할 일 추적하기」 · **우측 획득 토스트**(아이템·재화) · 인벤 NEW 점·귀속 금테.
  **전체 지도(M)** `FullMapPanel` — 휠 줌(내 위치 중심)·드래그(좌→우 = 지도 왼쪽)·마커·범례·빨간 점멸 현위치·피어·**우클릭 핀**(별도 청록 화살표) · 미니맵 타이틀바 [−][+].
  **채비 고정** — U 채비 탭 우측 하단 [채비 고정]/[고정 해제](캐스팅 전 팝업은 사용자 정정으로 제거) · `rigLocked` 세이브 · 고정 중 변경 무시 ·
  **1인칭 결과 후 항상 탑다운 복귀**(`recast` 폐기). **사시미 별 5개** `SashimiQuality`(신선·컷·식감·구성·완성도 + 감쇠 · 별 판매 배율).
  **생존** 드레인 하루 3끼 기준(허기 105/수분 134) + `FoodNutrition`(g·kcal·ml → 회복치). **초상** 렌더 문법을 필드 시트에 맞춤.
  ⚠ 하네스 함정 2건(헤드리스 rAF · `nearWater` 재계산 — 스킬 `verify-render`). 빌드 3/3 · typecheck 0 · EN 잔존 0 · pageerror 0.
  상세 `docs/wiki/03-WORKLOG/2026-09-18-155-quest-guide-fullmap-rig-lock.md`.
- **이전 작업**: 154차 — **불요리: 화구 조합 설치 + 실시간 조리 시뮬 + 맛 별 5개 + 걸어두기**
  (로드맵 다음 단계 + 사용자 지시 3건): **조합 설치** — 인벤 [화구 설치] → 스토브 + 캐니스터(끼우면 소모·55분) +
  용기(코펠/양수/팬/석쇠 — 나중에 올려도 됨) → 뭍 3타일 `[F]` 조리 / `[Shift+F]` 회수(연료 소실). 집 주방 가스레인지 = 연료 무한·수돗물.
  골격은 121차 통발 그대로(GameState·wall-clock·피어 공유 통발 채널 `kind:'stove'`). **리얼리즘** — 레시피 8종
  (매운탕·지리·생선구이·조림·오징어볶음·조개탕·전복죽·장어구이) · 필수/부가/택1/한도 · 소금·설탕 **양**(0.25큰술) ·
  단계 순서 · 열 모델(100°C 캡+증발·팬 150°C·바람·열관성) · 익음/탐. ⚖ **창작 요리 배제**(레시피 밖 재료 거부).
  **별 5개** = 간·온도·식감·신선도·완성도(넷 조화 — 하나라도 모자라면 다섯째 없음) + **시간 감쇠**(온도 20~30분·신선 240분).
  **걸어두기** = 실 10초 = 조리 1분 · 탑다운 배지 · 약불 보온 · 강불 방치 탐.
  ⚠ 실측 6건: 코펠 용량 1:1 부피(`solidVolumeFrac 0.6`) · **회수 시 스토브·용기 증발**(`recoverPlaceable` 카탈로그 폴백) ·
  패널 정적 층(레이아웃 키) · 상세보기 재료 행 왼쪽 삐짐 · 큰 고기 사유 · **vite가 core를 `@fs` 소스로 서빙해 dist 빌드 실패가 묻힌다**.
  도움말 「불요리」 3토픽 · 아이콘 83종 · 마트 코너 28종. 빌드 3/3 · typecheck 0 · pageerror 0.
  상세 `docs/wiki/03-WORKLOG/2026-09-18-154-fire-cooking.md`.
- **이전 작업**: 153차 — **대화 전용 70편 실목표 배선 + 아티팩트 장 접기 + 「할 일」 용어 통일**
  (사용자 지시 2건): ① *"대화 전용 임무 70편도 잘 바꿔줘"* — 152차는 그것을 **정직하게 표기하고 품삯을 0.25배로
  낮췄을 뿐**이었다. 70편 전부(메인 25·서브 45)에 **추적기가 실재하는 목표**를 심었다
  (`catch`·`release`·`butcher`·`sashimi`·`gather`·`craft`·`sell`(위판)·`trap`·`custom(job·buy)`).
  분포 **대화 70 → 0** · 수월 26 · 보통 112 · 까다로움 44 · 고난도 4(구간 1/4/8/13 → **1/3/6/8** · `custom` 0 → 1).
  ⚖ **닫히지 않는 목표는 만들지 않는다**(진행 중 세이브가 막힌다) —
  ⚠ **`spotKind: 'boat'`는 어디서도 발화하지 않아**(`standingSpotKind()`는 `breakwater|shore`만) 선상 조건은 0건 ·
  ⚠ **M3-07 「종묘방류」의 `communityWork`는 속초 전용 일감이라 무대인 포항에서 영원히 안 닫혔다** → 치어 방생 5회.
  추적기 없는 종류(`cook`·`boatTrip`·`holdPosition`·`survive`·`furnish`·`farm`·`mine`)의 `manual` 줄은
  이야기 표시로 남기고 그 옆에 실목표를 붙였다. ⚠ 70편이 품삯 0.25배 구간을 떠나 **재화 곡선이 위로 이동**한다.
  **죽은 배선 2건**(지시에 없던 것) — `CraftingStore`가 `grantXp`를 직접 불러 **제작 이벤트도 제작 숙련도도
  한 번도 오르지 않았다** → `addActivityXp` 경유 복구 · 면허 요구사항이 `questId`/`spotId`/`fishId`
  **내부 id를 그대로 출력**(§8-9 R1) → 이름 조회.
  **아티팩트** = 열람 중인 장이 안 접히던 범인 `if (!openCh.size && cur) openCh.add(...)` 제거 +
  **모두 접기/펼치기 + `7장 · 186편`** + 접힌 장 **금색 바·점·`열람 중 — …`** ·
  ⚠ `keepCurrentVisible()`의 `offsetTop`이 sticky 색인에서 어긋나 선택 행이 **55px 밖**에 남던 것 → rect 기준.
  **용어 임무 → 「할 일」 전수**(예외 = 제호 `조행록 퀘스트 항로도` · `퀘스트 정보 개요`) ·
  ⚠ `[퀘스트] …` 로그 6종은 **영문 규칙이 없어 영어 화면에 한국어로 남아 있었다** → 규칙 신설.
  빌드 3/3 · typecheck 0 · 무결성 0 · pageerror 0.
  상세 `docs/wiki/03-WORKLOG/2026-09-18-153-real-objectives-chapter-collapse.md`.
- **이전 작업**: 152차 — **퀘스트 클리어 난이도 + 선택지 영문 1,048줄 + 대화 초상 재렌더 + 아티팩트 수정 4건**
  (사용자 실검증 지시 5건): 관통하는 질문은 **"그냥 대화만 했을 뿐인데 재화를 지급해주는 퀘스트들"** 이었다 —
  **버그가 아니라 134차 설계**다. `manual` 목표는 자동 추적기가 없어 대화 [다음 단계]로 닫히고
  (`StoryStore.event`가 `if (o.manual && ev.kind !== 'talk') return;`) 라벨이 무엇을 약속하든 조작은 대화 한 번이다.
  실측 manual 84건 중 77건은 `talk`(무해) · **실제 문제 7건** · 라벨의 개발 상태 노출 **112건**.
  **난이도** = core `rules/QuestDifficulty.ts` — **수행 활동만** 세어 대화/수월/보통/까다로움/고난도 5단
  (⚖ **manual은 0점** — 강제되지 않는 것을 세면 표기가 거짓말이 된다 · ⚖ 요구 레벨·보상은 제외 ·
  조건 한 겹 +1 · 수량 2~3 +1 / 4~7 +2 / 8+ +3 · 기한 +1). 분포 **대화 70 · 수월 33 · 보통 79 · 까다로움 4**.
  ⚖ **품삯에 난이도 배율**(대화 **0.25배** — 0으로 만들지는 않는다. 대화도 이야기를 진행시킨다).
  manual 해제 4건(M3-03·N23-2·N20-3·N23-3) ⚠ **auto `visit`은 `placeKey` 없으면 영원히 안 닫힌다**.
  **선택지 영문** = 짝 파일 `db-schema/StoryChoicesEn.ts` 524엔트리 → **1048/1048 · 누락 0** · UI 호출부 변경 0
  ⚠ 키에 세트(`c`/`o`)를 넣는다(같은 id의 발주·완료 선택지 충돌 4건).
  **초상** = 150차는 시트 얼굴 창의 **실화소 10x9**를 NEAREST로 키운 것뿐이라 인물이 구별되지 않았다 →
  core **`art/FacePortrait.ts`** 신설, 같은 `CharConfig`로 **32x32 전용 격자에 다시 그린다**
  (흰자·홍채·동공·하이라이트·코·인중·입술·귀·턱 그늘·수염결 — **필드 스프라이트 불변 · 표시 크기 동일**).
  **아티팩트** = 장 표기 순서 · **장별 드롭다운 색인** · `퀘스트 정보 개요` · **겹침 해소**(범인은 `float:right` →
  2열 머리 그리드) · 난이도 행. 빌드 3/3 · typecheck 0 · pageerror 0.
  상세 `docs/wiki/03-WORKLOG/2026-09-17-152-quest-difficulty-en-choices-portrait.md`.
- **이전 작업**: 151차 — **영문 나레이션 186편 + 장비창 페이퍼돌 착용 반영 + 퀘스트 아티팩트 위키형 재발행**
  (사용자 지시 3건 — 백로그에 이름만 올라 있던 빚 셋 O1·O2·O4를 한 번에):
  **영문 나레이션** = 141차가 "영문은 후속"으로 미룬 것 → 한국어와 **같은 키를 쓰는 짝 파일**
  `db-schema/StoryNarrativeEn.ts` 186편 + `narrativeOf()` 합본. 화면 경로는 **i18n 사전**(원문이 곧 키)이라
  **UI 호출부 변경 0** · 커버리지 **829/829 · 누락 0**(`narrativeEnCoverage()`).
  ⚠ 잔여 = **선택지 영문 510개**(`labelEn = labelKo` 폴백 유지).
  **페이퍼돌** = 138차 레이어 14종이 **인벤토리를 읽는 곳이 한 군데도 없었다**(`refreshCharacterLook` 호출측 0 —
  미구현이 아니라 **끊어진 배선**) → `client/data/EquipOutfit.ts` + 렌더 호출측 5곳을 `characterLook()`으로.
  ⚖ **덮어쓰기지 대체가 아니다** — 시드 옷은 기본 미착용이라 전면 파생이면 **새 캐릭터가 벗고 시작한다**(실측).
  ⚠ **기저(`GameState.character.outfit`)는 절대 수정하지 않는다** — 덮어쓰면 그다음부터 벗기가 동작하지 않는다.
  **아티팩트** = 타르코프 위키 골격(대화·요구사항·목표·보상·안내·참고·여담)으로 같은 URL 재발행,
  생성기 `tools/gen_quest_wiki_data.mjs --html`이 `dist`에서 구워 **발행본과 바이트 동일**.
  ⚠ 실측 2건 — 390px 가로 넘침의 범인은 `grid-template-columns:1fr`(= `minmax(auto,1fr)`) ·
  초기 `scrollIntoView`가 첫 화면에서 제호를 밀어냈다. 빌드 3/3 · typecheck 0.
  상세 `docs/wiki/03-WORKLOG/2026-09-17-151-en-narrative-paperdoll-quest-wiki.md`.
- **이전 작업**: 150차 — **실검증 피드백 12건: 표현형 손질 + 스포일러 차단 + 표현 규칙 상시화**
  (사용자 지시 12건 + 참고 자료 3건): 관통하는 지적은 개별 버그가 아니라
  **"표현형이 AI가 함축된 텍스트로 direct하게 표시하려는 느낌"** 이었고, 마지막에
  **"내가 지정한 부분들은 규칙으로 정해놔"** 라고 해서 산출물이 코드 + **상시 규칙**이 됐다 —
  **AGENTS §4 「인게임 표현 규칙」 R1~R10** · §8 금지 9~11 · CLAUDE 7번 · `ui-panel` 스킬.
  **일지(J) 재작성** — 구 구조가 186퀘를 통째로 펼쳐 *"유저들의 재미를 다 떨어트리는"* 상태였다.
  타르코프 임무 화면으로 이식(좌 표: 의뢰인·임무·지역·상태·**진행률 N%+바** / 우 상세: 얼굴·
  **자체 스크롤 나레이션**·목표 바·보상 카드 + **이야기 탭** 붉은 점선 척추·현재 위치 ▼).
  **기본 노출 1 / 186** · 완료/잠긴 토글 둘 다 기본 꺼짐 · 잠긴 임무는 이름도 안 준다.
  **대화창 재작성** — 타이핑 · 위로 밀리는 로그 · 단락 끝 ▼ · 본문 20px · 표준 선택지 3종 ·
  **얼굴 확대 초상** + **오프닝 혼잣말 6단락**. ⚠ **한글이 입력되지 않았다**(IME 조합) →
  숨김 DOM 입력 `ui/TextInput.ts`. ⚠ **도로 위 횟집** — 106차는 *타일*만 걷어냈고 아스팔트는
  *도로 벡터*가 그린다 → `nudgeOffRoad`로 **문 좌표**를 민다(속초 199곳) · 타일 건물 지붕도
  가림 편입(경계 상자). 차량 ×1.4/×1.2(52.78·66px > 캐릭터 52px) · 별불가사리 −30% ·
  장비창 구형 캐릭터 교체 + 이모지 고스트 16종 → 픽셀 아이콘(48 → 68종) · 용어 치환.
  실렌더 47/47 · pageerror 0.
  상세 `docs/wiki/03-WORKLOG/2026-09-17-150-ux-overhaul-spoiler-gate.md`.
- **이전 작업**: 149차 — **구멍치기(테트라포드·사석) + M1-04 장소 게이트**
  (로드맵 다음 단계): ⚠ **조법 자체가 없었다** — 학습 태그·일지 라벨·사이소 저가 세트·
  도현수 대사("구멍치기는 부러져도 안 아까운 대로 하는 거야")까지 다 있는데 **그 조법으로 낚을 방법이 없었다.**
  반대로 **지형 판정은 114차부터 있었다**(`breakwaterClassAt` 0 안벽 / 1 상판 / **2 피복** / **3 사석**) —
  이번 차수는 그 위에 **조법 하나와 조건 한 줄**을 올린 것이다.
  core **`HoleFishing.ts`**(구멍 깊이 — 테트라포드는 블록이 겹쳐 연안 수심보다 깊은 수직 굴 ×1.15 /
  사석 ×0.85 · 1.5~9m 클램프 · 수면 거리 1.2~2.0m · **밑걸림 ×2.2 / ×1.7** ·
  구멍 어종 가중(볼락 2.6·우럭 2.4·쏨뱅이 2.2 ↔ 방어·삼치 0.1) · 미끄러짐) + `TUNING.hole` 15키(**mockup**).
  ⚖ **새 단축키를 만들지 않았다** — `F` 체인은 이미 꽉 찼고 **채집 스팟이 테트라포드 발밑에도 생겨** 서로 잡아먹는다.
  낚시는 마우스에 둔다: 블록 위에서 **짧게 = 구멍치기 / 꾹 = 캐스팅**(`tapMs` 320ms — 호핑/릴링과 같은 문법).
  같은 자리에서 두 조법을 다 쓰므로 **기존 캐스팅을 빼앗지 않는다**.
  ⚖ **장소 게이트는 라벨이 약속한 만큼만** — `StorySpotKind`·`StoryObjective.spotKind` 신설,
  M1-04 라벨이 "방파제에서"라 `breakwater`를 걸고 구멍치기(`hole`)가 이를 만족하게 했다
  (더 요구하면 계약 위반이고 **진행 중 세이브가 막힌다**). 조건**만** 어긋난 어획은 조용히 버리지 않고 이유를 말한다.
  ⚠ 실측 2건: 기본 `SeabedProfile`이 발앞을 깎아 **2.2m 구멍이 0.8m**로 그려졌다(→ `{floorRatio, alwaysRock}`) ·
  매 프레임 판정이 `resolveCastDepth`의 `[수심]` 로그를 도배(→ 타일 캐시 + `quiet`).
  도움말 **구멍치기 4페이지** · 실렌더 47/47 · pageerror 0.
  상세 `docs/wiki/03-WORKLOG/2026-09-17-149-hole-fishing-spot-gate.md`.
- **이전 작업**: 148차 — **인벤토리 윈도우드 스크롤 + 가방 사다리 2~5단계 실효화**
  (로드맵 다음 단계 · 사용자 선택 "윈도우드 스크롤"): ⚠ **가방은 이미 다 있었다** —
  141차가 `bagSlots` 3·5·8·10·12를 발행했고, 막고 있던 것은 **`GRID_CAPACITY_MAX = 30` 한 줄**이라
  **8·10·12가 전부 30으로 클램프**되어 원정 가방·가이드 가방·항해 가방이 **서로 같은 효과**였다
  (미구현이 아니라 **죽은 보상** — 144차 `'run'`·147차 `AuctionEngine`에 이은 세 번째 같은 패턴).
  ⚠ **그 30도 밸런스가 아니라 렌더 한계**였다(135차 주석: "더 늘리려면 패널 재설계가 먼저") —
  그래서 실제 작업은 **`InventoryPanel` 재설계**다. **윈도우드 스크롤**(보이는 행만 생성 · 휠 ·
  스크롤바 · `n–m / N행` · **드래그 중 가장자리 자동 스크롤**). ⚠ **마스크 금지** — Phaser는 마스크로
  **입력을 클립하지 않아** 스크롤아웃된 셀의 팬텀 히트가 남는다(54차 ShopPanel 전례).
  상한 **37**(= 25 + 최대 12) → **25/28/30/33/35/37** · 뷰포트 416px에 **가방 없음 5행 · 착용 6행**.
  **용량 밖 칸 = '잠긴 칸'**(꺼내기 O · 넣기 X) → 135차 경고가 가리키던 **시드 tackle 4건이 처음 화면에 나온다**.
  칸 축소 가드를 **`bagShrinkBlocked`** 로 일반화해 **작은 가방 교체**도 막는다.
  ⚠ **i18n 사전 수정 후에는 dev 서버 재시작 후 검증**(120차 함정 재현 — HMR `?t=` 분화로 "전 항목 미번역").
  상세 `docs/wiki/03-WORKLOG/2026-09-17-148-inventory-scroll-bag-ladder.md`.
- **이전 작업**: 147차 — **위판(경매 현장) + 어촌계 총회 연출 + 기한 초과 평판 감점**
  (로드맵 다음 단계 · 사용자 결정 5건): 착수 조사에서 **라벨과 실제가 어긋난 네 건**이 나왔다 —
  **"통발 D2"는 이미 121차에 끝나 있었고**, **`AuctionEngine` 332줄이 사문**(클라 소비처 0)이었으며,
  M2-01 목표 라벨이 문자 그대로 `위판 UI — 준비 중`, M1-04는 구멍치기 없이도 통과된다.
  ⚠ **사문의 기전 = 쓸 수 없는 방향** — `AuctionEngine`은 플레이어를 **중매인(사는 쪽)** 으로 모델링했다
  (`calcPlayerAuctionTotal` = 총 *비용*). 구매자 측을 **한 줄도 고치지 않고** 개폐장 스케줄·`AuctionLot`만
  공유하는 **`ConsignmentAuction.ts`(파는 쪽)** 를 신설했다.
  **위판 창구 = 건물 종류가 아니라 능력**(`ShopDef.auctionWindow` — 속초는 직판장 겸용, 전용 위판장이
  생기면 데이터만 바꾼다 · 사용자 결정). ⚖ **평판은 수수료율에만**(0 = 6% → 100 = 3%) — 낙찰가에 걸면
  시장 가격이라는 자유 콘텐츠에 진행도가 샌다(평판 0↔80 낙찰 총액 105,730원 **동일** 실측).
  ⚖ **위판 자격은 `enforceRodSell` 토글과 무관하게 항상** 본다(위판장은 벌칙이 아니라 계원 창구).
  ⚖ `canConsign`에 **생산자 요건** 추가(구매품 위판 = 재정거래 구멍). ⚖ **총회는 부결되지 않는다** —
  찬성률만 평판을 따르고(0 → 13/24 · 100 → 23/24) 과반 초과분이 곧 위판 수수료로 이어진다.
  ⚖ **기한 초과는 재화가 아니라 평판 감점**(M1-06~M1-11 구간은 현금이 품삯뿐이라 회복 불가가 된다).
  **M2-01 `sell` 목표 `manual` → auto.** ⚠ **합성 문자열이 사전을 비껴가는 131차 함정 재발** —
  붙이기 전 `t()` 번역으로 해소하되 단위어는 전역 사전 오염을 피해 로케일 분기.
  상세 `docs/wiki/03-WORKLOG/2026-09-17-147-consignment-auction-general-meeting.md`.
- **이전 작업**: 146차 — **유저 간 거래(공증) + 정보 보기 + 플리마켓 계약 + 팝업 중 동기화 결함 수정**
  (사용자 지시 3건 + 의견 요청): **피어 우클릭(한 칸 안) → 정보 보기 / 거래하기.** 서버 = **공증인**(인벤은 각자 로컬):
  제안 → 수락창 → 편집 → 양쪽 잠금 → 양쪽 확정 → `committed` 도장 → 각자 적용 → `applied`.
  **제안을 고치면 양쪽 잠금이 풀리고**, 도장 뒤엔 취소 불가, 적용 전 튕기면 **이어하기 때 다시 내려준다**(7일).
  예외(바쁨·거래 중·거리·이탈·시간 초과)는 전부 채팅 로그 문구. ⚖ **퀘스트 지원 우려는 구조적으로 작다** —
  목표 24종이 전부 행위 이벤트라 인벤을 읽는 목표가 없다; 유일한 구멍 `earn`은 **`addCoins(…, quiet)`**,
  `bound`는 **양도 금지**로 확장. 퀘스트 아이템 탭은 `deliverFree` auto화 시점에(백로그 R⑥).
  ⚖ 정보 보기 공개 = 레벨·장비·면허·최대어·출조 / 비공개(서버에도 안 감) = 재화·인벤·퀘스트·생존·스킬·우호도.
  ⚖ 경매·플리마켓 = **계약은 지금(`MpTradeItem`·`MarketListing`), 원장은 서버 서명 뒤**(에스크로 방식).
  ⚠ **팝업이 열리면 멀티 동기화가 멈추던 잠복 결함**(145차부터) · "보냈다" 플래그 경쟁 · 받은 개체 id 충돌 수정.
  ⚠ `addCoins(quiet)`는 거래 전용 · 멀티 동기화는 `uiBlocked` 가드 **앞**에 둔다.
  상세 `docs/wiki/03-WORKLOG/2026-09-17-146-p2p-trade-profile.md`.
- **이전 작업**: 145차 — **멀티 세션 세계: 설치물 공유 · 공용 시드 · 외형/활동 · 피어 밀어내기 · 건물 반투명 · 지역 채팅 · 이어하기**
  (143차 §8 목록에 대한 사용자 결정 7건 + 의견 요청 3건): **통발은 놓는 순간 남에게도 보인다**
  (서버가 설치 목록 보관 · 같은 칸 중복 거절 · 남의 통발 회수 거절). 진행도(퀘스트·인벤·어획)는 **설계상 계속 각자의 것**.
  **공용 시드** `mpWorldSeed(세션시드, 종류, 키, 슬롯)` — 서버 왕복 없는 순수 함수.
  ⚠ 구 보일링은 **플레이어 ±26타일**에서 발생 지점을 골라 **시드만으로는 안 맞았다** →
  **맵 격자 칸(24타일) × 45초 슬롯의 성질**로 재작성(양쪽 계산 완전 일치 실측). 과증식 표류는 **벽시계**로 옮기고
  배치 시각부터 되감아 재생 · 홈타운 날씨 1시간 슬롯 · 교통은 **초기 편성만**.
  ⚖ **퀘스트 게이트 규칙**(사용자 지시) — 진행도가 조건에 끼어드는 무작위는 `MpSharedRngKind`(닫힌 유니온)에 넣지 않는다.
  **외형 동기화**(서버가 `CharConfig` 보관 — 구 `characterOf('mp_'+id)`는 본인이 만든 얼굴과 무관했다) +
  **활동 배지 4종**(낚시/거래/실내/자리비움 · 필드 밖은 알파 0.55 + 밀어내기 제외).
  ⚖ **피어 밀어내기는 내 바디만** 민다(피어에 물리 바디 금지 — 1초 폴링 스냅샷에 끼인다) · 막힌 칸 금지 ·
  **완전 겹침(0벡터)이면 `playerId` 비교로 반대 방향**. ⚖ **건물 반투명은 트리거 대신 프리팹 기하 판정**
  (새 물리 바디 0개 · 알파 0.38 · 0.2초 · 6px 이력). **지역 채널 실채팅**(presence 편승).
  ⚠ 입력 중에는 **Phaser 키보드를 통째로 끈다** — `stopPropagation`으로는 `keydown-B`가 안 막혀 쿨러가 열렸다.
  **이어하기** — 서버가 이름이 아니라 **`userId`로** 사람을 알아보고 세션을 디스크(`.mp-sessions/`)에 남긴다
  (프로세스 완전 재시작 후 시드·자리·통발 복원 실측). ⚖ 배포 모델 = **호스트-클라이언트 P2P**
  (서버를 없애는 게 아니라 호스트로 옮기는 것 — 코드 경로 동일). 저장 소유권 = **호스트는 세계 / 각자는 캐릭터**.
  ⚠ 이름은 접속을 끊어도 **7일간 잠긴다** · **`userId`는 localStorage**라 브라우저를 바꾸면 다른 사람이 된다.
  상세 `docs/wiki/03-WORKLOG/2026-09-17-145-mp-shared-world.md`.
- **이전 작업**: 144차 — **달리기(Shift) + 캐릭터 만들기 프리뷰 회전·동작 버튼** (사용자 지시 2건):
  `VitalsActivity`의 **`'run'`은 125차부터 있었으나 어떤 호출측도 넘긴 적이 없어** 사문이었다 →
  빈 분기를 채웠다. **Shift 홀드 + 방향키 = ×1.55**(심리스 210 → **326 px/s**) ·
  위계 **걷기 210 < 달리기 326 < 자전거 420** 고정(달리기가 자전거를 넘으면 `drv_bike` 계통이 죽는다) ·
  `drainRun` [16,26,32] → **[22,34,44]**(피로 100%까지 걷기 8.3h ↔ 달리기 2.3h).
  ⚖ **소모값만으로 벌하지 않고** 피로 **85% 초과** 또는 허기·수분 임계 미만이면 **걷기로 강등**(+12초 스로틀 경고).
  `CharacterSprite.update(dt, moving, **paceMult**)` — 달리기는 **같은 걷기 4프레임을 1.55배로** 돌린다
  (⚠ **전용 달리기 포즈 없음** — 아트 후속). **캐릭터 만들기**: 미니어처 4종 폐기 →
  **◀ 방향명 ▶ 회전 행 + 정지/걷기/달리기 동작 행**(하단 인셋 104 → 146) · **자동 회전(2.4초) 폐기**.
  ⚠ `handleMovement`가 `tickVitals`보다 **먼저** 호출되어야 활동 종류가 같은 프레임 이동과 맞는다.
  상세 `docs/wiki/03-WORKLOG/2026-09-17-144-run-sprint-charcreate-preview.md`.
- **이전 작업**: 143차 — **싱글/멀티 진입 플로우 + 대화창·팝업 통일 + 필드 라벨 정리 + 문구 다듬기**
  (사용자 지시 4건 + 후속 1건): **멀티플레이 신설(S24)** — core `types/Multiplayer.ts`(**클라·서버 공용 계약**) ·
  서버 `/mp/*` REST + 인메모리 `SessionRegistry` · 클라 `net/MultiplayerClient.ts`(1초 폴링) · `MultiplayerLobbyScene`.
  순서는 **게임 시작 → 혼자 하기/여럿이 하기 → 로비(방 열기·코드 참가) → 슬롯 3칸 → 캐릭터 만들기**(사용자 지정).
  `[이대로 생성하기]` = **중복 여부 확인중 → 통과 → 생성 중 로딩 → 접속중**, 겹치면 **빨간 글씨**로 중단.
  같은 지역이면 서로의 스프라이트 + 머리 위 이름표. **대화창** 초상 열 세로 중앙 정렬 +
  **`DraggablePanel` 프레임을 `paintHudPanel`로**(전 팝업 동시 적용) + `ui/TextQuality.ts`(래스터 2배·텍스트만 LINEAR).
  **필드 바닥 이름표·점 기본 끔**(설정 '화면' 탭) → 미니맵이 인물·장소·상점·퀘스트 `?`/`!`를 떠맡는다(아이콘 44종).
  AI 메모성 문구·UI 이모지 제거 + 부제 '당신은 어떤 사람인가요?' + 표준 퀘스트 선택지 어투 교정.
  ⚠ **멀티에서 공유되는 것은 지도·시간·서로의 위치/이름뿐** — 퀘스트·우호도·인벤·설치물·어획은 각자 로컬이고,
  보일링·과증식·차량은 클라이언트마다 다른 난수로 돈다(워크로그 143 §8에 전수).
  상세 `docs/wiki/03-WORKLOG/2026-09-16-143-multiplayer-ui-tone-labels.md`.
- **이전 작업**: 141차 — **퀘스트 나레이션 186편 + 메인 해금형 보상 + 선택지 비공개 공개 + 발주 정책(once/event)**
  (사용자 지시 4건 + 추가 3건): `descKo`는 설계 요약이라 화면이 정의문을 노출했고 173편이 표준 선택지였다 →
  core **`StoryNarrative.ts`**(186/186 — 1인칭 서두 · NPC 대사 · 내용 기반 완료 선택지 · 에필로그). **메인 = 해금형 보상**
  (`rewards.items[].bound` 귀속 장비 20 · `skillUnlocks` 7 = 스킬 `unlock: quest` · `shopUnlocks` 5 = `ShopEntry.unlockKey`) /
  **서브 = 진행 보조** — 검증기가 겹침 거부. **보상은 고른 뒤에만**(대화창 힌트 제거 · 응답 → 받은 것 → 다른 답 `???`).
  **발주 정책** once 5(거절 = 영구 소멸) · event 9(계절·레벨 구간·쿨다운). `InvItem.bound` = 판매 제외.
  ⚠ 대화창에 보상 미리보기를 다시 붙이지 말 것 · once/event는 거절 답 필수 · 영문은 후속. 캐릭터 시안(스타듀 계열) 채택 — 구현 후속.
  상세 `docs/wiki/03-WORKLOG/2026-09-16-141-narrative-unlock-rewards-offer-policy.md`.
- **이전 작업**: 140차 — **얼굴형·머리 축소·측면 프로파일 + NPC 대화창(선택지 분기) + 우호도 + 숙련도 + 아티팩트 v4**
  (사용자 지시 6건): 머리가 어깨보다 넓었다(10행×12px vs 몸통 10px) → **9행×10px** · `FaceShape` 3종 · 측면 다리는
  `limb()` **행별 기울기**(구: 사각형 전체 이동 → 뒤로 휨) · 코 1px·인중·뒤통수. **대화창 재작성** — 하단 박스·딤·초상·
  우호도 게이지·선택지(↑↓ Enter). **모든 퀘가 발주 톤 + 완료 답을 고른다**(서브 표준 4종 품삯/가르침/사양/친밀 청탁 +
  손글 13편 · 메인은 XP·아이템·SP 금지). **우호도 −1~1 5티어**(서브 보상 ×0.7~1.3 · −0.4 미만 서브 거절 · 품삯 배율 ·
  메인 재화 1±0.1 · 자유 콘텐츠 불침범). **숙련도** 기술형 22스킬 = 배운 뒤 행위로 채워야 효과(0/.35/.6/.85/1/1.15).
  GPT Image 프롬프트 스펙 `.agents/CHARACTER_SPRITE_PROMPT_SPEC.md` · 아티팩트 v4. ⚠ **구세이브의 기술형 스킬은 효과 0으로 시작**.
  상세 `docs/wiki/03-WORKLOG/2026-09-16-140-face-shape-dialogue-choices-affinity-proficiency.md`.
- **이전 작업**: 139차 — **캐릭터 만들기 UI 재정비 + 스토리 아키텍처 v4**
  (사용자 지시 2건): 138차 생성 씬은 기능만 세운 초안이었다 — 패널이 평면 `fillRect`(게임의 모든 창은
  `paintHudPanel` 문법) · **색을 `3 / 10` 숫자로 골랐고**(픽셀 팔레트에는 이름이 없어 무엇을 고르는지
  알 수 없다) · 12행이 한 줄로 이어져 그룹 경계가 없었다. → **HUD 패널 3장 + 2열 5섹션**
  (체형/얼굴/머리/복장/이름) · **실제 색 견본 스트립**(직접 클릭 · 수염은 앞에 '없음' 사선 칸) ·
  선택 강조는 **색 + 좌측 금색 바**(메인 메뉴 관례) · 미리보기 스테이지(바닥·발판·배율 8 → 10·이름 캡션) ·
  안내 홈 · 푸터 `paintHudSlot` 버튼 · 방향 회전은 `setFrame`만.
  **`.agents/STORY_SPEC_v4.md` 신설 = 스토리 정본**(481줄) — v3의 기형(§0.5 코드 정합이 본문보다 우선)을
  **본문 승격**으로 해소하고 134~138차 구현 반영: 퀘 **186** · 아크 **23** · XP **2,809,150** ·
  목표 종류 24 · 학습 태그 66 · 법 강제 3단계 · 회귀 고리 · 생활 계통 · 캐릭터 계약 · 구현 현황표.
  **수치는 전부 `dist`에서 산출**(추정 없음). v3는 원안 산문 보관본 · 충돌 규칙 **코드 > v4 > S22 > v3**.
  ⚠ **캐릭터 아트(머리 크기·눈·입)는 사용자 검토 중이라 보류**.
  상세 `docs/wiki/03-WORKLOG/2026-09-16-139-charcreate-ui-story-v4.md`.
- **이전 작업**: 138차 — **바닐라 베이스 캐릭터 + 페이퍼돌 장비 + NPC 41인 분화 + 과증식 해양생물 4종 + 퀘스트 28편**
  (사용자 지시 6건): 구 캐릭터는 외부 출력물을 런타임 축소해 썼다(`man-idle-front` 260x467·26,817색 → 52px = **x0.111**) —
  **idle/walk 실효 해상도 2.3배 차이**·그레인 3종 혼재·**장비 착용 표현 불가**·인물 38인을 텍스처 5장으로 돌려막기.
  → core **`art/CharacterArt.ts`** 신설로 아트 격자를 코드가 소유(셀 **32x32 아트 px** · 몸 26행 · **정수 x2** = 몸 52px로
  레이아웃 불변 · 레이어 14종이 `pose()` 앵커만 참조 = **신규 장비 = 함수 1개**) ·
  **바닐라 = 나체 + 언더웨어**(여성 상의 속옷 포함), 상의·하의·신발은 **전부 장착 아이템** ·
  걷기 **4프레임 접지/통과**(접지 2장은 좌우 체중 이동으로 구분) · 얼굴 2x3 눈+하이라이트·눈썹·입 4종·홍조·**여성 속눈썹 1px** ·
  **`art/CharacterCast.ts`** 41인 결정적 분화(중복 0) · **`CharacterCreateScene`** 신설(12행 · 미리보기는 실게임 시트 그대로) ·
  **과증식 해양생물 4종**(보름달물해파리·노무라입깃해파리·별불가사리·아무르불가사리 — 수면 아래를 눌러 **빙산의 일각**처럼 걸치고,
  해파리는 **해안선 6칸 밖**에서 5분에 한 칸 표류 · 수거는 **씬 전환 없이 훌치기**) ·
  **퀘스트 158 → 186**(신규 아크 4 · 누적 XP 2,809,150 — 기존 보상 불변) · **만렙 200에서도 XP 누적** ·
  건물 프리팹 11장 → POI별 변형.
  ⚠ **비정수 배율 금지** — 배치는 항상 `setScale(정수)`. ⚠ **캐스트 해시 규칙 변경 = 구세이브 NPC 얼굴 전부 변경**.
  어종 실사는 **픽셀화 예외 유지**(사용자 결정). 상세 `docs/wiki/03-WORKLOG/2026-09-16-138-character-base-nuisance-quests.md`.
- **이전 작업**: 137차 — **퀘스트 증설 38편(회귀 고리·인물 연결) + 지역/인물 색인 + 일지 서브 트랙 스크롤**
  (사용자 지시 3건): 챕터가 넘어가면 다음 지역이 열리는 구조라 **이전 맵이 버려지던 맹점**을 메웠다 —
  메인 3편(**M2-11 되돌아온 상자**: 부산 상자 규격을 속초 좌판으로 / **M4-11 속초의 손님**: 낚시어선업 배로
  강두철·탁만수 승선 / **M6-09 고래마루의 마지막 손님**) + 서브 35편(챕터당 5) 신설 →
  **메인 68 · 서브 90 = 158퀘** · 아크 17 → **19** · **회귀 편 20 → 33** · 누적 XP 2,415,600.
  신규 아크 **N18 도현수**(7챕터 각 1편) · **N19 정옥선·탁만수**(오십 년 — "npc간 서로 좋아하는 설정", 심부름꾼은 늘 플레이어) ·
  인물 연결(강두철↔고만석 서른 해 전 같은 배 · 배누리 진로 · 이수연 식당) ·
  일지 서브 트랙 **윈도우드 렌더 + 휠 스크롤**(Ch5 12아크 15편) ·
  아티팩트 **섹션 네비 + 지역별 퀘스트 11카드(회귀 배지) + 인물 다중 아크**.
  ⚠ **id 번호는 발행 순서, 배열은 챕터 순서** — 기존 id를 다시 매기면 세이브가 진행을 잃는다.
  ⚠ 메인 삽입은 **각 챕터의 마지막(해금) 퀘스트 앞**에(배열 순서 체이닝).
  무결성 0건 · 실렌더 PASS. 상세 `docs/wiki/03-WORKLOG/2026-09-15-137-quest-expansion-return-loops.md`.
- **이전 작업**: 136차 — **스풀·베일(전유동·흘림) + 밑걸림 대처 + 장비 고장 + 일지 2트랙 + NPC 마커**
  (사용자 지시 6건): 채비에 **원줄 길이 `lineOutM`** 상태를 신설 — 팽팽하면 반지름 `lineOutM` 구면에 갇혀
  **호를 그리며 끌려오고**(닫힘 60초 거리 20 → **9.56m**), **R 홀드 = 베일 개방**이면 줄이 나가
  채비가 조류를 타고 흘러간다(개방 60초 **20m 유지 · 원줄 34.7m · 수심 12m 바닥 도달**) — 전유동·흘림이
  비로소 반유동과 다른 조법이 됐다. 파이팅 R = **줄 주기**(요구 장력 ×0.18 → 텐션 54 → **5.2** · 거리 19.7 → 27.2m ·
  바늘털이 정대응 / 여박기 오대응). ⚠ 자유 방출엔 **페더링 상한**(`maxSlackM 4`)이 필수 — 없으면 슬랙 무한 누적,
  넘으면 **백래시(원줄 꼬임) 신호**. **밑걸림 선택창**(끌어당기기 10/10/30/50 ↔ 끊기 50:50) ·
  **장비 고장 8종 + 내구도**(core `GearDurability.ts` — 저가 기준 확률 × 품질 보정, 내구도 = 고장이 잦아지는 계수.
  사이소 로드 200회 · 초릿대 1.5%/캐스트 · 수리비 3,000~28,500원 · 수리점 탭 · 자가 수리 · 슬롯 우하단 경고 배지) ·
  **일지(J) 2트랙 재작성**(레일 챕터 7 / 메인 스파인·서브 아크 · XP는 상세 보상에만) ·
  **NPC 퀘스트 마커**(미니맵 상점 8종 + 빨강 느낌표 = 새 의뢰 / 노랑 물음표 = 지금 해결 가능 · 필드 머리 위) ·
  픽셀 아이콘 29 → **42종**. ⚠ **절지 파단은 라인이 로드 등급 1.5배 초과일 때만** 굴린다(무작위 징벌 금지) ·
  `TUNING.spool` 9키는 **확정값**(136-b). 실렌더 전 항목 PASS · pageerror 0.
  **136-b 후속**: 줄 주기 **슬랙 유예**(1.5 → 3.75초) + **슬랙 경고 바**("바늘 빠짐까지 N초") ·
  밑걸림 선택창 **확률표 노출** · 도움말 **스풀 · 밑걸림 · 장비 고장 3토픽**(ko/en · 실캡처 8장) ·
  영문 사전 `i18n/en_gear.ts`.
  상세 `docs/wiki/03-WORKLOG/2026-09-15-136-spool-snag-durability.md`.
- **이전 작업**: 135차 — **미완성 접시 저장 + Ch1 실시스템(품삯·사이소·가방) + 법 강제 3단계**
  (사용자 지시 3건): 플레이팅 도중 접시를 내리면 **미완성 접시 아이템**(`InvItem.plateWip`)으로 보관 —
  상세보기 `플레이팅 진행 6 / 16점 (38%)` · 슬롯 **좌하단 % 배지** · 도마 재장착 시 방위·회전 복원 ·
  우클릭 **[해체하기]**. **판매 0원**(가격표가 만석 전제 — 사용자 결정) · **섭취 가능(식기 회수)** ·
  신선도는 **가장 오래된 조각 계승**(`msToSpoiled`).
  Ch1은 core **`DayJobs.ts`**(품삯 3종 — 얼음 12,000×2 · 그물 8,000×3 · 공동작업 25,000×1,
  행동력 소모 + 하루 상한) · 대화창 **일감 스트립** · **생활용품점 사이소**(속초 POI 17곳) ·
  **가방 사다리 1단계**(간이 백팩 → 인벤 **25 → 30칸**, 확장 칸 점유 시 해제 차단)로
  **M1-02·04·08·10을 `manual` → `auto`** 배선. **법 강제 = 3단계**(`enforceRodSell` 0/1/**2 = M1-06 완료 후**·기본 2).
  ⚠ 점검 실버그 6건 — **완성 접시가 항상 `'fresh'`라 '나쁨' 조각을 정가 접시로 세탁**할 수 있었다 ·
  미완성 접시만 보유하면 **이어담기가 막다른 길** · 부패 조각 배치 · `D--3` · 섭취 시 접시 소멸 · 배지 3px 침범.
  ⚠ **시드 tackle 29개 > 기본 25칸** — slot 25~28이 그리드에 안 보인다(기능은 정상 · 가방 착용 시 열림).
  ⚠ **M1-06 → M1-11 구간은 어획물 판매가 전면 불가** — 현금은 품삯뿐(일당 상한 73,000원).
  실렌더 22/22 + 35/35 · pageerror 0. 상세 `docs/wiki/03-WORKLOG/2026-09-15-135-wip-plate-dayjobs-law.md`.
- **이전 작업**: 134차 — **스토리 「조행록」 정립**(사용자 스펙 v3 + 지시 "플레이어 시나리오로 정립하고 인게임 요소를
  최대한 반영"): `.agents/STORY_SPEC_v3.md`(원안 · 139차에 **v4로 승계**) ·
  `.agents/PLAYER_SCENARIO.md`(5부 7챕터 요약) · core `types/Story.ts`·`rules/FisheryLaw.ts`(5조·T1~T7)·
  `StoryQuestDatabase`(**120퀘** + `validateStoryQuests` §8-1 XP 계약)·`JournalPages` 17장·`StoryArcs` 17아크 ·
  client `StoryStore`(진행 엔진·세이브 `story`)·`DialoguePanel`·`JournalPanel` 재작성·속초 NPC 7·HUD `D-nn`·판매 창 법 판정.
  ⚠ **법 강제는 OFF**(`TUNING.law.enforceRodSell = 0` — 켜면 테스터 수입원 소멸, 사용자 결정) · 가방 사다리는 core 모델만 ·
  자동 추적기 없는 목표는 `manual`(대화 [다음 단계 진행]). 실렌더 12/12 · 무결성 0건.
  상세 `docs/wiki/03-WORKLOG/2026-09-14-134-story-quests-law.md` · 시스템 페이지 `02-SYSTEMS/story-quests.md`(S22).
- **이전 작업**: 133차 — **어종별 체장-체중 관계(LWR) + 체형별 파이트 물리**(사용자 지시 "어종별 무게와 길이의
  비중을 디테일하게 / 갈치같이 길쭉한 어종이 주는 힘과 압력이 다르게"): 무게 모델 `wf × L³`(지수 3 고정) →
  **`lwrA × L^lwrB` + `bodyForm`** 교체 · **56종 전수 재보정**(구 모델은 체형을 못 담아 **갈치 전장 1m = 10kg** —
  실제 ≈ 0.7kg · 붕장어 1m 5kg ≈ 1.4kg · 갯장어 2m는 반대로 과소). 감성돔은 사용자 제시표 회귀 **a 0.0179 · b 2.95**
  (40cm **953g** · 50cm 1.84kg · 60cm 3.15kg — 전 구간 적중), 갈치는 FishBase *T. lepturus* a 0.00032 · b 3.17.
  **비만도** 신설(겨울 +7% · 산란기 +9% · 직후 −8% · 개체차 ±5% → 상세보기 `115% · 아주 통통함`) ·
  **체형 7종 파이트 배수**(장어·리본형 = 정적하중 ×0.55 · 추력 ×0.62 · **1.2Hz 요동** · 바늘빠짐 ×1.45 /
  납작형 = ×1.9 판자 저항 — 어종군 burst와 **직교**).
  ⚠ **드랙 결함 발견**(132차 백로그 I의 실체) — 상한이 **"릴을 안 감을 때"에만** 걸려 표준 채비+표준 어종이
  전멸했다(감성돔 50cm·1.5호 랜딩 **0/300**) → 감는 중에도 미끄러지게(`reelDragCapFrac`) + **충격 하중만 파단**
  (`shockBreakFrac`) + 유영 부하 0.22 → 0.10 + **체급 보정 안전대**(`safeFloorFrac` — 소형어 상시 '느슨함' 판정 해소).
  검증 core 20/20 · 시뮬(안내대로 감성돔 87% ↔ 라이트 채비 대물 18% · 부시리 2호 목줄 0% 유지) ·
  실렌더 복섬 10m **10.0s** / 20m **15.0s**(사용자 목표 정합). ⚠ **판매가·수율이 무게 비례로 이동**(구세이브 어획물은 저장값 유지).
  상세 `docs/wiki/03-WORKLOG/2026-09-11-133-lwr-bodyform-fight.md`.
- **이전 작업**: 132차 — **파이트 거리 정합**(사용자 리포트 "발앞인데 진행도가 한참 뒤쳐진다"):
  랜딩 진행도가 **거리와 무관한 자체 시계**(릴링 +11/s ≈ 9초 고정)였고 `distM`은 하한 1.2m라
  10m 착수는 7초에 발앞에 닿았다 → **발 앞에서 진행 바만 차오르는** 증상. 진행도를 **거리 진행도**로
  재정의하고 core `FightingPhase`의 내부 축은 **제압도(`subdue`)** 로 분리 ·
  씬 `stepFightKinematics`(횡·거리·수심) 신설 — **도주는 언제나 플레이어 반대쪽**(수평뷰 좌/상/우 ·
  수심뷰 좌) · **무대 좌표는 물리에서 파생**(이중 시뮬 제거) · **릴링 중엔 거리가 한 프레임도 안 는다**
  (`reelHoldCap`) · 완전 제압 시 저항 소멸 → 끌어오기.
  ⚠ **24g 복섬은 바늘털이 정대응이 곧 바늘빠짐**이었다(게이지를 임계까지 못 올리는 체급) → 체급 게이트.
  `TUNING.fightDist` 18키 + F8 13종. 시뮬 복섬 10m 8.1s / 20m 13.0s · 실렌더 랜딩 시 distM 2.44~2.49m.
  상세 `docs/wiki/03-WORKLOG/2026-09-11-132-fight-distance-coherence.md`.
- **이전 작업**: 131차 — **도움말 라이브러리 현행화 + 제작 UI 영문화**: 게임 안 도움말이 123차에 멈춰 있어
  **125~129차에 이미 구현된 생존 6토픽이 "준비 중"으로 남아 있던 것**을 전부 ready로 재작성
  (생존 지표·음식·상태이상·기절/사망·병원·상태 패널) · **'제작' 카테고리 신설**(기본 = U 창 제작 탭 /
  고급 = 설치 제작대 [F] · 성공률·재료 절약·Shift+F 회수) · 스킬 트리 토픽 **3페이지화**
  (**Σ 215 = 만렙 200 + 면허 15** · 해금 조건 · 시너지 히든 `???`) · 단축키 `U`·`K` 현행 + **`Shift+F`** 추가 ·
  실캡처 8장(ko/en × 4).
  ⚠ **129차 제작 UI가 영문 사전에 전혀 없었다**(도면·재료·버튼·로그가 en에서 한국어) →
  `I18n.buildRuntimeDict`에 제작 DB 합류 + 사전 156·규칙 16. **130차가 바꾼 스킬 포인트 헤더 형식 때문에
  죽어 있던 122차 정규식**도 함께 수정. 도움말 문자열 **356개 미번역 0**(착수 시 98).
  ⚠ **합성 문자열은 사전을 비껴간다** — `` `Tip · ${tip}` `` 은 붙이기 전에 `t()`로 번역.
  실렌더 ko 10/10 · en 13/13. 상세 `docs/wiki/03-WORKLOG/2026-09-11-131-help-library-craft-progression.md`.
- **이전 작업**: 130차 — **스킬 트리 확장 (a)(c)(d)(e)**: (a) 생존 지표 **최대치** 3노드
  (`maxHunger`/`maxHydration` 신설 — **소모 속도와 다른 축** · 임계 20%는 **비율**로 재정의) /
  (c) **행동력 확률적 면제**('요령' 4%p/랭크 — 비용 절감이 아니라 가끔 아예 안 든다) /
  (d) **해금 조건**(레벨·면허·카테고리 숙련 — 선행 랭크와 직교 · 기존 7노드에 부여) /
  (e) **시너지 히든 5종**(비용 0 · 조합 완성 시 **자동 습득** · 미해금 시 `???`).
  ⚖ **등식 재정의 Σ(유료) 200 → 215 = 만렙 200 + 면허 보너스 15** — 세이브는 랭크만 저장하고 포인트는
  DB 비용으로 재계산하므로 **기존 노드 비용을 깎으면 찍어둔 세이브의 사용 포인트가 소급 변동한다**.
  그래서 좌변이 아니라 **우변(예산)** 을 늘렸다("만렙 + 전 면허 = 전 스킬 마스터").
  ⚖ **티어 수용 6 → 8** — 간격만 줄이던 124차 방식은 7노드에서 **6.7px 겹쳤다** → **노드 높이까지 축소**.
  실배선 33 → **43/92**. 검증 core 43/43 · 실렌더 24/24.
  상세 `docs/wiki/03-WORKLOG/2026-09-11-130-skill-tree-expansion.md`.
- **이전 작업**: 129차 — **성장·생존 P7**: core `CraftingDatabase`(**도면 14종** — hand 8/workbench 6 ·
  성공률·재료 절약) + `CraftingStore`·`CraftBoard`(**U 창 '제작' 탭**과 **고급 제작대 [F] 팝업**이 공유) ·
  **약국 신설**(구급품 3 + 재료) · `GameState.applyRemedy`(상태이상 치료) · **홈타운 보건소 [F] 진료**
  (45,000원 → 독감·탈진 + HP 전량) / **피로 회복 음식**(일반 0 · 국물·커피 8~20 · **보양식 30~40 + 드레인 버프** ·
  **카페인 리바운드는 활동 시간**) — 수치는 **`data/ItemVitals.ts` 단일 테이블**(상점·시드·**세이브 백필** 공유) /
  제작 스킬 카테고리 해제 · 실배선 24 → **33/83** / 신규 아이템 26종 **전부 픽셀 아이콘**(아트 15종 신설).
  ⚠ 실버그 3건 — 설치물 **[F]가 기능 대신 회수만** 되던 것(수조도 동일) → **[F]=기능 / [Shift+F]=회수** ·
  통발 도면 id 규칙(`inv_trap_<specId>`) 불일치 · **`skillMult`로 add 모드 효과를 읽으면 조용히 1**.
  상세 `docs/wiki/03-WORKLOG/2026-09-11-129-crafting-firstaid-fatigue-food.md`.
- **이전 작업**: 128차 — **상태이상·생존 지표 픽셀 아이콘화 + 앵커 호버 팝업**: 이모지 금지 규칙 정정
  (막는 것은 이모지·텍스트 장식이고 **시각 아이콘은 권장**) · `tools/gen_pixel_icons.py` → `PixelIconArt` →
  `ui/PixelIcon.ts` 파이프라인 신설(13종) · `StatusBadge {ko,en,color}` → **`{icon,color}`** ·
  호버 팝업 = 커서 추종 폐기 → **앵커 확장 + 매초 갱신**.
  ⚠ **헤드리스에서 `scene.time` 타이머는 발화하지 않는다** · `GameState.vitals` 게터는 새 객체를 만든다(`commitVitals`).
  상세 `docs/wiki/03-WORKLOG/2026-09-10-128-pixel-icons-hover-tip.md`.
- **이전 작업**: 127차 — **성장·생존 P5·P6**: core `CastWeather.ts`(**풍속 3m/s 초과부터** 맞바람 비거리
  ×0.73 = 실비행 466px → **332px** · 옆바람 산포 ×1.6 + 착수점 밀림 · **비 = 유속 ×1.27·밑걸림 ×1.33**) +
  `TUNING.castWeather` 13키·F8 8종 · `CastLaunchParams.speedMult`·`solveCastPower` 신설 /
  **스킬 실배선 13 → 24/83**(생활 6·채집 속도·정투·바람 읽기) + `rollStatus`(면역력)·`resetSkills` +
  **리스펙 아이템**('조업 재교육 이수증' 20만원) / 조준 **400ms 홀드 = 필요 파워 눈금** · 산포 링 ·
  바람 화살표 · 비행 카메라 팔로우.
  ⚠ **비거리 배율은 완력이 아니라 `speedMult`(수평 속도 전체)** — 완력에만 곱하면 −7%에 그친다(실측) ·
  맞바람 손실과 횡풍 밀림은 **분리**(이중 계산 금지) · 바람 보정 스킬은 **손실의 20%만**(사용자 "아주 미비") ·
  **core 재빌드 후 `node_modules/.vite` 삭제 + dev 재시작** 안 하면 실렌더가 옛 core를 본다.
  상세 `docs/wiki/03-WORKLOG/2026-09-10-127-cast-weather-skill-wiring.md`.
- **이전 작업**: 126차 — **성장·생존 P3·P4 + UI 이모지 금지 규칙**: 상태 패널 '대'에 경험치 바 ·
  허기/수분 컴팩트 바 · **수치 호버 팝업**(상시 숫자 제거) · **상태이상 스트립**(패널 밖 색 칩 + Ko/En 툴팁) —
  '중/소' 회귀 0 · 패널 높이 불변 / **기절·사망 FSM**(`ui/CollapseOverlay.ts` — 눕기 90° · 비네트 0.5초 ·
  2초 후 팝업 · 기상 = 피로도 80% 캡 · 사망 = 재화 15%·인벤 유지·HP/허기/수분 50%) ·
  **UI 이모지·특수문자 금지 전면 규칙화**(AGENTS §4·§8 · CLAUDE 7번 · ui-panel 스킬) +
  `StatusEffects.icon` → `badge{ko,en,color}` 교체.
  ⚠ 게이지 rect는 **`RegionHud.barRects()` 단일 소스**(렌더·히트 공유) · 쓰러짐 정리는 create/shutdown 양쪽 ·
  **부활은 저장하지 않는다**(침대 저장 정책 불침범).
  상세 `docs/wiki/03-WORKLOG/2026-09-10-126-vitals-panel-collapse-fsm.md`.
- **이전 작업**: 125차 — **성장·생존 P2**: core `types/Vitals.ts`(활동 드레인 6종·행동 비용 8종·
  임계 20%·온도 계수·수면) + `types/StatusEffects.ts`(11종·진행 체인·자연치유·재발) ·
  `GameState.vitals` 세이브 · 배선 9곳(필드/1인칭 드레인 · 캐스팅/파이팅/손질/회뜨기/출조 비용 ·
  음식 회복치 14종 · 침대 수면 · **채집 부상 → 골절/출혈/생물중독 승격**).
  ⚠ **드레인은 활동 시간만** — 씬 update()의 일시정지·모달 가드 뒤에서만 `tickVitals` 호출(= 오프라인 정지) ·
  **HP 원본은 `player.stamina`**(vitals.hp는 뷰). 허기·수분 UI는 P3.
  상세 `docs/wiki/03-WORKLOG/2026-09-10-125-vitals-status-effects.md`.
- **이전 작업**: 124차 — **성장·생존 P1**: core `types/Progression.ts`(레벨 200 · `xpToNext = 25n^1.5+75` ·
  어획 XP = 희귀도×체장·첫 발견 ×5 · 활동 XP 5종) + `GameState.grantXp`(만렙 캡·구세이브 무마이그레이션) +
  배선 4곳(손질/회 ×등급·채집·준법 방생) · **스킬 트리 44→83노드 Σ=200pt**(39 증설 `wired:false` ·
  **제작 카테고리 신설(잠금)** · 무결성 검사) · **제작 설계 정정**(기본 = **U 창 '제작' 탭** 핸드크래프팅 /
  고급 = 설치 제작대 [F] 팝업 — 스펙 ⚖#8·§2-5 재작성).
  ⚠ 스킬 배치 제약: 티어 0~3 · 티어당 ≤6노드 · XP는 반드시 `grantXp` 경유.
  상세 `docs/wiki/03-WORKLOG/2026-09-10-124-progression-p1-level200-skilltree.md`.
- **이전 작업**: 123차 — **성장·생존 통합 스펙 v2**: 사용자 원안(레벨 200·스킬 200pt·생존 지표·상태이상·
  기절/사망)을 122차 실코드에 정합한 `.agents/PROGRESSION_SURVIVAL_SPEC.md` 신설(⚖ 결정 14건) +
  **도움말 '성장 · 생존' 카테고리 선반영**(ready 4 + planned 6 · 영문 사전 — en 잔존 0).
  상세 `docs/wiki/03-WORKLOG/2026-09-10-123-progression-survival-spec-v2.md`.
- **이전 작업**: 122차 — **121차 피드백 5건**: 상호작용 **E → F**(E = 장비창 전용) · 영문 잔여(도감 라벨·지역/구역 설명·
  출처 화면 → 데이터 `…En` 필드) · 팝업 **✕ 우측 상단 통일** · HUD 명패 로케일 재배치 · **면허(L)·스킬(K)·일지(J)** 패널
  (스킬 40종/실배선 13 · 면허 6군+7종 · 일지 골격 — 시스템 페이지 `02-SYSTEMS/progression.md` S21 신설).
  ⚠ 새 팝업은 `openPopup`이 밴드 최상단으로 올린다(`raiseToTop`) · 금액 규칙은 숫자만 · 홀드 키 변경 시 `addKey`도.
  상세 `docs/wiki/03-WORKLOG/2026-09-09-122-feedback-fkey-i18n-panels-skills.md`.
- **이전 작업**: 121차 — **인-맵 채집(해루질) + 어촌계 어장 실폴리곤 + 강원 조례 + 통발 아이템 설치/수거**
  (사용자 지시 "실제 해루질 요소를 현재 맵 안에 발현"). 속초 맵 갯바위·테트라포드 발밑·안벽에 시간 시드 스팟(≤220) ·
  야간 헤드랜턴 반경 · **[F] 홀드** 채집(122차 E→F)(도구 4종 — core `ForagingEngine`) · 국립해양조사원 어장 SHP →
  `fishfarms.json`(`tools/extract_fishfarms.py`, 원본은 `docs/reference/gis/` gitignore) · 조례(어장 안 5종 적발/압수/벌금 ·
  채집물 판매 금지 · 산란기 도루묵) · 통발 = 아이템(직판장 8종 · **T** 설치 · wall-clock 침지 · [F] 수거·반환).
  ⚠ 실데이터 파고 ≥1.5m면 채집이 막힌다(정상) · `TUNING.forage/trap`는 mockup(F8) · 레거시 별도 씬은 무수정(폐기 후보).
  상세 `docs/wiki/03-WORKLOG/2026-09-09-121-inmap-foraging-fishfarms-traps.md`.
  120차 — 영어 잔여 마무리(상호명 `nameEn`·지명 필드 구조화·콘텐츠 산문 사전 → 전수 잔여 0).
  119차 — 118차 피드백 8건: **HUD 타이틀바(캡션바) 규칙 신설**(상태 패널 밴드 · ◱◐ ↔ HP 바 4px
  겹침 해소 · 채널 밴드 최소 높이) · 지역 채널 **3단계 표시**(시각 제거 / 10자 말줄임) · 월드맵 홈 버튼이
  뒤로가기 **클릭을 삼키던 버그** + 흐름 배치 · 랜딩 후 **조류·바람 정지 + 발앞 수렴 + 결과창 좌클릭 차단** ·
  수심 물고기 아이콘 좌우 반전·1.2배·희귀도 색 · **i18n 규칙 캡처 재번역**(`applyTemplate`) + 사전 656항목 +
  지명(`i18n/places.ts`) + **캡처 주석 영문판 21장**(`--lang en`) · 버튼 '도움말' · 수량 우측 상단.
  ⚠ 신규 UI 규칙 2건: **타이틀바 전용 영역** · **wordWrap 아래 고정 y 금지(흐름 배치)**.
  118차 — 117차 피드백 8건: **HUD 실버그 2**(상태 패널 미표시·채널 중복) · 캐스팅 힌트 = 맵 직접
  클릭만 · **영어 설정**(`i18n/` Text.setText 훅 사전 ≈620 — 미수록은 한국어 잔존) · ESC [환경설정] · 도움말 실캡처
  21장 + 파이트 튜토리얼 합성 삽화(`__FP.devForceFight`) · 상태 패널 3단계 정보량 축소 + 호버 툴팁 · 단축키 버튼 토글. 117차 — **도움말 라이브러리**(F1·단축키 버튼 · 8카테고리 39토픽 · 실캡처 주석) +
  **HUD 크기·투명 단계** + **테스터 피드백 7건**(파이팅 **물리 텐션** = 체중×어종군 가속×대응 ÷ 라인 강도 ·
  희귀도 6단계 색 · 입질 튜토리얼 선표시 · 루어 전용 원줄·목줄 소켓 · 줄·바늘 상점 · 지그헤드 봉돌 면제).
  `TUNING.fightPhys`는 **mockup — F8 조율 대기**. 116차 — 조도 시트 **실제 위치**(794,427) 재적용(면적 72×60).
  115차 — **조도 드론 사진 → 갯바위 타일 시트**(`tools/pixelize_islet.py` —
  HSV 분류·휘도 순위 균등화·2px 그레인·물 투명 · 시트 1장 런타임 슬라이스).
  114차 — 방파제 **단면 자동 분류**(테트라포드/사석/상판) + **항로표지 등대**(`extract_lights.py`).
  113차 — 차량 배치 재설계(주차장 구역 한정 40~70% · 도로 위 정차 0 · 노상 0.8대/화면).
  상세는 `docs/wiki/03-WORKLOG/` 112~118 + `IMPLEMENTATION_PLAN.md` 이어받기 절.
- **🚧 재개 지점**: ⓪ **136차 잔여**(스풀 시각화 · 이빨 어종 루어 파손 · 내구도 0 이후 처리 — 백로그 K)
  ① ~~149차 구멍치기~~ ✅149 → ~~불요리~~ ✅154 → **농장** ← **현재 지점**
  (농사 스킬 배선·타입 정의 · S8 §5 E1~E5). ② **맵 확정 — 사용자 육안**(109~115차 신설분: 래스터 도심 `'r'`·교차부 마킹·
  **차량 밀도**·**방파제 테트라포드+등대**·**조도 갯바위 주간 톤**) ③ **OSM land 14개 확장**
  (지역마다 terrain ↔ OSM 대조·스폰 검수는 사용자 동반, 서해 taean은 seaEdges 조정 가능성)
  ④ **두족류 수동 검증**(무늬오징어 잔여 F9 + 문어 좌표 **재실측** — 100차 스프라이트 교체로
  subjectRect 변동) ⑤ 1회성 외부 작업 — **`py tools/backfill_hospital_poi.py sokcho_v2`**
  (네트워크 필요. 출조 지역 병원 POI — 홈타운 보건소는 이미 동작).
- **완료**: 원물 자유 손질(돔류+방어류) · 넙치류 다섯장뜨기 · 회썰기 2뷰 + 사시미 플레이팅 +
  **미완성 접시 저장**(135차) · **불요리**(154차 — 조합 설치·레시피 8·별 5개·걸어두기) ·
  **스토리 「조행록」 S22**(134차 — 120퀘·법 규칙 5조·일지·NPC 대화·플레이어 시나리오
  + 135차 **품삯·사이소·가방 사다리 1단계 · 법 강제 3단계**) ·
  **성장·생존 P1~P7 + 스킬 트리 확장 + P9 도움말**(레벨 200·**스킬 92노드 Σ=215**·생존 지표·상태이상·
  기절/사망·날씨 캐스팅·제작·구급품·보건소·시너지 히든 · **도움말 11카테고리 44토픽 + 제작 UI 영문화**) ·
  무늬오징어 20스테이지 · 한치·문어 개방 + 두족류 회뜨기 · 발견 도감 · **OSM 심리스 속초**(100차) ·
  맵 편집기 F7·도로 벡터·교통(101~102) · 해안 타일셋 3종(103~106) · **래스터 보강 Phase 1·2**(107~109) ·
  교차부 마킹 클리핑(110) · **차량 배치 3연속**(111~113) · **방파제 단면+등대**(114) ·
  **조도 사진 갯바위**(115~116) · **도움말 라이브러리 + 파이팅 물리 + 희귀도**(117) · **HUD 수정 + 영어 설정 + 실캡처**(118) · **인-맵 해루질·어장·조례·통발 1차**(121).
- **지형 고도(DEM) 방침**(2026-09-03 사용자 결정): **낚시 확립 후**로 후순위. 착수 시
  **B(해안 지형 변형) 최우선**, **경사로 통행을 막지 않는다**(채집·광질·이벤트 후보 보존).
  근거·방침 = `.agents/RASTER_UPLIFT_AMENDMENT.md` §4. 선행 조건: `pip install rasterio`(미설치).
- **향후 시스템**(사용자 계획): **채집(해루질 포함)·광질** — 실제 재현이 아니라 **농사·건축 제작
  재료 + 이벤트**용. 고도·경사는 차단이 아니라 **자원 배치 태그**로 소비한다.
- **사용자 대기 에셋**: ① 오징어 **껍질 완전 분리본·아가미 붙은 안쪽면·3분할 결과**
  ② 광어 **등쪽 단면 투명본** ③ 갑오징어 속껍질 ④ 한치 전용 실사(현재 무늬오징어 공유) ⑥ **기절·사망 눕기 스프라이트**(눈 감은 2프레임 man/girl)
  ⑤ OSM §11 비주얼 4레이어 에셋(타일셋 7군·프리팹 8종·프롭 6종·NPC 4종).
- ⚠ 방어류/돔류 손질은 마감. **퀘스트/스토리는 모든 컴포넌트 구현 후 도입.**
- **이후 대과제**: 농장, 스시, 요리 후보 확장·경영(식당 납품), CraftScene, 멀티(Phase 8),
  Tauri/Steam(Phase 9).

## 모노레포 구조

- `packages/core` (`@tra/core`) — 순수 TS 게임 로직. **Phaser/DOM/브라우저 API 절대 금지.** 새 파일은 반드시 `src/index.ts`에서 export.
- `packages/client-pc` (`@tra/client-pc`) — Phaser 씬 + UI. 게임 로직은 core에서 import (직접 구현 금지).
- `packages/server` (`@tra/server`) — Express + Socket.IO 멀티플레이 서버 (Phase 8 예정).
- `packages/map-builder` — **deprecated**(2026-09-02, 워크스페이스 제외 — 빌드 미참여). 정본 파이프라인은 루트 `tools/`. 8차 GIS 잔재 보존용.
- `apps/tauri-wrapper` — Tauri v2 데스크톱 패키징 (Phase 9 예정).
- `tools/` — 루트 유틸 스크립트 (`build_region_maps.py`, `pixelize.py` 등).
- `pixelazed/` — 지역 실지형 픽셀 지도 원본 PNG (타일맵 파이프라인 입력).
- `food assets/` — 어종/손질/부산물 실사 원본 (파이프라인 입력 — **이름 변경 금지**, 도구 경로 참조 다수).
- `assets/` — 기타 원본 (2026-08-05 정리): `branding/`(타이틀·아이콘 소스) · `characters/`(man/girl 원본 — 소비본은 `public/characters/`) · `guide/`(sashimi_pixel_guide.svg — pixelize_butchery 입력).
- `docs/` — `reference/`(공공 API 활용가이드·09.수심.zip·경락 CSV 등 외부 데이터 원본) · `mockups/`(UI 목업 — game_guide_hub.html = 가이드 삽화 19장 재렌더 소스).

## 자주 쓰는 명령어 (Windows, 레포 루트 기준)

```bash
npx pnpm install                                     # 의존성 설치
npx pnpm run build                                   # 전체 빌드 (3패키지 — core·client-pc·server)
npx pnpm --filter @tra/core run build                # core만 빌드
npx pnpm --filter @tra/client-pc run typecheck       # 클라이언트 타입 체크
npx pnpm --filter @tra/client-pc run dev             # 개발 서버 → http://localhost:5173
py tools/build_region_maps.py <region>               # 지역 타일맵 JSON 재생성 (예: sokcho)
```

- 검증 루틴: 작업 후 `npx pnpm run build` + `npx pnpm --filter @tra/client-pc run typecheck` 통과 필수 (기준: 2026-09-02 **3/3** 성공, 0 오류 — map-builder 워크스페이스 제외로 4/4 → 3/3).
- `noUnusedLocals`/`noUnusedParameters` 활성화 — 미사용 심볼은 제거하거나 `_` 접두사.

## 절대 규칙 요약 (상세는 AGENTS.md §8)

0. **`git commit`/`git push`는 사용자가 직접** (2026-08-14 지시) — 에이전트는 파일 변경까지만,
   완료 시 변경 목록과 함께 "커밋 대기"로 보고.
1. `@tra/core`에 렌더링/브라우저 코드 금지.
2. 하위 씬에서 `scene.start('FieldScene')` 금지 — 반드시 `scene.stop()` + `scene.resume('FieldScene')`.
3. `GameState`(대문자 싱글톤)만 사용, `gameState` 소문자 인스턴스 없음.
4. 씬 키 = 파일명. 변경 시 `main.ts` 동시 수정.
5. `TideInfo`·`SpotType` 등 확정 타입 임의 변경 금지.
6. 파일 상단 JSDoc 및 핵심 주석은 한국어.
7. **인게임 표현 규칙 R1~R10** (2026-09-17 · 150차 사용자 지시 — 상세는 AGENTS §4 「인게임 표현 규칙」):
   개발 용어·내부 id UI 노출 금지 · 미진행 정보 노출 금지(스포일러) · 대화는 타이핑+▼+본문 20px+표준 선택지 3종 ·
   나레이션은 정의문 요약 금지 · 초상은 얼굴 확대 · 텍스트는 창 **아래** 경계도 넘지 않음 ·
   한글 입력은 `ui/TextInput.ts`(IME) · 차량은 캐릭터보다 크게 · 도로 위 건물은 문 좌표를 민다 · 가리면 반투명.
8. **UI 이모지·특수문자 금지 + 텍스트 약어 배지 금지**(2026-09-10 · 128차 정정) — 막는 것은 ① 이모지 ② `[아이콘] [타이틀]`식 텍스트 장식이고, **시각 아이콘은 권장**이다. 아이콘 = **16x16 손그림 픽셀 아이콘**(`tools/gen_pixel_icons.py` → `PixelIconArt` → `ui/PixelIcon.ts`) / 절차 픽셀 그래픽 / 스프라이트. 예외 글리프는 `✕ ◀▶ ◱◐`. 상세 AGENTS §4.

## 타일맵 · 배포 (상세 절차는 스킬 참조)

- 지역 타일맵 추가/재생성 → **스킬 `add-region`** (`py tools/build_region_maps.py <region>` — 파이프라인·타일 문자·맵 그래프·함정 일체).
- 차기 과제: 낚시점 전용 상점(루어 판매), 어탐 레이더(SeabedProfile 조회), 타 지역(여수 등) 확장, POI 세분화, 사운드 이펙트 (IMPLEMENTATION_PLAN §6-5l 차기 참고).
- 테스트 배포: https://sarsah93.github.io/Pixel-Angler-The-Real/ (gh-pages — 최근 9차 배포 2026-09-03 = 89~115차 포함. 재배포 절차는 **스킬 `deploy-ghpages`**).

## 작업 이어받기 절차

1. `.agents/AGENTS.md` 완독 → 2. **`docs/wiki/README.md` 대시보드 + 건드릴 시스템 페이지** 확인 →
3. `IMPLEMENTATION_PLAN.md`에서 현재 단계 확인 → 4. `npx pnpm run build`로 상태 검증 → 5. 빌드 오류 우선 수정 →
6. 구현 → 7. 빌드/타입체크 재검증 → 8. **스킬 `work-log` 절차로 기록**(워크로그 1건 + 시스템 페이지 + 백로그 + AGENTS/PLAN 요약).
