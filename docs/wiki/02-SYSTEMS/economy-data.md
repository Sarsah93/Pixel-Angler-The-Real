# S6·S9. 경제(상점·시세) · 외부 실데이터

> 상태 **🟢 운영** (⚠ 배포 시 CORS 프록시 필요). 관련 차수 13·16·39·42·66·68·79

---

## 1. 목적·범위
아이템 **거래 가격**과 그 근거가 되는 **실측 데이터**. 어획물 가치 산정 · 상점 매매 · 경락 시세 · 기상/어획량.

## 2. 구성
| 계층 | 파일 | 역할 |
|---|---|---|
| core | `simulation/MarketPriceEvaluator.ts` | `evaluateFishSellPrice` — 어종 kg단가 × 중량 × 등급 × 길이 |
| core | `types/Economy.ts` | `SEAFOOD_AUCTION_MAPPING`(어종↔품목·기본단가·`weightExp`) |
| core | `simulation/AuctionEngine.ts` | 경매 |
| core | `api-client/` | 낚시지수 · **MAFRA 경락가** · KOSIS 어획량 · NMPNT 해양기상 · 기상청 단기예보 (+ Mock 폴백) |
| client | `store/ExternalDataStore.ts` | 스타트업 1회 `fetchAll()` 캐시 — 인게임 루프는 캐시만 참조 |
| client | `ui/ShopPanel.ts` · `data/ShopCatalog.ts` | 상점 6종(편의점·마트·직판장·식당·카페·주점) |

## 3. 동작 구조
```
판매가 = evaluateFishSellPrice(어종, 길이, 등급, 시세캐시) × 상태배율 × 0.6(매입할인)
  상태배율: 활어/신선/냉장/냉동 1.0 · 보통 0.5 · 나쁨 0.1 · 부패 0(판매 불가)
  ※ 시세 캐시를 넘기면 kg단가가 대체된다 — getMarketPriceFactor를 또 곱하면 이중 적용
  ※ 완성 사시미 접시(inv_sashimi_plate_)는 0.6 할인 미적용
대형어 폭증 완화: price ∝ weightKg^weightExp (방어 0.4 등)
```

### 외부 API
| 소스 | 상태 | 비고 |
|---|---|---|
| 바다낚시지수(해양조사원) | 실데이터 | 입질 P_base 0.7~1.4 배율 |
| MAFRA 경락가 | 실데이터 | **2023년 동월동일로 매핑**(수록 2000~2023), HTTP 전용 |
| KOSIS 어획량 | 실데이터 | 시도×어종 스폰 가중 0.7~1.8, **별도 인증키** |
| NMPNT 해양기상 | 실데이터 | 76개소 · 기관코드(mmaf)+지점(mmsi) 둘 다 필수 · **파고 0개소·수온 11개소** |
| 기상청 단기예보 | 실데이터 | 비/맑음/흐림 판정의 유일 소스(HTTPS·CORS 허용) |

## 4. 세부과제 현황
| 과제 | 상태 | 차수 |
|---|---|---|
| 5종 API 통합 수집 + Mock 폴백 | ✅ | 7차 계열 |
| dev CORS 프록시(vite) | ✅ | 17 |
| 어획물 실측 기반 판매가 | ✅ | 16 |
| 상태별 판매 배율 + 시세 tier | ✅ | 39 |
| 사시미 가격 체계(모듬 고정 / 단품 kg식) | ✅ | 66·68 |
| 상점 윈도우드 스크롤·오버플로 | ✅ | 54 |
| 어종 경락 매핑(56종) | 🟢 | 79차까지 확장 |
| **낚시점 전용 상점**(루어 판매) | ⬜ | 그리드 오버플로 회피로 보류 중 |
| 식당 납품·경영 | ⬜ | Phase 6 잔여 |

## 5. 잔여·차기
낚시점 상점 · 경영(납품/가격 협상) · 신규 해양 API 5종.

## 6. 함정·불변조건
1. **매칭 테이블은 부분 일치 + 선착순** — `MAFRA_ITEM_TO_SPECIES` / `KOSIS_SPECIES_MATCH`는 **긴 이름을 먼저** 둔다(`말쥐치⊃쥐치`, `강도다리⊃도다리`, `잿방어⊃방어`, `한치` 등). 순서가 틀리면 조용히 오매칭.
2. **API 키는 `.env`(`VITE_*`)** — 소스 하드코딩 금지. 프로덕션 번들엔 인라인되므로 노출을 감안한다.
3. **프로덕션 배포에는 vite 프록시가 없다** — HTTP 전용(MAFRA·NMPNT)은 서버 프록시 필요. gh-pages에서는 Mock 폴백.
4. 시세 이중 적용 금지(§3 주석).
5. **두족류 lengthCm 기준은 종마다 다르다** — 관례는 외투장(79차 한치)이나, **무늬오징어·문어는 전장**
   (무늬오징어 = 실측 캡처 정합 088차: 전장 30cm 400~500g / 45cm 1.2~1.3kg → wf 0.015, 1kg = 전장 40.5cm+).
   무게식은 `W = weightFactor × L³` 단일이므로 **기준(전장/외투장)과 wf를 반드시 한 쌍으로** 정한다 —
   87차 dev 지급처럼 라벨(외투장)과 wf(전장 어림)가 엇갈리면 ×9 과소/+46% 과대가 조용히 생긴다.
