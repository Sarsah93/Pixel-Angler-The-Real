# S8. 홈베이스 (홈타운 · 집 · 농장 경영)

> 상태 **🔶 부분** — Tier 0 원룸 + 칸 단위 설치까지. 관련 차수 44·46 · 조사 099
> **목표(사용자 확정 2026-08-14): 스타듀밸리형 농장 경영** — 광질(채굴)·채집·해루질·농사를
> 아우르는 생산 축 + 경영 루프. 세부 로드맵은 §5.

---

## 1. 목적·범위
플레이어의 **거점**: 출조 출발지 · 유일한 저장 지점 · 보관(냉장고) · 배치(설치물) · 향후 농사/수조/제작.

## 2. 구성
| 계층 | 파일 | 역할 |
|---|---|---|
| core | `types/HomeBase.ts` | MapObject 인스턴스 스키마 · `WorldObjectState`(removed/moved/placed) · `canPlaceAt` 칸 판정 · `PLACEMENT_DEFS` · `HouseTier` · `computeTravelFare` · `HOMETOWN_OBJECTS`(초기 19개) |
| client | `RegionFieldScene`(hometown) | 오브젝트 렌더·충돌 반영·[E] 상호작용·**설치 모드**(그리드·프리뷰·회수) |
| client | `HomeInteriorScene` | Tier 0 원룸 — 침대(저장)·냉장고·주방·소파 등 |
| client | `FridgeStore` · `FridgePanel` | 냉동 8 / 냉장 16 |

## 3. 동작 구조
```
필드 오브젝트 = 초기(HOMETOWN_OBJECTS) − removed + moved + placed   → effectiveObjects()
설치: 인벤 '설치하기' → placement-request → 그리드 오버레이
      footprint 프리뷰(초록 가능 / 빨강 불가) → 클릭 설치(아이템 1 소모)
      [E] 회수 = 아이템 반환 (이동 = 회수 후 재설치)
저장: locationTag 'hometown_interior' 에서만 canSaveHere() → 침대 [저장하고 쉬기]
교통: 출조 = ₩10,000 차감 / 귀가 = 무료
```

## 4. 세부과제 현황
| 과제 | 상태 | 차수 |
|---|---|---|
| 홈타운 맵 + 오브젝트 19 + 스폰 | ✅ | 44 |
| 실내 Tier 0 + 침대 저장 게이트 | ✅ | 44 |
| 칸 단위 설치/회수 + 영속 | ✅ | 44 |
| 출조 요금·귀가 | ✅ | 44 |
| 냉장고 · 주방 오브젝트 · 실내 스프라이트 | ✅ | 46 |
| 홈타운 어획 규제(볼락류 6종) · 랜덤 날씨/물살 | ✅ | 46 |
| **하우스 Tier 1~3**(평수·주방·지하·2층) | ⬜ | 스키마만 |
| **수조 2종**(활어 상업용 / 관상) | ⬜ | 스키마·상태 골격 완비, 패널 미구현 |
| 텃밭 농사 · 벌목 · 채굴 · 보트 | ⬜ | 상호작용 스텁("추후") |
| 실내 가구 배치 모드 | ⬜ | 스키마 준비됨 |
| 주방 ↔ CookScene/도마 연결 | ⬜ | 불요리 선행 |

## 5. 잔여·차기 — 농장 경영 로드맵 (E1~E5, 099 조사 기반)

**실상(2026-08-14 전수 조사)**: 동작하는 것은 **칸 단위 설치/회수뿐**. 농사·수조·하우스Tier·
벌목/채굴/채집/보트는 전부 타입·"추후" 안내 스텁이다(`till` 핸들러는 코드에 아예 없음 ·
`HOUSE_TIERS`는 어디서도 소비 안 됨 · `AquariumState.fish`는 `unknown[]`).

**선행 결정 — 시간 모델**: 게임 시계는 실시간(`getCurrentGameMinute` = wall-clock).
압축 달력을 새로 만들지 말고 **쿨러·냉장고의 "오프라인 경과" 패턴 재사용**
(작물 = `plantedAt` 절대 시각 기준 lazy 성장, 계절 = 실제 월) — 정체성 일치·재작업 최소.

- **E2 수조 (먼저 — 소~중)**: `AquariumState` 실타입화(InvItem[]) + 세이브 직렬화 +
  `freshnessMult`(0.1) 소비(활어 신선도 정지 = 경제 직결) + 패널 UI(쿨러 패널 재사용) +
  실내(interior) 배치 개방(현재 exterior만 — `aquarium_display`는 설치 불가 상태).

- **E1 농사 (중)**: `CropDatabase`(씨앗·성장 단계·실시간 성장 h·계절 적합도) +
  `FarmPlotState`(칸별 cropId/plantedAt — 세이브) + `till`/plant/harvest 핸들러 +
  성장 렌더 + **레시피 재료 접점**(현재 레시피 13종에 농산물 0 — 채소 레시피 추가).

- **E3 채집류 (각 소)**: 벌목/채굴/갯바위/보트 — 공통 패턴 "도구 게이트 → 드랍 →
  `removedIds` 영속 → 리스폰". 나무 11·바위 2는 이미 배치·렌더됨(핸들러만).
  갯바위 `gather`는 해루질(S14)과 통합 검토. 산출물 = 제작(CraftScene) 재료.

- **E4 하우스 Tier (중)**: GameState에 tier 상태 + `interiorMapId` 기반 실내 씬 파라미터화 +
  승급 구매·features(주방/지하/2층) 해금 + 실내 배치 모드.

- **E5 경영 루프 (중~대)**: 판매 인프라(경매·직판·식당) 위에 납품 계약(어종·수량·기한)·
  수요/시세 대응·재고 관리·일일 정산. 침대 수면 = 정산·피로 회복 지점으로 확장.

## 6. 함정·불변조건
1. **초기 오브젝트는 core TS 데이터**(JSON 아님) — 타입 안전 + 파이프라인 재생성 없이 조정 가능. 타 지역으로 확산되면 JSON 스키마 이관 검토.
2. **저장은 집 침대뿐** — 새 씬을 만들 때 `locationTag`를 설정하지 않으면 저장 불가가 기본값(의도).
3. Phaser `Container.body`는 예약 프로퍼티 — 필드명 충돌 주의(`gridC` 사용).
4. 실내 캐릭터도 `PLAYER_FOOT_SINK` 접지 보정을 적용한다(스프라이트 하단 여백).
