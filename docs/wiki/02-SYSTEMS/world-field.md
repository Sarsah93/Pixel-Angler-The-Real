# S2·S7. 월드맵 · 지역 타일맵 · 필드(이동·환경)

> 상태 **🔶 부분**(지역 3곳 개방 — 속초는 **OSM 심리스 v2**). 관련 차수 3·17·18·19·44·46·73·**100**

---

## 1. 목적·범위
전국 지도에서 **출조 지역 선택** → 실지형 타일맵 필드에서 **이동·상호작용·캐스팅**. 낚시 자체는 [S1](fishing-loop.md).

## 2. 구성
| 계층 | 파일 | 역할 |
|---|---|---|
| core | `types/WorldMap.ts` | 노드 11곳 · `REGION_AREA_NODES`(출조 구역) · 잠금 판정 |
| core | `types/RegionMap.ts` | 타일맵 계약(7문자) + 맵 그래프(legacy) + **심리스 등록**(`SEAMLESS_REGIONS`·`ACTIVE_REGION_MODE`·`RegionMeta`/`RegionPoi`) |
| core | `types/DepthProfile.ts` | 실측 연안 수심(거리→수심 보간·외삽) |
| tools | `regions_config.py` · `fetch_region_osm.py` · `build_osm_tilemap.py` | **OSM 파이프라인** — 지역 17개 bbox 레지스트리 · Overpass 수집 캐시 · 10m/타일 래스터라이즈(flood·마스킹·스폰 스냅) |
| tools | `build_region_maps.py` · `build_depth_profiles.py` | 지형 PNG → 타일 JSON(**심리스 분기 포함**) / 수심 ZIP → 프로필 |
| client | `WorldMapScene` | 핀·툴팁·줌인 진입·교통비 결제·귀가 |
| client | `RegionFieldScene` | legacy 전맵 베이킹 / **심리스 분기**(meta 스폰·OSM POI·엣지 비활성) · 캐스팅 · 조명/날씨 · 설치 모드 |
| client | `scenes/SeamlessChunks.ts` | **청크 스트리밍** — 64타일 청크 · RT 풀 12 LRU · 3×3 상주 · 프레임당 1베이킹 · 근접 충돌 · 벡터 차선 마킹 · 프롭 10종 · `invalidateTiles`(편집 반영) |
| client(dev) | `dev/MapEditorPanel.ts` + `vite.config.ts` 미들웨어 | **맵 편집기(F7)** — 지형/프롭/지붕 페인트 · Ctrl+Z · `patch.json` 저장 · Ctrl+클릭 순간이동(맵·미니맵) |
| client | `RegionHud` · `MiniMap` · `FieldEventManager` · `HydroCurrentRenderer` | HUD·미니맵(대형 맵 = CanvasTexture)·이벤트·조류 시각화 |

## 3. 동작 구조

```
[legacy]  pixelazed/<region>/*.png ──build_region_maps.py──► data/<region>/<mapId>.json
          엣지 전환: 그래프 링크 → scene.restart({entryEdge, entryT})

[심리스]  fetch_region_osm.py(1회 온라인) → pixelazed/_osmcache/
          build_osm_tilemap.py → pixelazed/<region>/terrain.png(손수정 정본)·pois·meta
          build_region_maps.py → data/<region>/seamless.json + pois.json + meta.json
          RegionFieldScene + SeamlessChunks: 3×3 청크 상주 렌더/충돌 · meta.spawn · 엣지 없음
```

- 타일 문자 8종: `.` 육지 `~` 바다 `#` 건물 `,` 잔디 + **`r` 차도 `w` 보도 `s` 모래사장
  `b` 방파제**(walkable). **1타일 = 5m**(101차 확정 — 도로 폭은 차로 수 기준 `ROAD_W_M`/`ROAD_LANES`,
  차도 양옆 보도 프린지 자동). **심리스 타일 = 32px**(Kenney 16px ×2 — legacy 맵은 20px 유지).
- 지면 L1 = Kenney 베이스 타일(`extract_tileset_assets.py GROUND_CELLS` — 맨땅 베이지 포장·잔디·
  아스팔트·회색 보도·크림 모래·청회색 부두) + 절차 전이(포말·젖은 모래·연석·캡·계선주·지붕·그림자).
- 심리스 베이커 = **§11 L1·L3 절차 구현**(101차): 질감 스페클 · 차선 점선/연석 · 신축이음 ·
  계선주·포말·파도·배 · 연결요소 지붕(박공/패널)+그림자 · 나무 스프라이트(y-sort).
  HUD는 `ui/HudPanelStyle.ts` 공용 픽셀 패널(타이틀 명패 포함).
- 바다 = 육지 거리 BFS 6단계 수심 그라데이션 + 결정적 노이즈 암초 (legacy·심리스 공통 팔레트).
- OSM POI(속초 310개) = 청크 상주 시 마커/라벨 · 거래 가능 매핑(마트/직판장/식당/카페 등)은
  기존 건물 [E] 거래 흐름에 편입 · 문 위치 = 앵커가 `#` 안이면 최근접 걷기 타일 BFS.
- 조명: KST 야간/황혼 명암 오버레이 + 건물 창·네온·가로등(파사드 요소는 플레이어 아래 depth).
- 날씨: 기상청 실데이터 → 비 2레이어·물파문 / 소나기 / 진눈깨비 / 눈 / 안개.

## 4. 세부과제 현황
| 과제 | 상태 | 차수 |
|---|---|---|
| 실지형 타일맵 파이프라인 + 속초 7맵(legacy 보존) | ✅ | 6-5e |
| 부산 8맵 + 컴포넌트 자동 연결(`connect_components`) | ✅ | 17·19 |
| 홈타운 단일 맵 + 오브젝트 스키마 | ✅ | 44 |
| 실측 수심 프로필(속초) | ✅ | 8차 |
| 수심 타일 렌더 + 낮/밤 조명 + 날씨 | ✅ | 3 |
| 자전거(R) + 접지/크기 + 배타 액션 게이트 | ✅ | 17·18 |
| 씬 전환 안전망(SceneFade 17곳) | ✅ | 73 |
| 교통비·귀가·잠금 지역 | ✅ | 44 |
| 래스터 보강 — Sentinel-2 fetch·분류·terrain 병합(Phase 1·2, 속초) | ✅ | 109 |
| 래스터 Phase 3 — DEM 경사(해안 변형·음영) | ⬜ | DEM 도엽 재확보 후 |
| **OSM 심리스 v2 — 속초**(파이프라인·청크 스트리밍·POI·검증) | ✅ | 100 |
| 스케일 5m/타일 + 보도 분리 + L1·L3 절차 렌더 + HUD 픽셀 패널 | ✅ | 100 후속 |
| dev 맵 편집기(F7)·순간이동 + 차도 벡터 마킹(대각선) + 프롭 10종 | ✅ | 101 |
| 차선 연속성·차도 폭(차로 기준) + **타일셋 통합**(Gemini·TopDown·Kenney — POI 프리팹·차량·NPC·프롭 40여종) | ✅ | 101 후속 |
| **TR 32 전환 + Kenney 지면 베이스**(L1) — 청크 32타일 | ✅ | 101 후속 2 |
| **건물 2.5D**(하단 2줄 충돌·지붕 y-sort) · 상점 오브젝트 충돌 · 도로선 격자 스냅 · **TrafficSystem**(우측통행 주행) · 지면 접경선 | ✅ | 101 후속 3 |
| **에셋 학습 재구성** — Kenney 건물 키트 채움(지붕 오토타일+벽 모듈) · 직각삼각형 대각 엣지 · 전 오브젝트 충돌 · 배치 격자(녹/황/적)+겹침 방지 · 편집기 탭 팔레트(썸네일) · 정점 그래프 교통+U턴 보간 · 보행자 NPC · 주택/펜스 정확 크롭 | ✅ | 101 후속 4 |
| **도로 벡터 밴드 렌더**(보도→연석→아스팔트 굵은 선, 라운드 조인) · 교차로 박스(마킹 조각 인셋) · 차간 거리/양보 · 막다른 끝 페이드 재등장 · 소형 건물 지붕만 · POI NPC 깊이 | ✅ | 101 후속 5 |
| **편집기 도로 벡터 툴**(정점 이동/삽입/삭제 → `RegionPatch.roads`) · 횡단보도·정지선 벡터 · Kenney 아스팔트 톤 + 반점 · 보행자 밴드 거리 판정 | ✅ | 101 후속 6 |
| **회전교차로**(OSM 태그 → 교통섬·양보선·링 우선·반시계) · 왕복 4차로 이중 황색선 · 일방통행 · 횡단보도 겹침 줄무늬 생략 · **차량↔캐릭터**(접근 대기·충돌 넉백 HP−20%·차량 180s 정지) | ✅ | 101 후속 7 |
| **맵 후속 잔여 일괄** — 사고 추월 · 분리섬(oneway 쌍 쐐기) · 편집기 새 도로 그리기(정점 스냅·접속 삽입) · 지면 오토타일(잔디 블롭 16조합 + 포장 8방위 테두리) · 물 타일셋(수심 버킷 절차 베이크) · 지붕 2×2 패널·대각 스텝 컷 · 신호 교차로(신호등 프롭 + 대형만 스크램블 X) | ✅ | 102 |
| **섬 지역 분류** — `regions_config.access` (`'boat'` = 울릉·독도, 배 출조 콘텐츠 시 개방 — 1차 확장 14개로 분리) | ✅ | 102 |
| **해안 지형 디테일** — 방파제 추론(coastline 조각 폭 ≤10 → `b` 2,510타일) · 해변-바다 연결 · 모래 웜 틴트 · 서프 포말 · 수심 챔퍼+디더 · 테트라포드(외해측) · 섬/암초 렌더(조도) | ✅ | 103 |
| **TTP 타일 시트 적용** — 사용자 시트 2장 그리드화(`extract_tileset_assets.py ttp` — 25스프라이트) · 테트라포드 **소형 유닛 클러스터**(2밴드·플립) · 해변 접경 셀 4방위 회전(바다 색 투명 오버레이) · **외해 마스크**(`computeOpenSea` — 석호·항 내측 배제) | ✅ | 104 |
| **해안 시트 3장 적용** — 실사 항공사진 격자 → 스프라이트(106차에 96셀 전량) · 갯바위 실사 산포 · ⚠ `'b'` 자동 사석 분류·접경 오버레이·자동 테트라포드는 **106차에 폐기**(수동 배치로 대체) | 🔶 | 105·106 |
| **도트 입자 2px 통일** — `_clean(grain=)`으로 해안·TTP·Gemini 리베이크(입자 1px→2px · Gemini 22색) | ✅ | 106 |
| **해변 경계 재작업** — 포장 쪽 노이즈 모래 스필(톱니·검정 경계선·빗변선 제거) · 바다 쪽 포말 셀 2종+미러(연석형 세로줄·벽돌담 해소) · 수심 버킷 알갱이 전이 | ✅ | 106 |
| **편집기 개별 배치** — 시트 셀 126 타일 + 오브젝트 27(자동 생성 `TileCatalog.ts`) · 회전/반전(R/X/Y) · **겹침 허용 자유 배치**(O — 0.25타일, 충돌 없음) · `RegionPatch.tileTex` | ✅ | 106 |
| **도로 마킹 현실화** — 이중도로 황색 중앙선 · 골목 접속부 무절단 · 마킹 아스팔트 클립(직선 횡단보도 우선) · 골목 중앙선 제거 · 차도 안 건물 274타일 런타임 정리 · 편집기 호버 고스트 | ✅ | 106 후속 |
| 방파제 두부(head) 끝단 배치 · 부두 **코너 셀** 자동 방위 매핑 · `submerged_*` 셀 재선정 | ⬜ | 105·106 |
| 자동 프롭↔수동 프롭 겹침 검사 · 차량 신호 준수 · "도로 위 건물" 위치 제보 대기 · 도심 골목 도로 폭(체감 vs 실측) | ⬜ | |
| OSM 지역 확장 — **land 14개**(boat 2개 분리) — fetch→build→검수 반복 | ⬜ | 코드 변경 없음 — 스폰·terrain 검수 필요 |
| §11 에셋 교체(타일셋·프리팹) + L4 NPC | ⬜ | 절차 구현이 자리 — 에셋 도착 시 교체 |
| POI 세분화(비거래 시설 전용 씬·지역 실상점 카탈로그) | ⬜ | 현재 마커+SHOP_CATALOG 프리셋 |
| 스폰 다중화(`meta.spawns[]`) | ⬜ | 스펙 §12-2 |
| 사운드 이펙트 | ⬜ | |

## 5. 잔여·차기

- OSM 확장 절차: `regions_config.py`의 지역 키로 `fetch → build → build_region_maps →
  SEAMLESS_REGIONS 등록 → terrain/스폰 검수` 반복. ⚠ 서해 2곳(taean)은 seaEdges 조정 가능성.
- **래스터 보강(109차)**: 신규 지역은 Browser 정찰로 장면 날짜 확정 → `regions_config.raster`
  딕셔너리 추가 → `fetch_region_raster → classify_raster → build_osm_tilemap`(병합 자동).
  잔여 = 사용자 육안 확정(도심 포장 'r' 46k 체감 — 과하면 병합 가드 조정) ·
  **Phase 3 DEM 도엽 4장 재다운로드**(이 PC 부재 — .env 와 같은 케이스) · b/s 발판 보너스 배선.
- **확장 대상은 `access` 미지정(land) 14개만** — `'boat'`(울릉·독도)는 배 출조 콘텐츠
  (흘림낚시·다운샷·원도권 갯바위) 업데이트 시 개방(102차 — 사용자 결정).
  지역 bbox 안 부속 섬은 별도 조치 불요 — 심리스 빌드는 자동 카브를 안 해(OSM 벡터 정본)
  고립 컴포넌트로 남고, 빌드 리포트 걷기 컴포넌트 수로 검수한다.
- **사용자 제작 에셋 대기**: **조도 위성 픽셀 시트**(작업 중 — 도착 시 섬 렌더 교체).
  방파제·갯바위 시트는 106차에 **전 셀 개별 타일화** 완료(`extract_tileset_assets.py coast → catalog`).
- **방파제 외해측은 이제 비어 있다** — 자동 테트라포드 피복을 폐기했으므로(106차),
  F7 편집기에서 **겹침 허용(O)** 으로 테트라포드를 쌓아 만든다.
- 사용자 육안 확인 대기(101차 이월): **주간 시각 확인 · 정지선 방향**(우측통행 진입 차로).
- 차량 신호 준수(현재 신호등은 시각 전용) · OSM `traffic_signals` 노드 보존(재fetch 동반) ·
  수심 버킷 경계 디더 — 체감/요청 시.
- legacy 7맵 제거 시점은 사용자 결정(v2 안정화 후 권장 — 스펙 부록 3).
- POI 상호작용·사운드는 콘텐츠 확장 단계에서. 상세 미결은 스펙 §13-5.

## 6. 함정·불변조건
1. **타일 텍스처는 맵당 캐시(`rmaptex_`)** — 렌더 알고리즘을 바꾸면 브라우저 풀 리로드가 필요하다.
2. **카브(통로 연결) 직선은 4-연결**이어야 한다 — 대각 Bresenham은 걷기 판정에서 끊긴다(19차).
3. `RegionMapGraph.depthProfileUrl`은 **등록된 지역만** — 없는 URL을 로드하면 dev SPA 폴백이 index.html을 반환해 JSON 파싱 pageerror.
4. **`isTransitioning`은 create()에서 리셋** — Phaser 씬은 재사용되므로 필드 초기값이 재진입에 적용되지 않는다(73차 먹통 원인).
5. 비용 차감은 **전환 가드 통과 후**(더블클릭 이중 차감 방지).
6. **"에러 표시 없는 검은 화면" 3계열과 방어선**(088차): ① WebGL 컨텍스트 유실(JS 에러 0 — GPU 부하) →
   `game.ts installCrashGuards` 오버레이 ② 씬 create 중 예외(그리다 만 채 멈춤) → 전역 에러 배너
   ③ 맵 JSON 캐시 미스 → `RegionFieldScene.create` 가드(`bootFailed`) + 메인 메뉴 복귀.
   새 씬의 create가 외부 데이터를 소비하면 **캐시 미스 가드부터** 넣는다.
7. **WebGL 캔버스 픽셀 직접 샘플(drawImage) 금지** — `preserveDrawingBuffer=false`라 항상 검정.
   밝기 검증은 Playwright 스크린샷 버퍼를 디코드한다(088차 하네스 오판 사례).
8. **`pointer.worldX/Y` 사용 금지** (100차) — 스크롤된 카메라에서 갱신되지 않아 스크린 좌표가
   그대로 들어온다(소형 맵은 스크롤 ≈ 0이라 잠복). 포인터→월드는 반드시
   `camera.getWorldPoint`(`RegionFieldScene.pointerWorld`) 경유.
9. **사용자 목업 시트는 격자가 아니다**(104차) — 라벨 스크린샷이라 셀 크기가 불균일하고
   압축 노이즈가 있다(72×72에 고유색 1,531개 실측). 반드시 **테두리선 실측 → 크롭 →
   목표 크기 area 리샘플 → 팔레트 양자화** 순서로 굽는다. 원본 크롭 직결은 금물.
10. **시트의 바다 색을 그대로 깔지 말 것**(104차) — 게임 물은 수심 버킷 절차 베이크(연한 파랑)라
   시트 청록(73,150,156)을 통짜로 깔면 접경마다 사각 색 패치가 생긴다. 접경 셀은 **바다를
   투명으로 파서 오버레이**로 쓴다.
11. **"열린 바다" 판정은 `waterDist`만으로 안 된다**(104차) — 청초호 같은 넓은 석호도
   "바깥 4·7타일 수심 ≥ 5"를 통과한다(인접 물 75타일 중 41타일 실측). 맵 경계 물에서
   `waterDist ≥ 8`만 타고 퍼진 **외해 마스크**(`computeOpenSea`)와 AND로 걸러야 한다.
12. **모래 웜 틴트 `#f6d47c`는 두 곳에 있다** — 클라이언트 `ensureGroundTextures`의 `'s'` 틴트와
   파이썬 `TTP_SAND_TINT`. 한쪽만 바꾸면 지면 모래와 접경 셀 모래의 색이 갈린다.
13. **사용자 시트의 "흰 배경"은 흰색이 아니라 알파 0일 수 있다**(105차) — 뷰어가 투명을 희게
   보여줄 뿐이다. 테두리 셀 탐지를 `convert('RGB')`로 하면 투명이 (0,0,0)이 되어 셀 전체가
   "검정 테두리"로 잡힌다. 마스크는 **불투명 AND 근검정**으로 만든다.
14. **실사 셀은 군 단위로 톤을 맞춰야 한다**(105차) — 촬영 광량 차이로 셀마다 휘도가 달라
   (deck 123~153 실측) 그대로 깔면 **바둑판 이음매**가 보인다. `_clean(match=)`으로 채널 평균을
   목표색에 맞춘 뒤 굽는다(정규화 후 149.4~149.7).
15. **심리스 캐스팅 판정은 스펙 §5(b/s 전용)가 아니라 기존 물가 규칙** — 속초 OSM에 방파제
   `man_made` 태그가 거의 없다(실측 `b` 25타일). 근거·조정은 스펙 §0.5 코드 정합 노트.
16. 심리스 청크 충돌은 **상주 즉시**, 시각 베이킹만 프레임당 1청크 — 순서를 바꾸면 스폰 직후
    바다/건물 관통이 생긴다. 청크 크기(64)를 바꾸면 `RegionFieldScene.SEAMLESS_CHUNK_TILES`
    (POI 청크 키)와 함께 바꿔야 한다.
17. **`roads.json`·`patch.json`은 항상 존재해야 한다** (101차) — build_region_maps가 빈 파일이라도
    쓴다. 없는 URL을 로드하면 SPA 폴백 pageerror(함정 3과 동일). 편집기 저장은 vite dev
    미들웨어(`/__dev/region-patch`) 전용 — 프로덕션엔 없다.
18. **편집 패치가 terrain.png보다 우선** — 재빌드 시 patch.json 타일이 PNG 분류 위에 덮인다.
    PNG를 손수정했다면 패치의 같은 타일을 비워야 반영된다.
19. **Kenney `_transparent` 시트는 마젠타 배경 + 17px 마진선**(101차 후속) — 격자로 그대로 자르면
    다중 셀 스프라이트 한가운데 빈 줄이 생긴다. `extract_tileset_assets.py`의 `kenney_sheet()`
    (키잉 + 마진 제거) 경유 좌표만 유효. 컨택트시트 라벨은 **셀 아래**에 붙는다(오독 주의).
20. **차선 마킹은 폴리라인 정점을 인셋하지 않는다** — 정점 인셋 = 연결 끊김(101차 후속 리포트).
    교차부 완화는 양 끝점 인셋만. 차로 기하는 `lanes`(roads.json) 기준이라 폭을 바꿔도 안에 머문다.
21. **`TR`은 모드별 값**(심리스 32 / legacy 20) — 하네스·툴에서 20을 하드코딩하지 말고 씬의
    `worldW / cols`로 읽는다. 지면 타일셋 재베이크는 `tr % 16 === 0`일 때만(아니면 절차 폴백).
22. **Kenney 지면 셀은 자동 색 통계로 고르지 않는다** — 환기구·균열·코너 변형 셀이 섞여 반복 무늬가
    생긴다. 셀 가장자리 1px 밝기 분석(테두리) + 내부 밝기(균열)로 **무테·무균열 셀만** `GROUND_CELLS`.
    아스팔트는 (11,19)만 완전 무지(나머지는 차선 조각 포함).
23. **건물 충돌은 `isBlockedAt`(하단 2줄) — 씬의 `blocked` 그리드는 여전히 `#` 전체**(스폰·걷기
    판정용). 지붕 텍스처 `roof_<c0>_<r0>`는 소유 청크 언로드 시 제거되므로 다른 곳에서 참조 금지.
24. **주행 차량은 맵 안에 완전히 든 도로만**(`TrafficSystem.drivable`) — OSM 경계 밖 폴리라인이면
    차량이 맵 밖으로 사라진다(실측 4대). 차량 스프라이트 ×1.3(차로 29px 기준).
25. **타일셋 크롭은 격자 라벨 확대 서베이로 확정**(`extract_tileset_assets.py` zoom류 → `_survey/*_zoom.png`)
    — 연결요소 자동 크롭은 인접 조각(지붕 연장·펜스)을 붙여 낸다. TopDown은 12px(TR 비정수비 → 오브젝트
    ×2 전용), Kenney는 16px(×2 = TR — 건물 키트·지면). 오픈소스 두 팩에 배 스프라이트는 없다.
26. **건물 키트 벽 모듈은 bbox 최하단 2줄에만** — 남쪽 이웃 기준으로 깔면 계단형 풋프린트에서 벽돌이 번진다.
    충돌(`isBlockedAt`)은 열 기준 하단 2줄이라 벽 그림과 정확히 일치하지 않는 계단 칸이 있을 수 있다.
27. **프롭 풋프린트는 `propFootprint` 단일 기준**(편집기 격자·겹침 판정·충돌 바디 공유) — 새 프롭은
    `PROP_DEFS`에 넣기만 하면 되고, 충돌을 빼려면 `passable: true`.
28. **교통 U턴 pose는 세그먼트 끝 t = len으로** — advance가 t를 뺀 뒤 pose를 재면 세그먼트 시작점으로 점프.
29. **도로 그림은 벡터 밴드, 타일 `r`/`w`는 판정 전용** — 편집기 도로 툴(도구 탭)로 정점을 옮기면 그림·마킹·
    차량은 따라오지만 걷기 판정은 타일이라 `r`/`w`도 같이 칠한다. `RegionPatch.roads`가 있으면 roads.json을
    대체한다(파이프라인 재실행은 roads.json만 갱신 — 패치가 우선). 밴드 순서 보도→연석→아스팔트를
    **전 도로 단위로 층별** 그려야 교차부가 합쳐진다.
30. **마킹은 교차 정점에서 조각내고 인셋** — 끝점만 인셋하면 관통 도로(way가 안 쪼개진 T자)의 마킹이 박스를
    관통한다. 막다른 끝은 인셋 없음.
31. **교통 막다른 끝은 페이드 재등장** — U턴 보간은 뒤로 미끄러져 "거꾸로 달리는 차"로 보인다.
32. **Kenney 지면 셀은 행별 밝기 프로파일이 4행 주기로 반복하는 것만** — 행 22 셀은 13행에 밝은 모르타르 줄.
33. **roads.json의 `roundabout`/`oneway`는 pts 순서 = 주행 방향** — OSM 재빌드(`build_osm_tilemap.py`)가 붙인다.
    `oneway=-1`은 파이프라인이 pts를 반전해 두므로 클라이언트는 항상 순방향(+1)만 쓴다.
34. **횡단보도·정지선은 조각 길이 ≥ 2.5 · 도로 길이 ≥ 5 · 회전교차로 반경+2.5 밖에서만** — 복합 교차부의
    슬립로마다 그리면 어수선하다(수복탑 사거리 실측). 겹침은 줄무늬 단위로 생략.
35. **RegionFieldScene은 재사용되는 씬** — 지연 생성 GameObject(`objHintText` 등)·심리스 객체
    (`chunks`/`traffic`/`poiWalls`)는 **`create()` 최상단에서 리셋**. shutdown 핸들러 안에서 정리하면
    한 번의 예외로 다음 지역이 오염된다(홈타운 drawImage null — 08-29). shutdown 시 물리 그룹은 우리
    핸들러보다 먼저 파괴되므로 `walls.remove`는 `walls.children` 확인 후.
36. **회전교차로 접근로는 OSM에서 대부분 oneway 쌍**(진입로·진출로 분리 이중도로 — 수복탑 실측
    접근로 10/10) — 분리섬·접근로 로직에서 "양방향 접근로" 전제는 전멸한다(102차). 물리 시설은
    쌍 **사이 공간**에 있다.
37. **Kenney 블롭 셀은 픽셀 분류로 방위를 확정하고 쓴다** — 잔디 이너코너 노치(흙 블롭 모서리 셀)는
    반타일 흙이라 계단 경계 안쪽마다 갈색 블롭이 찍히고(폐기), 지붕 행 2~3을 열 0~3로 묶으면 세로/
    와이드 패널 파편이 "05" 숫자처럼 합성된다(정합 2×2는 열 4~5·6~7뿐 — 102차 실렌더 확인).
38. **`kn/` 재추출은 Pillow 버전에 따라 기존 PNG가 바이트만 바뀐다**(픽셀 diff 0) — 커밋 전
    픽셀 동일 확인 후 기존 파일은 `git restore`로 원복하고 신규 파일만 남긴다(102차 47파일).
39. **OSM 방파제는 man_made 태그가 아니라 coastline 조각으로 온다**(속초 실측 — 태그 way 3개뿐) —
    파이프라인 후처리 3c(폭 ≤ 10타일 양끝-바다 런 + 인프라 컴포넌트)가 `b`로 재분류한다.
    타 지역 확장 시 빌드 로그 `[post] 방파제 추론 N타일` + terrain.png 육안 대조로 오분류 검수.
40. **coastline 장벽(3타일 두께)은 전부 '.'가 된다** — 해변은 후처리 3b(모래 성장)가 물가까지
    잇는다. 같은 이유로 모든 해안선에 '.' 밴드가 남는 것은 정상(호안/부두 톤).
41. **모래('s')와 tan 포장은 무틴트 시 육안 구분 불가**(Kenney 크림 ↔ 베이지) — sand 셀은
    반드시 웜 틴트 베이크(`_t` 키)를 거친다. "모래가 안 깔렸다"는 리포트는 terrain부터 실측할 것
    (103차 — 데이터엔 이미 's'가 있었다).
19. **도트 입자는 2px가 기준**(106차) — Kenney 지면(16px ×2)·절차 물 디더와 맞춘다. 실사/생성
    에셋을 32px 1:1로 구우면 그 타일만 입자가 절반이라 인접 대비가 4~20배로 튄다(실측: 잔디
    0.85 / 상판 3.2~8.6 / 사석 15~20). 새 시트는 `_clean(..., grain=2)`로 굽는다.
    ⚠ 반대로 **TopDown 세트는 다시 굽지 말 것** — 3~9색 네이티브 도트라 2px로 뭉개면 손해다.
20. **접경 셀 풀에 코너·빈 셀을 섞지 말 것**(106차) — `corner_*`는 모래 폭이 직선 셀의 1/4,
    `edge_still`은 좌측 열이 통째로 투명(L=-1)이라 직선 해안에 섞이면 벽돌담처럼 각지고 틈이
    생긴다. 직선 구간은 **경계 높이가 맞물리는 셀만**(포말 2종: 좌우 끝 21·21 / 23·21) + 미러.
21. **접경 셀과 절차 장식은 둘 중 하나만**(106차) — TTP 접경 셀이 이미 젖은 모래·포말을 갖고
    있어 절차 6px 젖은 모래 띠를 더하면 물가에 **연석 같은 밝은 세로줄**이 생긴다(실측색
    (202,183,126)). `ttpReady`로 절차 쪽을 끈다.
22. **유기 지형 경계에는 선을 긋지 않는다**(106차) — 해변↔포장에 Kenney 어두운 테두리 셀·
    삼각 빗변선·절차 접경선(검정 α0.28)이 겹치면 타일 계단이 "그려 넣은 톱니"로 읽힌다.
    `drawSandSpill`(노이즈 프론트 + 2px 알갱이)로 **경계를 녹이는** 쪽이 정답이다.
23. **편집기 상태는 `globalThis.__MAPEDIT`로만 조작**(106차) — 하네스가 import한
    `MapEditorPanel` 모듈은 게임 인스턴스와 갈라진다(`.ts`/`.js` URL 분화 — 실측
    `mapEditorState` 동일성 false).
24. **겹침 허용(free) 오브젝트는 충돌 바디가 없다**(106차) — 겹쳐 쌓은 만큼 바디가 생기면
    일대가 통째로 벽이 되기 때문. 통행을 막아야 하는 구조물은 겹침을 끄고 배치한다.
25. **마킹은 도로 벡터를 벗어날 수 있다** — 횡단보도·정지선·스크램블은 반드시 `onAsphalt`로
    클립한다(106차 후속). 정점 절단은 대등한 도로끼리만(`otherHalf ≥ max(1.25, halfW/2)`) —
    전 정점 절단은 골목마다 정지선을 찍어 "주기적 무늬"가 된다. 스크램블 X의 중심은 신호
    클러스터 센트로이드가 아니라 **두 도로의 실제 교차점**(센트로이드는 접속 정점에 끌려
    1.5타일 비껴난다 — X가 구석에 몰리는 실측 원인).
26. **OSM 대로는 방향별 oneway 쌍**으로 온다 — 낱개 oneway엔 중앙선이 없으므로
    `dualBoundaryOffset`이 쌍을 찾아 사이에 황색선을 긋는다. 쌍 판정에 좌/우 부호 필터를 넣지
    말 것(y-down 좌표에서 부호 혼동 — 대향 평행 + 거리 조건으로 충분, 실제로 첫 구현이 이 부호
    필터 때문에 그려지지 않았다).
