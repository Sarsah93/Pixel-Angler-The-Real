# 4. 잔여 · 위험/충돌 · 최적화

> 갱신 2026-09-10 (125차 기준). **작업 완료 시 이 문서도 함께 갱신한다**(스킬 `work-log`).
> 여기 있는 항목은 "언젠가"가 아니라 **착수 조건이 명시된 것**만 남긴다.

---

## A. 잔여 작업 (우선순위 순)

### A1. 즉시 착수 가능
| # | 항목 | 시스템 | 선행 | 규모 |
|---|---|---|---|---|
| J | **135차 점검 잔여 4건** — ① `deadline.onMiss: 'cost'` **미구현**(D-day가 지나도 벌칙 없음 — 표기만 `D+n (기한 초과)`로 수정. 벌칙 수치는 **사용자 결정 대기**) ② **시드 tackle 29개 > 기본 25칸** — slot 25~28(쿠션고무·봉돌 3종)이 그리드에 안 그려진다(채비 선택창·상점엔 나오므로 기능은 살아 있다 · 가방 착용 시 6행이 열려 보인다). 시드를 줄일지 용량을 올릴지 = **데이터 결정** ③ **가방 2단계 이상** 도입 시 `equipItem` 교체 경로에도 칸 축소 가드 필요 ④ U 패널 destroy 시 **인벤 완전 만석**이면 조각 소실 가능(61차 안전망 한계 — 미완성 접시는 1칸만 쓰므로 위험은 크게 감소) | [S12](02-SYSTEMS/inventory-equipment.md)·[S22](02-SYSTEMS/story-quests.md) | ①은 사용자 결정 | 소 |
| K | **136차 잔여 3건** — ① 스풀 시각화(방출 애니·슬랙 곡선) — 현재 수심 패널 텍스트 1행 ② **이빨 어종 랜딩 시 루어 파손** 미배선(현재 밑걸림·뭍 충돌만) ③ **내구도 0 이후 처리** = 현재 '고장이 잦아진다'로만 표현, 완전 폐기 단계 도입 여부는 사용자 결정 (⚠ 136-b에서 해소: 도움말 3토픽 ko/en · 영문 사전 · `TUNING.spool` 확정 · 줄 주기 슬랙 유예) | [S3](02-SYSTEMS/fishing-loop.md)·[S12](02-SYSTEMS/inventory-equipment.md) | ③은 사용자 | 소~중 |
| F | **해루질·통발 인-맵 잔여**(121차 1차 완료 후) — 야간 실검증(야간 생물·랜턴 반경) · F8 조율(밀도·성공·적발·벌금 mockup) · 생물 실사 스프라이트 12종(D4) · wade/dive 접근(가슴장화·스킨/스쿠버 — 사용자 계획) · 레거시 `NightHuntingScene`/`TrapScene`·legacy 쿨러 폐기 결정 · HUD 로그 영어 규칙 재수집 · 미니맵 스팟/어장 표시 | [S14](02-SYSTEMS/night-hunting-trap.md) | 야간 세션 / 에셋 / 사용자 결정 | 소~중 |
| G | **스킬 효과 실배선 49종(43/92) + 신규 면허 효과 6종 + 농사 카테고리 해제** — 제작은 129차, 생존 최대치·행동력 면제·시너지는 130차에 배선됐다. `wired:false` 스킬은 '예정' 태그로 학습만 가능하며, 대상 시스템(불요리·농장·차량·보트·주식) 착수 시 `effect.key`를 소비한다. 미배선 제작 효과 3종(`trap_durability`·`lure_tuning`·`egi_tuning`)은 통발 내구·루어 편차 시스템 대기 · **히든 시너지는 운전·농사에 아직 없다**(농장 착수 시 추가) · 스토리 본편은 **134차 S22로 착수**(퀘 보상 XP는 `grantXp` 경유) | [S21](02-SYSTEMS/progression.md) | 각 시스템 | 소(배선당) |
| H | **성장·생존 통합 — `.agents/PROGRESSION_SURVIVAL_SPEC.md` P1~P9** — ~~P1 레벨 200 곡선+스킬 트리~~ ✅124 · ~~P2 Vitals/StatusEffects~~ ✅125 · ~~P3 상태 패널~~ ✅126 · ~~P4 기절·사망 FSM~~ ✅126 · ~~P5 스킬 실배선 + P6 날씨 캐스팅~~ ✅127 · ~~P7 제작·구급품·보건소·피로 회복 음식~~ ✅129 · ~~스킬 트리 확장 (a)(c)(d)(e) + Σ 등식·티어 제약 재정의~~ ✅**130차**(Σ 200 → **215** = 만렙 200 + 면허 15 · 티어 수용 6 → 8 · wired 43/92) · ~~P9 도움말 ready 전환~~ ✅**131차**(제작 카테고리 신설 · 생존 6토픽 ready · 스킬 3페이지 · 제작 UI 영문화) · ~~P8 퀘스트 XP·Ch1~~ ✅**134차**(S22 — 120퀘 · 완료 XP `grantXp` · 일지 실갱신). **P1~P9 전부 완료.** ~~법 강제 판단~~ ✅**135차**(3단계 · 기본 = M1-06 완료 후) · ~~Ch1 manual 실시스템(품삯·사이소·가방)~~ ✅135차. 다음 = **통발 D2 → 불요리 → 농장** | [S21](02-SYSTEMS/progression.md) 외 관통 | 없음 | — |
| I | ✅ **해소(133차)** — 대물 파이트 텐션. 원인은 밸런스가 아니라 **드랙 모델**이었다: 상한이 "릴을 안 감을 때"에만 걸려 감는 순간 요구 장력이 그대로 실렸고, 유영 부하(`pullIdle 0.22 × burst`)와 `reelLoadKg 0.6`이 더해져 1.84kg 감성돔이 1.5호 목줄에 124%를 띄웠다(랜딩 0/300). 드랙을 **감는 중에도 미끄러지게**(`reelDragCapFrac 0.97`) + **충격 하중만 파단**(`shockBreakFrac 2.8`) + 유영 부하 0.10 → 안내대로 플레이 시 감성돔 87% / 참돔 83% / 부시리 43%, 라이트 채비 대물 18% · 부시리 2호 목줄 0%(설계 의도 유지). 잔여는 **사용자 실플레이 체감 확정**뿐 | [S3 낚시](02-SYSTEMS/fishing-loop.md) | 사용자 실플레이 | 소(슬라이더) |
| H2 | **레거시 UI 이모지·글리프 정리** — 규칙(126차 · **128차 정정**)에도 `InventoryStore` 시드·`ShopCatalog` 구 아이템의 `icon` 이모지(🐟🥫🦀 …)가 `iconTexture` 폴백으로 남아 있고, **HUD 날씨 배지 글리프**(`☀ ⛅ ☁ ☂ ❄ ≋`)도 그대로다. **129차에 신규 아이템 26종은 전부 픽셀 아이콘으로 들어갔고**(아트 15종 신설 · `iconTexture: 'px:<키>'` 경로 신설), 남은 대상은 **구 비어획 아이템 + 날씨 6종**이다. 어종은 `speciesId` 폴백이 실사 텍스처를 그리므로 제외. 절차: `tools/gen_pixel_icons.py`에 아트 추가 → 재생성 → `icon: ''` + `iconTexture: 'px:<키>'` | [S9 UI](02-SYSTEMS/ui-framework.md) | 아트 물량(어느 범위까지 그릴지) 결정 | 중 |
| 0 | 🚧 **무늬오징어 손질 수동 실플레이 검증 — 진행 중** — 90~96차로 전 구간 재구성(20스테이지 · 가로 4스윕 실측 · 크기 통일 · 연출 후 전환 · 3분할 조각 밀림·부리 가운데 조각). 잔여 = F9 실측(내장 2/2·뜯기 ②·날개 껍질째 2경로) + 품질 확인(플랩 발색·gill_on·합성 3종) | [S3](02-SYSTEMS/butchery.md) | 없음 — **최우선** | 소 |
| ~~0b~~ | ~~껍질 분리(4단계) 재구성~~ — **91차 완료** (온전 상태 = 영상 참고 추론 합성 `squid_skin_on`·마무리 스윕 배선·2회 뜯기). 실사 입수 시 같은 키 교체만 | S3 | — | — |
| 1 | **두족류 트리 확장** — ~~무늬오징어~~ ✅87~96차 · ~~한치(트리 공유)~~ ~~문어 8~~ ~~슬라이싱(회뜨기 3모드)~~ ✅**97차** → 잔여 = **갑오징어 13**(bone_lift·속껍질) + 수율·등급 연동 | [S3](02-SYSTEMS/butchery.md) | 갑오징어 속껍질 에셋 | 중 |
| 1f | **~~문어 에셋~~ ✅100차**(사용자 픽셀 투명 에셋 0~9 전량 교체 — `octo_scrub` 품질 해소 · `octo_invert1` 신규 · 부리 아이콘) → 잔여 = **OCTO 가이드 F9 실측**(외번·내장·악판·salt/scrub 스윕 — 100차 스프라이트 교체로 subjectRect 변동, 재실측 필수) + 오버라이드 키 `ceph_mantle/fin/arms/octoWhole/octoLeg` | S3 | **사용자 측정** | 소 |
| 1g | **OSM 심리스 지역 확장 — land 14개**(속초 v2 ✅100차 · **울릉·독도 = `access='boat'` 분리** ✅102차 — 배 출조 콘텐츠 시 개방) — `fetch → build → build_region_maps → SEAMLESS_REGIONS 등록 → terrain·스폰 검수` 반복(코드 변경 없음). ⚠ 서해 taean 2곳 seaEdges 조정 가능성 · 대형 지역 수집 수십 분 · 맵 후속 잔여는 ✅102차(주간 시각·정지선 방향 사용자 확인만 이월) | [S2/S7](02-SYSTEMS/world-field.md) | **사용자 검수 동반**(terrain ↔ OSM 대조) + 맵 확정 | 중 |
| ~~1b~~ | ~~두족류 시트 도트 추출~~ — **89차에 실사 도트로 해소**(사진 11장 → 14스테이지). SVG는 나머지 3종 참고자료로 잔존 | S3 | — | — |
| 1c | 두족류 가이드 F9 실측 — 90차 1~3단계 + **92차 들추기·뜯기 ①·아가미 반영 완료**. 잔여 = 뜯기 ② 스윕(회전 뷰)·날개 4경로·3분할·부리 | S3 | **사용자 측정**(진행 중) | 소 |
| 1d | 무늬오징어 **도마 사진 부재** — ~~S5 내장~~(90차) · ~~껍질 온전·아래로~~(91차 합성) · ~~분리 완료·아가미 온전~~(**92차 파생** — 실사 오면 교체) · 껍질 완전 분리본 · **아가미 붙은 안쪽면** · 3분할 · 부리. 목록·근거 → [워크로그 089 §6-a](03-WORKLOG/2026-08-10-089-squid-photo-stage-sprites.md) + [092 §6](03-WORKLOG/2026-08-13-092-squid-skin-rotation-fin-split.md) | S3 | **사용자 사진** — 오면 `SQUID_STAGE_SPRITE`/`CEPH_SRC` 1줄씩 | 소 |
| ~~1e~~ | ~~두족류 부산물 아이콘(껍질·아가미)~~ — **97차 부산물 개편으로 미지급 전환** (아이콘 불요. `ceph_ink_sac` 차용은 잔존) | S3 | — | — |
| 2 | 소모품 `kitchen_towel` 등록 — ~~`coarse_salt`~~ ✅97차(시드+마트) | S5 | 갑오징어 착수 시 검토 | 소 |
| 3 | 광어 `FLAT_GUIDE` F9 실측 — **84차 목록**: `upSep1/2`·`dnSep1/2`(보간 파생)·`dnScore`(미러)·`engawa` (구 upLift/gutSweep 폐지. 85차 클리핑은 렌더뿐 — 좌표 유효. **박피 2종은 86차 반영 완료**) | S3 | **사용자 측정** | 소 |
| 3c | 회썰기 유도선 실측 — 편집기(F9)·`SASHIMI_CUT_OVERRIDES` 준비됨(86차), 값 비어 있음 (엔가와·필렛·고급 키별) | S4 | **사용자 측정** | 소 |
| ~~3b~~ | ~~포 뜨기 칼 연출 튜닝~~ — **현행 확정**(사용자 2026-08-09 "괜찮을 것 같아") | S3 | — | — |
| 3d | 한치·갑오징어·문어 무게 곡선 실측 정합 — 088차는 무늬오징어만(캡처 제공분). 한치는 79차 사용자 확정값이라 보류 | S6 | **종별 실측 캡처** | 소 |
| ~~4~~ | ~~gh-pages 재배포~~ — **✅ 10차 배포 완료(2026-09-09, 116~120차 포함)**. 라이브 검증 404 0 · pageerror 0 · 한/영 양 로케일 스모크 | S-배포 | — | — |
| 6 | **방파제 안 모래 패치**(114차 잔여) — 동방파제·대포항 두부에 래스터 `'s'` 잔존. 자동 흡수는 뿌리 해변을 먹을 위험이 있어 **편집기(F7) `'b'` 덧칠**이 안전 | S2/S7 | 육안 확인 | 소 |
| ~~7~~ | ~~조도 주간 톤 확인~~ — **✅116차**(실제 위치 재적용 시 주간 실렌더). 잔여 = 초지 뭉침(나무 프롭 수동) · 가장자리 셀 임계 0.5→0.35 판단 | S2/S7 | 사용자 체감 | 소 |
| 8 | **`fightPhys` 실플레이 조율**(117차 mockup — staticFrac·burst·대응 배수·드랙 상한·dragInTimeScale) → F8 스냅샷으로 확정 + 어종군 `burst` 웹 리서치 재검(현 돔 4·방어 5.5·고등어 3·볼락 2.5·광어 2·농어 3.5·두족 1.5) | [S1](02-SYSTEMS/fishing-loop.md) | 사용자 체감 | 소 |
| 8b | **`castWeather` 실플레이 조율**(127차 mockup — 3m/s 임계·비거리 0.045/ms·산포 0.10/ms·횡풍 0.035/ms·강수 게인) → F8 스냅샷으로 확정. 파고(`waveHeightM`)의 캐스팅·조류 연동은 미착수 | [S1 낚시](02-SYSTEMS/fishing-loop.md) | 실플레이 체감 | 소 |
| ~~9~~ | ~~`help_fp_fight` 실캡처 교체~~ — **✅118차**(`__FP.devForceFight`로 자체 캡처 · 튜토리얼 파이트 1페이지 합성 삽화). 잔여 = 나머지 튜토리얼 SVG 삽화(회수·밑밥·회뜨기) 실캡처 교체 | S9 | — | 소 |
| 10 | **영어 잔여** — 119차 사전 656항목 + 120차 **전수 잔여 0 달성**(콘텐츠 산문 사전 = 어종 57·손질 53·레시피 22·통발 8·신선도 8 + 규칙 12 + 상호명 `nameEn`). ① 상호명 = **로마자 표기로 확정**(사용자 2026-09-08 — 타일 그림이 아니라 문 위 Text 라벨이고 `[E]` 거래가 되는 길찾기 요소라 읽혀야 한다). OSM `name:en` 혼용 + 미보유 42건 사전 → **해소**. ③ 동적 조립 문자열 → **해소**. ④ `nameEn` 필드 → **해소**(데이터가 정본, `places.ts` 는 폴백). **잔여 = ② 튜토리얼 삽화 18장 영문판**(`docs/mockups/game_guide_hub.html` 영어 재렌더 → `guide_*_en.png` → BootScene 폴백. 실캡처 `guide_fight_6_en` 만 완료) + 타 지역 POI 백필(`py tools/backfill_poi_nameen.py <region>` — OSM 확장 시 절차에 포함) | [S9](02-SYSTEMS/ui-framework.md) | ② 없음 | 중 |
| 5 | 사용자 검은 화면 **원인 확정** — 088차 입력 경로 4종 재현 실패(컨텍스트 유실 추정). 방어선(오버레이·배너)이 원인을 화면에 자가 보고하므로 **재발 시 문구 캡처**로 확정 | S7 | 재발 관찰 | 소 |

### A2. 사용자/외부 대기

| # | 항목 | 비고 |
|---|---|---|
| R1 | **래스터 보강** — ~~발주~~ → **Claude Code 직접 실행 ✅109차**(Phase 1·2 — 좌표 정합 98.84% · 피복 병합 · b diff · 회귀 0 · FPS 60). 잔여 = **사용자 육안 확정**(도심 'r' 체감) · **Phase 3 DEM 도엽 4장 재다운로드**(현 PC 부재) · b/s 발판 보너스 배선(승인 후) | [워크로그 109](03-WORKLOG/2026-09-02-109-raster-phase12-exec.md) |

| 항목 | 대기 중인 것 | 없으면? |
|---|---|---|
| ~~`halibut_lift_b` 도마 제거~~ | **오판이었음** — 갈색은 나무판이 아니라 젖혀진 필렛 아래 **등면 껍질** | — |
| ~~`halibut_fin_score` 수평 보정~~ | 사용자 제공 `ref5_transparent`(세로·칼 예시 포함)로 해결 | — |
| ~~`halibut_lift_a` 재촬영~~ | **불필요** — `fin_score`에서 파생 합성(82차) | — |
| `ceph_skin`·`ceph_gill`·`ceph_inner_skin` | trimmings 에셋 3종 | 아이콘 없이 텍스트 표기로 진행 가능 |
| 두족류 가이드 시트 부리 카드 | SVG 카드 추가 | 스테이지는 동작, 삽화만 공백 |
| 방어류 단계 실사 4장 | — | **의도적 보류**(70차 판단 — 기존 스프라이트가 방어 실사 기반) |
| **출조 지역 병원 POI** | **네트워크(Overpass) 접근** — `py tools/backfill_hospital_poi.py sokcho_v2` 1회 | 홈타운 보건소로 진료는 항상 가능(절차 배치라 OSM 무관) |
| **면허 7종 발급 경로** | 해당 시스템(선박·토지·농지·상업 통발 등) 구현 | 130차 등식(예산 215)의 마지막 7pt가 잠겨 있다 — 지금 도달 가능 예산은 208 |
| **도움말 신규 캡처 4장의 한국어 콜아웃** | 한글 폰트가 있는 환경 | 131차 작업 환경에 한글 폰트가 없고 egress도 막혀 축소본만 저장했다(영문판은 정상). `py tools/annotate_help_images.py <raw> --only craft_tab workbench skill_tree vitals_panel` 재실행이면 끝 |

### A3. 다음 대과제 (순서 = 사용자 지정 · 99차 조사로 세분화)

**지형 고도(Phase 3 DEM) — 낚시 확립 후** (2026-09-03 방향 확정):
- **B 해안 지형 변형 최우선** — 급경사 해안 → 갯바위 / 완경사 → 모래 / 수직 → 인공 호안.
  낚시 발판·해루질 스팟과 직결. A(음영)·C(레벨·절벽)는 배제하지 않고 후순위.
- **경사로 통행을 막지 않는다** — 채집·광질·이벤트 후보에서 지형이 빠지지 않게. 차단은 수동 패치만.
- 선행: `pip install rasterio`(이 PC 미설치 — DEM 값 판독 불가) · 절벽 타일 에셋 부재(C의 실질 블로커).
- 근거·방침: `.agents/RASTER_UPLIFT_AMENDMENT.md` §4
1. ~~**해루질·통발 완성**~~ → **✅ 121차 인-맵 1차 완료**(채집 스팟·어촌계 어장 실폴리곤·강원 조례·통발 아이템 설치/수거 —
   D2~D5 배선을 별도 씬 없이 속초 맵 위에서). 잔여 = A1-F → [S14 페이지](02-SYSTEMS/night-hunting-trap.md) §5.
2. **불요리**(화구·용기) → CookScene 실조리 → 스시.
   ⚠ 최고 레버리지 — 낚시/해루질/통발/농사 전 산출물의 "쓰임(sink)". 레시피 13종·부산물 훅은 데이터 완비.
3. **농장 경영(스타듀밸리형 — 사용자 확정 2026-08-14)** — 광질(채굴)·채집·농사·수조 + 경영 루프.
   실상은 "설치 시스템만 동작"(99차 조사). 로드맵 E1~E5 → [S8 페이지](02-SYSTEMS/home-base.md) §5.
   선행 결정: 시간 모델 = **실시간 오프라인 성장**(쿨러 패턴 재사용) 권장.
4. `CraftScene`(벌목/채굴 재료 소비) · 멀티(Phase 8) · Tauri/Steam(Phase 9)
5. **퀘스트/스토리** — ~~모든 컴포넌트 구현 후~~ → **134차 착수**(스펙 v3 · 120퀘 · 법 규칙 · 일지·대화).
   잔여 = 법 강제 ON 판단 · 가방 UI·지급품 · Ch1 manual 목표의 실시스템화(품삯·구멍치기·사이소·공동작업) ·
   배 출조·좌판·구술 채록 계통 · Ch2+ 대사 → [S22](02-SYSTEMS/story-quests.md) §4·§5.

### A4. 시스템별 소소한 잔여
| 시스템 | 항목 |
|---|---|
| S1 낚시 | 어탐 레이더 · 사운드 이펙트 · 가이드 삽화 실사화 · 신규 해양 API 5종 |
| S2/S7 필드 | ~~새 도로 그리기·분리섬·신호등·사고 추월·물 타일셋~~ ✅102차 · ~~수심 디더·방파제·해변·테트라포드·섬 렌더~~ ✅103차 · ~~TTP 시트 그리드화~~ ✅104차 · ~~해안 시트 3장 에셋화~~ ✅105차 · ~~도트 입자 2px 통일(해안·TTP·Gemini)·해변 경계 재작업·편집기 개별 타일 126/오브젝트 27·회전/반전/겹침 자유 배치~~ ✅106차 · ~~교차부 마킹 관통(회랑 클리핑)~~ ✅110차 · ~~주차장 차량 열~~ ✅111차 · ~~열 주축·광장 톤·항만 디테일~~ ✅112차 · ~~차량 배치 재설계(주차장 구역·도로 위 금지·밀도)~~ ✅113차 · ~~조도 갯바위 재구현~~ ✅115차(사진 시트 — 잔여: 초지 톤·주간 톤 육안) · ~~방파제 단면·등대~~ ✅114차(잔여: 방파제 안 모래 패치·두부 원형 확장·등대 야간 발광) ✅111차(잔여: 열 방향 주축·도로변 평행 주차·'r' 콘크리트 톤) → 잔여: **방파제 외해측 테트라포드 수동 재구성**(106차에 자동 피복 폐기 — 편집기 겹침 허용으로 쌓기) · 부두 **코너 셀** 자동 방위 매핑 · 방파제 두부(head) 끝단 배치 · `submerged_*` 원본 셀 재선정 · **조도 위성 픽셀 시트 납품 대기** · "도로 위 건물" 위치 제보 대기 · 도심 골목 도로 폭 · 차량 신호 준수 · OSM traffic_signals 노드 보존 · 자동 산포↔수동 프롭 겹침 검사 · 어선 실사 에셋 · NPC 상호작용 · 차량 교차부 정렬 · 편집기 미니맵 즉시 갱신 · 스폰 다중화(`meta.spawns[]`) · 비거래 POI 전용 씬 · 지역 실상점 카탈로그 · legacy 7맵 제거 시점(사용자 결정) |
| S4 회썰기 | 스시 · 접시 배치 세이브 · 두족류 `whole` 슬라이싱 |
| S5 인벤 | 예약 슬롯 6종 · 냉장고 드래그 이송 |
| S6 경제 | 낚시점 전용 상점(루어) · 경영 |
| S8 홈 | 하우스 Tier 1~3 · 수조 패널 · 농사/벌목/채굴/보트 · 실내 배치 모드 |
| S10 UI | 저순위 팝업 검수 · `LicensePanel` 목록 스크롤 · fight/rod/yield TUNING 소비 전환 |

---

## B. 위험 · 충돌 요소

### B1. 계약(타입) 위험
| 위험 | 내용 | 방어 |
|---|---|---|
| `ButcheryStage.orientation` 위드닝 | 두족류 뷰가 어류 경로로 새어들 수 있음(80차) | 경계 어댑터 `asFishOri` 유지 |
| 확정 타입 변경 | `TideInfo`·`SpotType` 등 | AGENTS §8 — 변경 금지 |
| core에 렌더 코드 유입 | `@tra/core`는 순수 TS | AGENTS §8-1 |

### B2. 회귀 위험 (과거 실제로 깨진 곳)
| 지점 | 무엇이 깨졌었나 | 재발 방지 |
|---|---|---|
| 부산물 지급 경로 | 종료 경로 3개 중 하나만 빠져도 아이템 증발 (59·61·62·63차 4연속) | 새 종료 경로 추가 시 정산/회수 필수 |
| 발견 기록 세이브/동기 | 첫 부팅(세이브 없음)은 applySaveData·newGame을 안 거쳐 시드 동기 누락(99차 실측 FAIL 2건) | 리셋/로드/부팅 3경로 전부 DiscoveryStore 배선 · 일괄 동기는 onNew 억제 |
| 통발 타깃 데이터 | 타입 유니온에 있어도 실생물 0이면 조용히 죽는 타깃(`'shellfish'` — 99차) | 카테고리·어종 id **실존 확인** (오타는 필터에서 조용히 사라진다) |
| 씬 전환 페이드 | `camerafadeoutcomplete` 미발화 → 검은 화면 (46·52·71·73차) | `SceneFade.fadeOutThen` 경유 |
| 재진입 가드 | 씬 재사용이라 필드 초기값이 재진입에 미적용(73차 먹통·이중 차감) | `create()`에서 리셋 |
| 구운 스프라이트 | PNG만 바꾸고 생성기 미실행 → 반영 안 됨(74·76차) | `asset-pipeline` 스킬 체크리스트 |
| 세이브 스키마 | 구세이브에 신규 정적 필드 없음(53차) | `deserialize` 시드 백필 |
| 매칭 테이블 순서 | 부분일치 선착순 오매칭(`한치`·`잿방어`·`말쥐치`…) | 긴 이름 먼저 |
| 마스크 팬텀 히트 | 스크롤아웃 셀이 클릭됨 | 인터랙티브 목록 = 윈도우드 렌더 |
| ButcheryPanel rect 혼용 | 회전 도입 후 fish rect 앵커 UI가 밀리고, `toPanelPx` 미경유 렌더는 자국이 가로로 눕음(83차) | 생선 위 = `toPanelPx` / 오버레이·목록 = board rect |
| 포인터 월드 좌표 | `pointer.worldX/Y`가 스크롤 카메라에서 미갱신 — 스크린 좌표 그대로(100차 심리스에서 발현·조준 반전) | `camera.getWorldPoint` 경유(`pointerWorld`) — 신규 씬도 worldX 직접 사용 금지 |
| 파이트 진행도 ↔ 거리 | 진행도를 거리와 별개 시계로 굴려 "발앞인데 진행도 80%"가 됐다(132차 사용자 리포트) | 랜딩 진행도는 **`landProgress()` 단일 소스** — 새 표시를 붙일 때도 여기서 파생 |
| 소형어 슬랙 규칙 | 게이지 임계(6%)까지 못 올리는 체급은 정대응(슬랙)이 곧 바늘빠짐(132차 시뮬 67%) | 절대 게이지 임계를 쓰는 규칙에는 **체급 게이트**를 건다 |
| 어종 무게(LWR) | 지수 3 고정이라 길쭉한 어종이 터무니없이 무거웠다(구 모델 갈치 전장 1m = 10kg, 실제 ≈ 0.7kg) — 무게는 판매가·수율·피로·요구 장력의 공통 입력 | 새 어종은 `lwrA`·`lwrB`·`bodyForm`을 **실측 무게 두 점**으로 잡는다(133차) |
| 드랙 상한 | "릴을 안 감을 때만" 상한을 걸면 표준 채비 + 표준 어종이 전멸한다(감성돔 50cm·1.5호 0/300) | 드랙은 **감는 중에도 미끄러진다**. 파단은 충격 하중·과부하가 담당(133차) |
| 두족류 레퍼런스 경로 | 소스 폴더 이동 시 파이프라인이 경고만 내고 키를 건너뛰어 **재생성 시 스프라이트 소실**(100차 미연 방지) | 재생성 로그의 "입력 없음" 경고 확인 · `CEPH_SRC_DIR` 정합 |

### B3. 데이터·환경 위험
- **프로덕션엔 vite 프록시가 없다** — HTTP 전용 API(MAFRA·NMPNT)는 배포 시 Mock 폴백. 서버 프록시 필요.
- **API 키가 번들에 인라인**된다(사용자 승인 상태). 공개 배포 시 재검토 대상.
- **NMPNT 데이터 공백**: 파고 0개소 · 수온 11/76개소 · 강수/운량 필드 없음 → 날씨 판정은 기상청 API 의존.
- **MAFRA는 2023년으로 날짜 매핑** — "실시간 시세"가 아니다. 밸런스 판단 시 유의.

### B4. 이중 경로(구조적 부채)
| 부채 | 내용 | 판단 |
|---|---|---|
| 레거시 `FieldScene` 계열 | 탑다운 구버전 + `FishingScene` 낚시 루프가 병존 | 유지만 — 신규 기능은 `RegionFieldScene`/FP에만 |
| FP 씬 ESC | popupStack이 아닌 **하드코딩 순서**(강제 방생 `lockedOpen` 결합) | 정리 시 방생 흐름 회귀 주의 |
| 두 개의 손질 진입 | `UtilizationPanel` 도마 / `CookScene` 안내 | CookScene은 안내만 — 실조리 도입 시 통합 |

---

## C. 최적화 요소

| 대상 | 현황(실측) | 개선안 | 우선도 |
|---|---|---|---|
| 번들 크기 | `index-*.js` **2.92 MB**(gzip 679 kB) — 500 kB 경고 | 동적 `import()` 코드 스플릿(씬 단위) 또는 `manualChunks`(Phaser 분리) | 중 (배포 체감) |
| 소스맵 | dist에 14 MB `.map` | gh-pages 배포에서 이미 제외 중 — 유지 | — |
| 구운 데이터 파일 | `PixelFish*.ts` 스냅샷이 번들에 포함 | 필요 시 JSON+지연 로드 | 하 |
| 타일 텍스처 | 맵당 `generateTexture` 캐시(`rmaptex_`) | 현행 유지(알고리즘 변경 시 리로드 필요만 인지) | — |
| 문서 비대 | `AGENTS.md` **375 KB** · `IMPLEMENTATION_PLAN.md` 106 KB — 세션 컨텍스트를 크게 먹는다 | **이 위키 도입으로 신규 기록은 분산**. 기존 히스토리는 §9에 보존(불변), 위키가 인덱스 역할 | 진행 중 |
| 검증 하네스 | 매번 스크립트 작성 | 반복 시나리오를 `scratchpad` 재사용 스크립트로 | 하 |

---

## D. 미해결 설계 질문 (결정 필요)

| 질문 | 현재 기본값 | 결정권 |
|---|---|---|
| ~~두족류 아가미·껍질 쓰임새~~ | **97차 부산물 개편으로 미지급** — 내장·먹물주머니만 잔존(밑밥 연결은 계속 보류) | — |
| 문어 통마리 후속(삶기) | **98차 간이 경로**(인벤 우클릭 [삶기] — `boilOctopus`). 불요리(화구·용기) 실구현 시 CookScene으로 이관·우클릭 제거 | 불요리 착수 시 |
| ~~대문어(`giant_octopus`) 트리~~ | **97차 확정 — 참문어 트리 공유**(`buildOctopusStages` 라우팅) | — |
| 접시 배치 상태 저장 | 세션 메모리(닫으면 반환) | 세이브 스키마 확장 여부 |
| ~~낚싯대 어획물 판매 금지 강제 시점~~ | **135차 확정 — `enforceRodSell = 2`**(M1-06 완료 후) | — |
| **스풀 구속 체감** | 조류가 센 곳에서 베일을 닫고 방치하면 채비가 눈에 띄게 끌려온다(닫힘 60초 20 → 9.56m). 장거리 캐스팅은 착수 시 여유줄 덕에 거의 영향이 없고 단거리·심수에서만 세게 걸린다 — **의도이나 기존과 다른 체감** | 사용자 실플레이 |
| **줄 주기 슬랙 바늘빠짐** | 파이팅 R 개방 10초에 텐션 54 → 5.2 → `slackHookOffBelow` 아래에서 `slackTimer` 누적. 설계된 대가지만 튜토리얼 없이는 억울할 수 있다 | 도움말 토픽 작성 시 |
| **초릿대 고장률 1.5%/캐스트** | 사용자 제시값 그대로. 사이소 로드 100캐스팅 = 기대 2.43회(수리비 3,000원 · 자가 본드칠 무료 65%) | 사용자 실플레이 |
| 가방 사다리(2×5)·지급품·시작 재화 ₩312,000 적용 | core 모델만(`Backpack.ts`) — 인벤 5×5·현행 시드 유지 | 1-4·1-8 플레이 가능 시 한 묶음 |
| `octopus` 학명(*O. vulgaris* vs *O. sinensis*) | 현행 유지 | 도감 정정 건으로 분리 |
