# 4. 잔여 · 위험/충돌 · 최적화

> 갱신 2026-09-01 (102차 기준). **작업 완료 시 이 문서도 함께 갱신한다**(스킬 `work-log`).
> 여기 있는 항목은 "언젠가"가 아니라 **착수 조건이 명시된 것**만 남긴다.

---

## A. 잔여 작업 (우선순위 순)

### A1. 즉시 착수 가능
| # | 항목 | 시스템 | 선행 | 규모 |
|---|---|---|---|---|
| 0 | 🚧 **무늬오징어 손질 수동 실플레이 검증 — 진행 중** — 90~96차로 전 구간 재구성(20스테이지 · 가로 4스윕 실측 · 크기 통일 · 연출 후 전환 · 3분할 조각 밀림·부리 가운데 조각). 잔여 = F9 실측(내장 2/2·뜯기 ②·날개 껍질째 2경로) + 품질 확인(플랩 발색·gill_on·합성 3종) | [S3](02-SYSTEMS/butchery.md) | 없음 — **최우선** | 소 |
| ~~0b~~ | ~~껍질 분리(4단계) 재구성~~ — **91차 완료** (온전 상태 = 영상 참고 추론 합성 `squid_skin_on`·마무리 스윕 배선·2회 뜯기). 실사 입수 시 같은 키 교체만 | S3 | — | — |
| 1 | **두족류 트리 확장** — ~~무늬오징어~~ ✅87~96차 · ~~한치(트리 공유)~~ ~~문어 8~~ ~~슬라이싱(회뜨기 3모드)~~ ✅**97차** → 잔여 = **갑오징어 13**(bone_lift·속껍질) + 수율·등급 연동 | [S3](02-SYSTEMS/butchery.md) | 갑오징어 속껍질 에셋 | 중 |
| 1f | **~~문어 에셋~~ ✅100차**(사용자 픽셀 투명 에셋 0~9 전량 교체 — `octo_scrub` 품질 해소 · `octo_invert1` 신규 · 부리 아이콘) → 잔여 = **OCTO 가이드 F9 실측**(외번·내장·악판·salt/scrub 스윕 — 100차 스프라이트 교체로 subjectRect 변동, 재실측 필수) + 오버라이드 키 `ceph_mantle/fin/arms/octoWhole/octoLeg` | S3 | **사용자 측정** | 소 |
| 1g | **OSM 심리스 지역 확장 — land 14개**(속초 v2 ✅100차 · **울릉·독도 = `access='boat'` 분리** ✅102차 — 배 출조 콘텐츠 시 개방) — `fetch → build → build_region_maps → SEAMLESS_REGIONS 등록 → terrain·스폰 검수` 반복(코드 변경 없음). ⚠ 서해 taean 2곳 seaEdges 조정 가능성 · 대형 지역 수집 수십 분 · 맵 후속 잔여는 ✅102차(주간 시각·정지선 방향 사용자 확인만 이월) | [S2/S7](02-SYSTEMS/world-field.md) | **사용자 검수 동반**(terrain ↔ OSM 대조) + 맵 확정 | 중 |
| ~~1b~~ | ~~두족류 시트 도트 추출~~ — **89차에 실사 도트로 해소**(사진 11장 → 14스테이지). SVG는 나머지 3종 참고자료로 잔존 | S3 | — | — |
| 1c | 두족류 가이드 F9 실측 — 90차 1~3단계 + **92차 들추기·뜯기 ①·아가미 반영 완료**. 잔여 = 뜯기 ② 스윕(회전 뷰)·날개 4경로·3분할·부리 | S3 | **사용자 측정**(진행 중) | 소 |
| 1d | 무늬오징어 **도마 사진 부재** — ~~S5 내장~~(90차) · ~~껍질 온전·아래로~~(91차 합성) · ~~분리 완료·아가미 온전~~(**92차 파생** — 실사 오면 교체) · 껍질 완전 분리본 · **아가미 붙은 안쪽면** · 3분할 · 부리. 목록·근거 → [워크로그 089 §6-a](03-WORKLOG/2026-08-10-089-squid-photo-stage-sprites.md) + [092 §6](03-WORKLOG/2026-08-13-092-squid-skin-rotation-fin-split.md) | S3 | **사용자 사진** — 오면 `SQUID_STAGE_SPRITE`/`CEPH_SRC` 1줄씩 | 소 |
| ~~1e~~ | ~~두족류 부산물 아이콘(껍질·아가미)~~ — **97차 부산물 개편으로 미지급 전환** (아이콘 불요. `ceph_ink_sac` 차용은 잔존) | S3 | — | — |
| 2 | 소모품 `kitchen_towel` 등록 — ~~`coarse_salt`~~ ✅97차(시드+마트) | S5 | 갑오징어 착수 시 검토 | 소 |
| 3 | 광어 `FLAT_GUIDE` F9 실측 — **84차 목록**: `upSep1/2`·`dnSep1/2`(보간 파생)·`dnScore`(미러)·`engawa` (구 upLift/gutSweep 폐지. 85차 클리핑은 렌더뿐 — 좌표 유효. **박피 2종은 86차 반영 완료**) | S3 | **사용자 측정** | 소 |
| 3c | 회썰기 유도선 실측 — 편집기(F9)·`SASHIMI_CUT_OVERRIDES` 준비됨(86차), 값 비어 있음 (엔가와·필렛·고급 키별) | S4 | **사용자 측정** | 소 |
| ~~3b~~ | ~~포 뜨기 칼 연출 튜닝~~ — **현행 확정**(사용자 2026-08-09 "괜찮을 것 같아") | S3 | — | — |
| 3d | 한치·갑오징어·문어 무게 곡선 실측 정합 — 088차는 무늬오징어만(캡처 제공분). 한치는 79차 사용자 확정값이라 보류 | S6 | **종별 실측 캡처** | 소 |
| 4 | gh-pages 재배포(74차 이후 전부 미배포) | S-배포 | 없음 | 소 |
| 5 | 사용자 검은 화면 **원인 확정** — 088차 입력 경로 4종 재현 실패(컨텍스트 유실 추정). 방어선(오버레이·배너)이 원인을 화면에 자가 보고하므로 **재발 시 문구 캡처**로 확정 | S7 | 재발 관찰 | 소 |

### A2. 사용자/외부 대기
| 항목 | 대기 중인 것 | 없으면? |
|---|---|---|
| ~~`halibut_lift_b` 도마 제거~~ | **오판이었음** — 갈색은 나무판이 아니라 젖혀진 필렛 아래 **등면 껍질** | — |
| ~~`halibut_fin_score` 수평 보정~~ | 사용자 제공 `ref5_transparent`(세로·칼 예시 포함)로 해결 | — |
| ~~`halibut_lift_a` 재촬영~~ | **불필요** — `fin_score`에서 파생 합성(82차) | — |
| `ceph_skin`·`ceph_gill`·`ceph_inner_skin` | trimmings 에셋 3종 | 아이콘 없이 텍스트 표기로 진행 가능 |
| 두족류 가이드 시트 부리 카드 | SVG 카드 추가 | 스테이지는 동작, 삽화만 공백 |
| 방어류 단계 실사 4장 | — | **의도적 보류**(70차 판단 — 기존 스프라이트가 방어 실사 기반) |

### A3. 다음 대과제 (순서 = 사용자 지정 · 99차 조사로 세분화)
1. **해루질·통발 완성** — 실상은 "엔진·씬 동작 / 배선 미완"(99차 조사 — ⬜ 표기는 오류였다).
   결함 2건(D1)은 99차 수정 완료. 잔여 = 배선 D2~D5 → [S14 페이지](02-SYSTEMS/night-hunting-trap.md) §4·§5.
2. **불요리**(화구·용기) → CookScene 실조리 → 스시.
   ⚠ 최고 레버리지 — 낚시/해루질/통발/농사 전 산출물의 "쓰임(sink)". 레시피 13종·부산물 훅은 데이터 완비.
3. **농장 경영(스타듀밸리형 — 사용자 확정 2026-08-14)** — 광질(채굴)·채집·농사·수조 + 경영 루프.
   실상은 "설치 시스템만 동작"(99차 조사). 로드맵 E1~E5 → [S8 페이지](02-SYSTEMS/home-base.md) §5.
   선행 결정: 시간 모델 = **실시간 오프라인 성장**(쿨러 패턴 재사용) 권장.
4. `CraftScene`(벌목/채굴 재료 소비) · 멀티(Phase 8) · Tauri/Steam(Phase 9)
5. **퀘스트/스토리 — 모든 컴포넌트 구현 후**(사용자 방침)

### A4. 시스템별 소소한 잔여
| 시스템 | 항목 |
|---|---|
| S1 낚시 | 어탐 레이더 · 사운드 이펙트 · 가이드 삽화 실사화 · 신규 해양 API 5종 |
| S2/S7 필드 | ~~새 도로 그리기·분리섬·신호등·사고 추월·물 타일셋~~ ✅102차 · ~~수심 디더·방파제·해변·테트라포드·섬 렌더~~ ✅103차 · ~~TTP 시트 그리드화·테트라포드 피복·해변 접경·외해 마스크~~ ✅104차 · ~~해안 시트 3장(돌 방파제·바위·부두 모서리) 에셋화 + `b` 실사 타일·갯바위 산포~~ ✅105차 → 잔여: 물 상세 7종(코스틱·그라데이션) 편입 여부 · `submerged_*` 원본 셀 재선정 · 방파제 두부(head) 끝단 배치 · 부두 코너 셀 활용 · **조도 위성 픽셀 시트 납품 대기**(사용자 작업 중) · **"도로 위 건물" 위치 제보 대기** · 도심 골목 도로 폭(체감 vs 실측 — 사용자 결정) · 차량 신호 준수(현재 시각 전용) · OSM traffic_signals 노드 보존(재fetch) · 자동 산포 프롭↔수동 프롭 겹침 검사 · 어선 실사 에셋 발주 · NPC 상호작용 · 차량 교차부 정렬 · 편집기 미니맵 즉시 갱신 · 스폰 다중화(`meta.spawns[]`) · 비거래 POI 전용 씬 · 지역 실상점 카탈로그 · legacy 7맵 제거 시점(사용자 결정) |
| S4 회썰기 | 스시 · 접시 배치 세이브 · 두족류 `whole` 슬라이싱 |
| S5 인벤 | 예약 슬롯 6종 · 냉장고 드래그 이송 |
| S6 경제 | 낚시점 전용 상점(루어) · 경영 |
| S8 홈 | 하우스 Tier 1~3 · 수조 패널 · 농사/벌목/채굴/보트 · 실내 배치 모드 |
| S10 UI | 저순위 팝업 검수 · `LicensePanel` 목록 스크롤 · fight/rod/yield TUNING 소비 전환 |

---

## B. 위험 · 충돌 요소

### B1. 계약(타입) 위험
| 위험 | 내용 | 방어 |
|---|---|---|
| `ButcheryStage.orientation` 위드닝 | 두족류 뷰가 어류 경로로 새어들 수 있음(80차) | 경계 어댑터 `asFishOri` 유지 |
| 확정 타입 변경 | `TideInfo`·`SpotType` 등 | AGENTS §8 — 변경 금지 |
| core에 렌더 코드 유입 | `@tra/core`는 순수 TS | AGENTS §8-1 |

### B2. 회귀 위험 (과거 실제로 깨진 곳)
| 지점 | 무엇이 깨졌었나 | 재발 방지 |
|---|---|---|
| 부산물 지급 경로 | 종료 경로 3개 중 하나만 빠져도 아이템 증발 (59·61·62·63차 4연속) | 새 종료 경로 추가 시 정산/회수 필수 |
| 발견 기록 세이브/동기 | 첫 부팅(세이브 없음)은 applySaveData·newGame을 안 거쳐 시드 동기 누락(99차 실측 FAIL 2건) | 리셋/로드/부팅 3경로 전부 DiscoveryStore 배선 · 일괄 동기는 onNew 억제 |
| 통발 타깃 데이터 | 타입 유니온에 있어도 실생물 0이면 조용히 죽는 타깃(`'shellfish'` — 99차) | 카테고리·어종 id **실존 확인** (오타는 필터에서 조용히 사라진다) |
| 씬 전환 페이드 | `camerafadeoutcomplete` 미발화 → 검은 화면 (46·52·71·73차) | `SceneFade.fadeOutThen` 경유 |
| 재진입 가드 | 씬 재사용이라 필드 초기값이 재진입에 미적용(73차 먹통·이중 차감) | `create()`에서 리셋 |
| 구운 스프라이트 | PNG만 바꾸고 생성기 미실행 → 반영 안 됨(74·76차) | `asset-pipeline` 스킬 체크리스트 |
| 세이브 스키마 | 구세이브에 신규 정적 필드 없음(53차) | `deserialize` 시드 백필 |
| 매칭 테이블 순서 | 부분일치 선착순 오매칭(`한치`·`잿방어`·`말쥐치`…) | 긴 이름 먼저 |
| 마스크 팬텀 히트 | 스크롤아웃 셀이 클릭됨 | 인터랙티브 목록 = 윈도우드 렌더 |
| ButcheryPanel rect 혼용 | 회전 도입 후 fish rect 앵커 UI가 밀리고, `toPanelPx` 미경유 렌더는 자국이 가로로 눕음(83차) | 생선 위 = `toPanelPx` / 오버레이·목록 = board rect |
| 포인터 월드 좌표 | `pointer.worldX/Y`가 스크롤 카메라에서 미갱신 — 스크린 좌표 그대로(100차 심리스에서 발현·조준 반전) | `camera.getWorldPoint` 경유(`pointerWorld`) — 신규 씬도 worldX 직접 사용 금지 |
| 두족류 레퍼런스 경로 | 소스 폴더 이동 시 파이프라인이 경고만 내고 키를 건너뛰어 **재생성 시 스프라이트 소실**(100차 미연 방지) | 재생성 로그의 "입력 없음" 경고 확인 · `CEPH_SRC_DIR` 정합 |

### B3. 데이터·환경 위험
- **프로덕션엔 vite 프록시가 없다** — HTTP 전용 API(MAFRA·NMPNT)는 배포 시 Mock 폴백. 서버 프록시 필요.
- **API 키가 번들에 인라인**된다(사용자 승인 상태). 공개 배포 시 재검토 대상.
- **NMPNT 데이터 공백**: 파고 0개소 · 수온 11/76개소 · 강수/운량 필드 없음 → 날씨 판정은 기상청 API 의존.
- **MAFRA는 2023년으로 날짜 매핑** — "실시간 시세"가 아니다. 밸런스 판단 시 유의.

### B4. 이중 경로(구조적 부채)
| 부채 | 내용 | 판단 |
|---|---|---|
| 레거시 `FieldScene` 계열 | 탑다운 구버전 + `FishingScene` 낚시 루프가 병존 | 유지만 — 신규 기능은 `RegionFieldScene`/FP에만 |
| FP 씬 ESC | popupStack이 아닌 **하드코딩 순서**(강제 방생 `lockedOpen` 결합) | 정리 시 방생 흐름 회귀 주의 |
| 두 개의 손질 진입 | `UtilizationPanel` 도마 / `CookScene` 안내 | CookScene은 안내만 — 실조리 도입 시 통합 |

---

## C. 최적화 요소

| 대상 | 현황(실측) | 개선안 | 우선도 |
|---|---|---|---|
| 번들 크기 | `index-*.js` **2.92 MB**(gzip 679 kB) — 500 kB 경고 | 동적 `import()` 코드 스플릿(씬 단위) 또는 `manualChunks`(Phaser 분리) | 중 (배포 체감) |
| 소스맵 | dist에 14 MB `.map` | gh-pages 배포에서 이미 제외 중 — 유지 | — |
| 구운 데이터 파일 | `PixelFish*.ts` 스냅샷이 번들에 포함 | 필요 시 JSON+지연 로드 | 하 |
| 타일 텍스처 | 맵당 `generateTexture` 캐시(`rmaptex_`) | 현행 유지(알고리즘 변경 시 리로드 필요만 인지) | — |
| 문서 비대 | `AGENTS.md` **375 KB** · `IMPLEMENTATION_PLAN.md` 106 KB — 세션 컨텍스트를 크게 먹는다 | **이 위키 도입으로 신규 기록은 분산**. 기존 히스토리는 §9에 보존(불변), 위키가 인덱스 역할 | 진행 중 |
| 검증 하네스 | 매번 스크립트 작성 | 반복 시나리오를 `scratchpad` 재사용 스크립트로 | 하 |

---

## D. 미해결 설계 질문 (결정 필요)

| 질문 | 현재 기본값 | 결정권 |
|---|---|---|
| ~~두족류 아가미·껍질 쓰임새~~ | **97차 부산물 개편으로 미지급** — 내장·먹물주머니만 잔존(밑밥 연결은 계속 보류) | — |
| 문어 통마리 후속(삶기) | **98차 간이 경로**(인벤 우클릭 [삶기] — `boilOctopus`). 불요리(화구·용기) 실구현 시 CookScene으로 이관·우클릭 제거 | 불요리 착수 시 |
| ~~대문어(`giant_octopus`) 트리~~ | **97차 확정 — 참문어 트리 공유**(`buildOctopusStages` 라우팅) | — |
| 접시 배치 상태 저장 | 세션 메모리(닫으면 반환) | 세이브 스키마 확장 여부 |
| `octopus` 학명(*O. vulgaris* vs *O. sinensis*) | 현행 유지 | 도감 정정 건으로 분리 |
