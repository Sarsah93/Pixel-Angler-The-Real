{
  "name": "@tra/client-pc",
  "version": "0.1.0",
  "description": "The Real Angler — PC/Steam Client (Phaser 3 + Vite)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@tra/core": "workspace:*",
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "rimraf": "^6.0.1"
  }
}
