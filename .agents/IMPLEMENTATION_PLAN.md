# The Real Angler — 구현 계획서 (IMPLEMENTATION_PLAN)

> **최종 업데이트**: 2026-09-03 — **115차: 조도 드론 사진 → 갯바위 타일 시트**(`pixelize_islet.py` · 32×27 지형 패치 · 시트 슬라이스).
> 상세 [워크로그 115](../docs/wiki/03-WORKLOG/2026-09-03-115-jodo-photo-islet.md).
> 직전: 114차 방파제 단면 + 등대([워크로그 114](../docs/wiki/03-WORKLOG/2026-09-03-114-breakwater-section-lighthouse.md)).
>
> **차수별 상세 히스토리는 이 문서에 쓰지 않는다** — 80차 이하 원장은 `.agents/AGENTS.md` §9,
> 81차부터의 본문은 `docs/wiki/03-WORKLOG/`. (구 20~80차 압축 요약 블록은 AGENTS §9와 전부 중복이라 제거 — git 히스토리 보존)
>
> **작업 기준**: 이 계획서가 **로드맵·다음 착수**의 기준입니다. 작업 완료 시 반드시 업데이트하세요.
> **구조화 뷰(시스템별 현황·세부과제·잔여·위험)는 `docs/wiki/`** — 착수 전 대상 시스템 페이지를 읽고,
> 완료 후 **스킬 `work-log`** 절차로 기록합니다. 81차부터 **상세 본문은 `docs/wiki/03-WORKLOG/`**,
> 이 문서와 `AGENTS.md` §9에는 **요약 3~5줄 + 링크**만 남깁니다.

---

## ★★ 작업 이어받기 (2026-07-31) — 여기부터 읽으세요

> **전체 위치**: 지금은 **Phase 6(게임플레이 심화 — 낚시·손질) 안의 "회뜨기(손질) 시스템"을 구현 중.**
> 회뜨기는 낚시 루프에 붙는 **서브시스템 하나**다 — 완결되면 퀘스트/경영/제작 등 대과제로 넘어간다(아래 §차기 대과제).

### ✅ 직전 완료 (106차, 2026-09-02) — 도트 입자 2px 통일 + 해변 경계 재작업 + 편집기 카탈로그

- **질의에 대한 실측 답**: 밝기는 정합이었고(상판 149.5 ↔ 콘크리트 143), 문제는 **입자**였다 —
  맵 규칙 2px 대비 실사 셀만 1px이라 인접 대비가 4~20배. `_clean(grain=2)`로 해안·TTP 리베이크,
  Gemini 생성본도 22색·2px 도트화(15k~24k색 해소).
- **해변 경계 2건 재작업**: 포장 쪽은 삼각 톱니·빗변선·검정 접경선을 걷어내고 **노이즈 모래 스필**로,
  바다 쪽은 접경 풀을 **포말 2종 + 미러 4변형**으로 좁혀 연석형 세로줄·벽돌담을 없앴다.
- **편집기(F7)**: 시트 셀 **126 타일 + 오브젝트 27** 개별 팔레트(자동 생성 `TileCatalog.ts`) ·
  회전/반전(R/X/Y) · **겹침 허용(O) 자유 배치**(0.25타일·충돌 없음) · `RegionPatch.tileTex` 저장.
- ⚠ **`'b'` 자동 규칙·자동 테트라포드 폐기** — 방파제 외해측 소파블록은 이제 **수동으로 쌓는다**.
- **후속(같은 날)**: 이중도로(oneway 쌍) **황색 중앙선** · 골목 접속부 마킹 무절단(주기적 정지선
  해소) · 횡단보도/스크램블 **아스팔트 클립**(직선 우선) · 차도 안 건물 274타일 런타임 정리 ·
  편집기 **호버 고스트**(회전·반전 실물 미리보기).
- 검증: 실렌더 6지점 · FPS 55(min 51) · 저장 왕복 · pageerror 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 106](../docs/wiki/03-WORKLOG/2026-09-02-106-tile-grain-editor-catalog.md)

### ✅ 이전 완료 (105차, 2026-09-01) — 해안 시트 3장 에셋화 + 방파제 실사 타일·갯바위 산포

- **입력 3장의 성격이 서로 달랐다** — 실사 항공사진 + 사용자 격자(96셀) / 검정 테두리 셀 시트 2장.
  ⚠ 바위 셀의 "흰 배경"은 **알파 0**이라 RGB로 탐지하면 셀 전체가 검정으로 잡힌다(실측 후 정정).
- `extract_tileset_assets.py coast` 신설 → **51 스프라이트**(상판·사석·두부·부두 모서리·바위 20·
  Rock-Water 4·물 상세 7). **그룹 톤 정규화**(deck 123~153 → 149.4~149.7)로 바둑판 이음매 해소.
- **방파제 `'b'` 렌더 전면 교체** — 물 비접촉 = 상판 / 외해측·3면 물 = 사석 사면 / 항내측 = 상판
  (물 타일 쪽에 안벽 모서리 오버레이). 실측 분류 상판 1,876 · 사석 628 · 이음 79.
- **갯바위 산포** = 절차 사각형 → 실사 바위 스프라이트(조도 둘레).
- 검증: 실렌더 4지점 · **60 FPS**(방파제 이동 5초) · pageerror 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 105](../docs/wiki/03-WORKLOG/2026-09-01-105-coast-tileset.md)

### ✅ 이전 완료 (104차, 2026-09-01) — TTP 타일 시트 그리드화 + 테트라포드 피복·해변 접경

- **시트 2장은 격자가 아니었다** — 라벨 목업 스크린샷(셀 55×60/64×64 불균일 · 72×72에 고유색
  1,531개). 실측 크롭 → area 리샘플 → k-means 양자화로 **25 스프라이트**를 굽는 추출기 신설
  (`extract_tileset_assets.py ttp` → `public/tileset/ttp/`, 타일 32px = TR 1:1).
- **테트라포드** = 한 덩어리(구 gem 124px 축소) → **소형 유닛 2~3개 클러스터 + 2타일 두 번째 밴드**
  (3크기 × 좌우 플립). 편집기 프롭도 새 스프라이트로.
- **해변 접경** = 시트 접경 셀 4방위 회전 + **바다 색 투명 오버레이**(시트 청록을 통짜로 깔면
  접경마다 사각 색 패치 — 실렌더로 확인) → 103차 절차 서프 밴드 대체.
- **외해 마스크 신설**(`computeOpenSea`) — 구 "바깥 4·7타일 수심 ≥ 5"는 청초호 석호도 통과했다
  (인접 물 75타일 중 41 실측). 맵 경계 물에서 `waterDist ≥ 8`만 타고 확산 → 석호·항 내측 배제.
- 검증: 실렌더 6지점(동명항 피복 · 청초호 0 · 해수욕장 접경 · 조도) · pageerror 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 104](../docs/wiki/03-WORKLOG/2026-09-01-104-ttp-tileset.md)

### ✅ 이전 완료 (103차, 2026-09-01) — 해안 지형 디테일 (사용자 캡처 7장 리포트)

- **원인 실측**: 모래는 terrain에 이미 있었다 — Kenney sand(크림) ↔ tan 포장(베이지) 육안 구분
  불가가 "모래 미적용"의 정체. 방파제는 OSM man_made가 아니라 **coastline 조각**(태그 way 3개뿐).
- **파이프라인**: 방파제 추론(양끝-바다 런 폭 ≤ 10타일 + 인프라 컴포넌트 → `b` 73→**2,510타일**) ·
  해변-바다 연결 성장(`ssss..~~` 단절 해소). 속초 재빌드(patch 321 보존).
- **렌더**: 모래 **웜 틴트**(골든 — tan과 분리) · **서프 포말 밴드**(모래 접경 + 부서진 거품 줄) ·
  수심 **챔퍼 거리 + 버킷 디더**(직각 계단 해소) · **테트라포드**(외해측 수심 ≥ 5 판정 —
  내수면 제방은 자동 제외) · **섬/암초 렌더**(조도 — 45° 모서리 컷·여 산포·이끼·얕은 물 헤일로).
- 잔여: "도로 위 건물" 위치 제보 대기(중앙로 재현 실패 — OSM 건물 성긴 지역) · 대각 계단은
  모래 발색 후 사용자 재평가.
- **후속**: 경계 파편 정리(사용자 X 3건) — 지형 디노이즈(고립 1타일 다수결 동화) ·
  삼각 빗변 경계선(테두리 셀과 연속) · 잔디 삼각 비활성. 경계선 = 경계에만·연속 확인.
- 검증: 실렌더 6+3지점 스크린샷 · pageerror 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 103](../docs/wiki/03-WORKLOG/2026-09-01-103-coast-terrain-detail.md)

### ✅ 이전 완료 (102차, 2026-09-01) — 맵 후속 잔여 일괄 + 섬 지역 분류

- **섬 지역 분류**(사용자 결정): `regions_config.access` 신설 — 울릉·독도 = `'boat'`
  (도보권 불가 — 배 출조 콘텐츠 시 개방). **1차 확장 대상 = land 14개**로 확정.
- **교통**: 사고 차량(180s halt) **추월**(왼쪽 차로 횡 이동·대향차 틈 대기·복귀) —
  "뒤차 무한 대기" 정체 해소. 회전교차로 **분리섬**(OSM oneway 쌍 사이 쐐기 스트립 —
  접근로 10/10이 oneway라 양방향 전제는 전멸, 실측).
- **편집기**: **새 도로 그리기**(클릭 정점·기존 정점/선 자동 접속(투영점 삽입)·우클릭 취소·
  Enter 확정·폭 프리셋 4종) — patch.roads 오버라이드·교통 재생성 공용 커밋.
- **렌더**: 지면 **오토타일**(잔디 블롭 16조합 흙 림 + tan/pier 8방위 테두리 — 노치 셀은
  갈색 블롭이라 폐기) · **물 타일셋**(수심 버킷×2변형 절차 베이크 — Kenney엔 바다 셀 없음) ·
  지붕 **2×2 패널 타일링 + 대각 스텝 45° 컷**(열 4~7이 정합 블록 — 열 0~3은 "05" 파편) ·
  **신호 교차로**(w≥4×2 클러스터 90곳 — 신호등 프롭 + half≥3.2 대형만 스크램블 X).
- 검증: 실렌더 하네스 5종(추월 시계열·새 도로 실마우스·분리섬/오토타일/지붕/신호 스크린샷) ·
  pageerror 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 102](../docs/wiki/03-WORKLOG/2026-09-01-102-map-followups-batch.md)

### ✅ 이전 완료 (101차, 2026-08-28) — dev 맵 편집기·순간이동 + 차도 벡터 마킹 + 프롭 10종

- **F7 맵 편집기**(dev): 지형/프롭/지붕 페인트 → 영향 청크 즉시 재베이킹 · Ctrl+Z ·
  `patch.json` 저장(vite dev 미들웨어 — 정본 `pixelazed/` + 런타임 `public/data/`).
- **Ctrl+클릭 순간이동**(맵·미니맵) · F10 '+최대' — 수동 검증 동선 단축.
- **차도 마킹 벡터화**: `roads.json` → 노란 중앙 실선·흰 점선 차선·가장자리선, **대각선 대응**.
- 프롭 10종(침엽수·덤불·바위·벤치·가로등·화단·기념탑·어선 …) 편집기 팔레트.
- **후속**: 차선 연속성(오프셋 폴리라인)·차도 폭 차로 기준 + **타일셋 통합**(Gemini·TopDown·Kenney →
  POI 프리팹(횟집/팝업/고층/주택)·차량·NPC·프롭 40여종) + **TR 32 전환 + Kenney 지면 베이스**
  (사용자 결정 — 청크 32타일·이동 210px/s) + **후속 3**(건물 2.5D 충돌/지붕 y-sort · 상점 오브젝트
  충돌 · 도로선 격자 스냅 · TrafficSystem 우측통행 · 지면 접경선) + **후속 4**(에셋 격자 학습 → Kenney
  건물 키트 채움 · 직각삼각형 대각 엣지 · 전 오브젝트 충돌 + 배치 격자 · 편집기 탭 팔레트 · 정점 그래프
  교통 · 보행자) + **후속 5**(도로 벡터 밴드 렌더 · 교차로 박스 · 차간/양보 · 막다른 끝 페이드).
  + **후속 6**(편집기 도로 벡터 툴 · 횡단보도/정지선 벡터 · 아스팔트 톤 · 보행자 밴드 판정)
  + **후속 7**(회전교차로·일방통행·이중 황색선 · 차량↔캐릭터 대기/충돌).
  잔여 = 새 도로 그리기·분리섬/신호등·사고 정체 해소·대각 건물 코너 셀·물 타일셋.
- 검증: 실렌더(페인트 9타일·되돌리기·프롭 저장 디스크 확인·순간이동 2경로·타일셋 5지점) · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 101](../docs/wiki/03-WORKLOG/2026-08-28-101-map-editor-road-vectors.md)

### ✅ 이전 완료 (100차, 2026-08-27) — 문어 픽셀 에셋 매핑 + OSM 실지형 심리스 맵 v2

- **문어 에셋**: 사용자 픽셀 투명 에셋 10장 → `octo_*` 전량 교체(scrub 품질 해소 ·
  `octo_invert1` 신규 · 부리 아이콘 7.png) + 오징어 레퍼런스 이동 경로 정합(재생성 소실 함정).
- **OSM 심리스 v2 — 속초 완주**: OSM 파이프라인 3종 `tools/` 배치 → fetch/build(589×321 ·
  POI 310 · 스폰 스냅) → `SeamlessChunks` 청크 스트리밍(RT 풀 12 · 3×3 상주 · 근접 충돌) →
  POI 거래·캐스팅→1인칭·60FPS 실렌더 검증. legacy(부산·홈타운) 폴백 보존 · ODbL 크레딧.
- **잠복 버그 수정**: `pointer.worldX` 미변환(스크롤 카메라에서 조준 반전) → `getWorldPoint` 교체.
- **후속(같은 날 — 사용자 피드백)**: 스케일 2배(`TILE_M 5` · 도로 미터 폭 · 보도 `w` 신설) +
  §11 L1·L3 절차 구현(차선·연석·지붕·나무·포말·배 등) + **HUD 픽셀 패널**(`HudPanelStyle` —
  타이틀 명패·구름 텍스처·심리스 야간 발광 수정). 실렌더 4지점 · 60FPS 고정.
- 스펙 = [`.agents/OSM_TILEMAP_SPEC.md`](OSM_TILEMAP_SPEC.md) — **§0.5 코드 정합 노트가 본문보다 우선**.
- 검증: 실렌더 4하네스 PASS · pageerror 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 100](../docs/wiki/03-WORKLOG/2026-08-27-100-octo-pixel-osm-seamless.md)

### 🚧 다음 착수 — 맵 확정(사용자 육안) → OSM land 14개 확장 → 두족류 마무리 → 해루질·통발 (2026-09-01 102차 갱신 — 사용자 로드맵)

**0-a′. 맵 확정 — 사용자 육안 확인** (102·106차로 dev 잔여는 전부 소화)

- 주간 시각 확인 · 정지선 방향(우측통행 진입 차로) — 101차부터 이월된 육안 항목.
- 102차 신설분 체감: 사고 추월 · 분리섬 · 오토타일 흙 림 발색 · 물 그레인 · 지붕 패널 ·
  신호등/스크램블 X 밀도(휴리스틱 90곳 — 과하면 임계 조정).
- **106차 신설분 체감**: 도트 입자 2px 통일(해안·TTP·**Gemini 건물/NPC 외형 변화**) ·
  해변 경계(모래 스필·포말선) · `'b'` 상판 단일화.
- **106차 후속 수작업**: 방파제 외해측 테트라포드를 F7 **겹침 허용(O)** 으로 다시 쌓는다
  (자동 피복 폐기). 사석·안벽·부두 모서리도 개별 타일 팔레트에서 직접 배치.

**0-a. OSM 심리스 지역 확장 — land 14개** (속초 v2 ✅100차 · 맵 확정 후 착수)

- **울릉·독도는 제외** — `regions_config.access='boat'`(102차, 사용자 결정): 도보권 불가 섬,
  배 출조 콘텐츠(흘림낚시·다운샷·원도권 갯바위) 업데이트 시 개방.
- 절차(코드 변경 없음): `py tools/fetch_region_osm.py <region>` → `build_osm_tilemap` →
  `build_region_maps` → core `SEAMLESS_REGIONS` 등록 + `REGION_AREA_NODES` 구역 노드.
- **지역마다 terrain.png ↔ OSM 육안 대조 + 스폰 검수 필수**(스펙 §9 — 사용자 동반).
  ⚠ 서해 taean 2곳은 sea ratio 경고 시 seaEdges 조정. 지역 안 부속 섬은 자동 카브가
  없어(OSM 벡터 정본) 고립 컴포넌트로 남는다 — 걷기 컴포넌트 리포트로 확인만.
- 후속 결정 대기: §11 비주얼 4레이어 에셋 발주 · 스폰 다중화(`meta.spawns[]`) ·
  legacy 7맵 제거 시점.

**0. 🚧 무늬오징어 수동 검증 — 진행 중** (90~95차로 전 구간 재구성 완료 — **20스테이지**)

- ~~0a. 재구성~~ — **95차 완료**: 껍질 가로 뜯기 = **4단계 분할**(짧은 당김마다 다음 사진 —
  0-1→1-2→2→3→4.1, 뜯기 ② 완료 = 4.2) · 들추기 플랩 = **목업 0-1 직접 베이크** ·
  날개살 = 드래그 **후** 전환(5.1 좌우+상하반전) · peel 당김 품질 = 스윕 길이 비례.
  상세는 [워크로그 095](../docs/wiki/03-WORKLOG/2026-08-13-095-squid-peel-4step-flap-bake.md).
- **0b. 잔여 좌표 F9 실측** — ~~껍질 가로 1/4~4/4~~(**96차 실측 반영**) ·
  내장 2/2 당김(`CEPH_VISCERA_PATH_2`) · 뜯기 ② 상→하(`CEPH_PEEL_FINISH_SWEEP`) ·
  날개 껍질째 2경로(`CEPH_FINSKIN_PATHS`).
  peel 좌표 기준 = **그 단계의 시작 사진**(subjectRect 캐노니컬).
- **0c. 품질·체감 확인**(사용자) — 플랩 재채색 발색(96차 — 좌 껍질톤/우 흰색) ·
  `squid_gill_on`(아가미 다소 성김) · 91차 합성 3종 · 회전 배치 크기(고정 250px — 96차).
- 함께 확인할 것: 아가미 붙은 안쪽면·껍질 완전 분리본 실사(계속 대기 — 오면 키 교체만) ·
  사이드바 어류 문구("삼면뜨기 (양살 2필렛 + 중골)") 잔존.

**0d. 97~100차 신설분 수동 확인**(사용자) — 회뜨기 체감(몸통살 22점·날개 2장·촉완 0.70 근사) ·
  **문어 픽셀 에셋 체감(100차 — 0~9 전량 교체)** · 삶기→다리 분리(3컷)→숙회(7컷) 체인 ·
  문어 가이드 좌표 F9 실측(**100차 스프라이트 교체로 subjectRect 변동 — 재실측 필수**,
  오버라이드 키 `ceph_octoWhole`/`ceph_octoLeg` 포함).

**1. 두족류 잔여 — 갑오징어 13** (~~한치~~·~~문어~~·~~슬라이싱~~ = **97차 완료**)

- 스펙 §4.3: `bone_lift`(갑 들어내기)·속껍질(`peelTool:'towel'`) — 프리미티브 2종 판정 신설 +
  `kitchen_towel` 소모품 검토. **날개살 미지급·'갑오징어 뼈' 규칙은 97차에 데이터 완비.**
- ⑦ 수율·등급 연동(현재 회 조각 등급 = 회뜨기 정확도만) — 갑오징어와 함께.
- ⚠ 착수 전 §0.5(코드 정합 v3.1) 필독. 스테이지 1개=작업 1개·순서강제 규약 유지.

**1b. ~~두족류 시트 도트 추출~~ — 89차에 실사 도트로 해소.** `ui/CephalopodFish.ts`는 이제
**실사 우선 · 파라메트릭 폴백**이고, 무늬오징어 정본은 SVG가 아니라 **구운 실사 스프라이트**(`squid_*`)다.
SVG 4장은 나머지 3종(한치·갑오징어·문어) 착수 시 참고자료로 남는다.

**1c. 두족류 가이드 F9 실측** — **1~3단계(시메×2·개복·내장·연골)는 90차 반영 완료.**
잔여는 위 **0b**(껍질 들춤·아가미·날개 2선·3분할 2컷·부리)와 같은 항목이다.
⚠ 좌표 기준은 89차부터 **피사체 rect** — 87차 근사값을 그대로 신뢰하지 말 것.

**2. 광어 가이드 잔여** — F9 `upSep1`·`upSep2`·`dnSep1`·`dnSep2`·`dnScore` (**엔가와·박피는 반영 완료**) ·
등쪽 단면 실사(`halibut_back_open`) 투명본 대기. **칼 연출은 현행 확정**(사용자 2026-08-09).

**3. 해루질·통발 완성** — 99차 조사로 실상 확정(엔진·씬 동작 / 배선 미완) + 결함 2건 수정 완료.
잔여 배선 D2~D5는 [S14 페이지](../docs/wiki/02-SYSTEMS/night-hunting-trap.md) §4·§5.
이후 **불요리(sink 최고 레버리지) → 농장 경영(스타듀밸리형 — E1~E5,
[S8 페이지](../docs/wiki/02-SYSTEMS/home-base.md) §5)**. **퀘스트/스토리는 모든 컴포넌트 구현 후**(사용자 방침).

### ✅ 직전 완료 (99차, 2026-08-14) — 발견 도감·위키 + 통발 결함 2건 + Dev 크리에이티브 콘솔

- **통발 결함 2건**: `'shellfish'` 타깃 매칭 0 → `gastropod` 정정 · 장어/어류 통발에
  `targetFishSpecies` 신설(붕장어·먹장어·갯장어·볼락류 실포획 — 쿨러 type 'fish' 연동).
- **발견 시스템**: `DiscoveryStore`(어종/해양생물/아이템 공용 — 세이브 영속·구세이브 legacy 백필) +
  발견 소스 4곳(어획/통발/해루질/취득) + HUD 신규 발견 토스트 + **N 키 도감 진입**.
- **도감 4탭**: 어종(미발견 = 실루엣+서식 힌트)/해양생물/아이템 위키(판매처 입수 힌트)/조과 기록.
- **Dev 콘솔(F10)**: 무적(신선도 동결·손실/줄터짐 없음)·재화·아이템/어종 지급·도감 전체 해금.
- 검증: 통발 시뮬 500×3 · 실렌더 **19/19** · pageerror 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 099](../docs/wiki/03-WORKLOG/2026-08-14-099-discovery-wiki-trap-devmode.md)

### ✅ 이전 완료 (98차, 2026-08-13) — 문어 실사 스프라이트 + 삶기 · 다리 분리 · 숙회 · 플레이팅

- **실사 9장 → 도트 9키**: 파이프라인 **KEEP_POLY**(피사체 폴리곤 — 손·싱크대 제거) 신설 +
  스테이지 **재배열**(부리를 소금 앞으로 — 사진 넘버링 = 실제 공정) + 드래그 진행 프레임 3구간.
- **통마리 아이콘** = '손질 완료' 실사 도트(`gen_octo_assets.cjs` 신설 — 삶은 문어 계열 PNG 4종 포함).
- **삶기 체인**: 통마리 우클릭 [삶기](**'신선'** 시작·간이 경로) → **삶은 문어만 도마** →
  3컷 다리 분리(머리 1 + 다리 8) → **사선 7컷 숙회**('문어 숙회 한 점' ×8 — 밀림 연출·접시 = 숙회 실사).
- 부수 정정: ceph 부산물 `byproductKind` 내장류 한정(통마리에 '만들기'가 뜨던 함정).
- 검증: 실렌더 **32/32**(실마우스 컷·스크린샷 8장) · 생성물 회귀 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 098](../docs/wiki/03-WORKLOG/2026-08-13-098-octopus-photo-boil-sukhoe.md)

### ✅ 이전 완료 (97차, 2026-08-13) — 두족류 부산물 개편 + 회뜨기 3모드 + 한치 개방 + 문어 트리

- **부산물 개편**: 껍질·아가미 미지급 · 뼈 명명(무늬오징어/갑오징어) · **순수 필렛 최종 지급 제거**
  (몸통살 = 원물 판매가 ×0.75 승계).
- **회뜨기 3모드**: 몸통살 22점(가로 1 + 세로 10 관통) · 날개살 4점(좌우 2장) ·
  다리부 → 촉완 ×2 + 촉완 제거 다리부(회 아님).
- **한치** = 무늬오징어 트리 공유 · **문어 8스테이지**(굵은소금 1개 소모 게이트 — 시드+마트) +
  OCTO 3뷰 파라메트릭.
- 검증: core **38/38** + 회귀 61/61 · 실렌더 **32/32** · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 097](../docs/wiki/03-WORKLOG/2026-08-13-097-ceph-byproducts-slicing-octopus.md)

### ✅ 이전 완료 (96차, 2026-08-13) — 패널 침범 해소 + 크기 통일 + 연출 후 전환 + 3분할 조각 밀림

- **작업 패널 3열**(7작업 = 3×3 · 하단 148 ≤ 프레임 164) · **크기 통일** — 표시 스프라이트 세로
  고정 250px(`cephRotScaleFor`) — 프레임 널뜀·도마 이탈 해소.
- **연출 후 전환**(`CEPH_SWITCH_T` 0.7 + `cephAnimStageId` 표시 고정) — 53차 규칙 재적용.
- **3분할 조각 밀림**(`drawHeadmass`) + **부리 = 가운데 조각만**(좌표 불변) · 가로 4스윕 실측 ·
  플랩 재채색(좌 껍질톤/우 흰색).
- 검증: core **61/61** · 실렌더 **30/30**(완주) · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 096](../docs/wiki/03-WORKLOG/2026-08-13-096-squid-layout-size-splitfx.md)

### ✅ 이전 완료 (95차, 2026-08-13) — 껍질 가로 4단계 분할 + 플랩 목업 베이크 + peel 품질 길이 비례 (20스테이지)

- **가로 뜯기 4분할** — 짧은 당김마다 다음 사진(라이브 프레임 전환 폐기 — finskin만 잔존).
  뜯기 ② 완료 = 4.2 원본. 2~4/4 당김 좌표 근사(사용자 수동 지정 예정).
- **플랩 = 목업 0-1 베이크**(절차 합성 폐기) — 파생 정리 4단(보더 플러드·검은 띠·침식·연결요소).
  **날개살** = 드래그 후 전환(5.1 좌우+상하반전). **peel 품질** = 스윕 길이 비례(짧은 스윕 실패 해소).
- ⚠ 함정 2건 문서화: ERASE_POLY의 알파 경로 오판 · 캡처 투명 여백의 도마색 잔존.
- 검증: core **61/61** · 실렌더 **27/27** · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 095](../docs/wiki/03-WORKLOG/2026-08-13-095-squid-peel-4step-flap-bake.md)

### ✅ 이전 완료 (94차, 2026-08-13) — 내장 2분할(미러) + 날개 순서 재정의 + 3분할·부리 실측 (17스테이지)

- **내장 분리 2분할** — 뽑기 1·2 미러 재베이크(개복 화면과 좌우 정합) + 드래그 2회 단계화
  (1/2 = 뽑기 1 화면 · 2/2 완료 = 뽑기 2 + 부산물 팝업). 2/2 당김 경로는 근사(F9 대상).
- **날개 순서** — 날개살 분리 1/2·2/2 → 껍질째 뜯기 1/2·2/2 (실측 수평 2선 · 5.1 상하반전
  `FLIPV_KEYS` 신설). **3분할**(세로 0.771/0.510)·**부리**(0.575→0.493) 실측 배선.
- 검증: core **53/53** · 실렌더 **23/23**(s16_00a~18) · 어류 회귀 6/6 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 094](../docs/wiki/03-WORKLOG/2026-08-13-094-squid-viscera-split-fin-order.md)

### ✅ 이전 완료 (93차, 2026-08-13) — 껍질·날개 detail 시퀀스: 진행 프레임 5장 + 손잡이 연출 + 좌표 정정

- **껍질 = 회전 뷰(270) 유지**: 탭(→ 손잡이 플랩 `squid_skin_lift`) → 우→좌 곡선 · 5프레임
  (들춤→1-2→2→3→4.1) → 상→하 마무리(연출 4.2) → **실사 6**(구 파생 대체). 신규 사진 cw 베이크.
- **탭 좌표 정정** — 캡처 「벗기기 0」의 링 위치(화면 우상단)로 92차 로컬 오독 확정 → `[1−v,u]` 역변환.
- **날개살 분리** — 시작본 `squid_fin0`(+미러) 신설 + path 드래그 진행 주입(`CEPH_DRAG_FRAMES` 일반화)
  = 드래그 중 실시간 들림 → 5.1/5.2.
- 검증: core **49/49** · 실렌더 **20/20**(s16_01~18) · 어류 회귀 6/6 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 093](../docs/wiki/03-WORKLOG/2026-08-13-093-squid-skin-detail-sequence.md)

### ✅ 이전 완료 (92차, 2026-08-13) — 무늬오징어 껍질 3구간 회전 뷰·진행 프레임 + 날개 각 2단 분할 + 아가미 합성

- **껍질**: 들추기 = **탭 + 좌 90° 회전 뷰**(F9 실측 7점) / 뜯기 ① = 실측 스윕 + **진행 프레임**
  (`peelDragT` 온전→4.1) / 뜯기 ② = **우 90° 회전**(4.1→합성B · 완료 연출 4.2) / 완료 = 순살만(파생).
- **날개 각 2단 분할**(14→**16스테이지**·trim 5작업) — 껍질째 2/2 = 미러 베이크 · 날개살 = 5.1/5.2.
  **아가미** = `squid_gill_on` 합성(clean + 2.2 기관 색 추출 506px) + 실측 스윕.
- **회전 체계** — 두족류 `advance()` 회전 자동 스냅(수동 R 차단) + `renderedRotation`(연출 중 옛 회전) +
  `cephRotScale`(세로 예산 동적). 파생/미러 4키 재굽기(기존 0 삭제).
- 검증: core **48/48** · 실렌더(실마우스) **17/17** · **어류(광어) 회전 회귀 6/6** · pageerror 0 ·
  빌드 4/4 · typecheck 0.
- 상세: [워크로그 092](../docs/wiki/03-WORKLOG/2026-08-13-092-squid-skin-rotation-fin-split.md)

### ✅ 이전 완료 (91차, 2026-08-12) — 무늬오징어 3구간 재구성: 껍질 2회 뜯기 + 내장 연출 2프레임 + 날개 2단

- **에셋 6키** — 사진 3(내장 뽑기 연출 1/2 · 날개뜯기1) + **추론 합성 3**(`deriveSkinOver` — 껍질 온전 ·
  아래로 뜯기 시작 · 필렛+껍질+날개 온전. 영상(껍질벗기기.mp4) 형태 참고, 실사 부재라 사용자 지시로 추론).
- **트리 12→14** — `ceph_skin_finish`(아래로 — 90차 실측 스윕 배선) · `ceph_finskin_off`(날개 껍질째 2경로) 신설.
  아가미·닦기는 날개 뒤로 이동(완료본 실사와 진행 정합).
- **판정** — peel 잡기 = `tapPoint` 반경 · 당김 = 스윕 방향 투영(구 좌로 고정이면 대각 스윕 q 0.084로
  가이드대로 그어도 실패). **연출** — 내장 뽑기 = 실사 2프레임(`cephAnimOverride`) · peel = 그립 링(칼 아님).
- 검증: core **28/28**(14스테이지 완주·구 공식 실패 재현) · 실렌더 스테이지별 + 연출 프레임 실측
  (lift1→lift2→해제) · 굽기 회귀 0 삭제 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 091](../docs/wiki/03-WORKLOG/2026-08-12-091-squid-skin-viscera-fin-rework.md)

### ✅ 이전 완료 (90차, 2026-08-11) — 무늬오징어 가이드 F9 실측(1~3단계) + 트리 12스테이지 + 당김 연출

- **좌표 실측 5경로 반영** — 시메①②·개복·내장·연골(피사체 rect 기준) + 껍질 마무리 스윕 보관
  (`CEPH_PEEL_FINISH_SWEEP` — 4단계 재구성 시 배선). "전체적으로 커버율 부족" 해소.
- **트리 14→12** (사용자 지시) — 펼치기·분리 결과 확인 제거. 개복 완료 = 곧 펼쳐진 화면에서 내장 분리,
  결과는 머리+다리+내장 덩어리 **부산물 팝업**(174g·전용 아이콘)이 대체.
- **`evalNerveCut` 재작성** — 선 위 시작 + 선 길이 비례(구 판정은 실측 시메 선에서 시작 링 시작도 실패).
  **당김 연출 분리** — `drag_out`/`lift_flap`은 칼 글리프 없이 그립 링 + 잔선.
- 검증: core 실판정 33/33 · 실마우스 실렌더 S1~S5 완주 · pageerror 0 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 090](../docs/wiki/03-WORKLOG/2026-08-11-090-squid-guide-coords-stage-trim.md)

### ✅ 이전 완료 (89차, 2026-08-10) — 무늬오징어 도마 실사 도트 배선 + 좌표계 피사체 기준화

- **실사 11장 → 14스테이지** — 사용자가 공정 순서로 이름 붙인 사진(`reference/cephalopod/`)을
  `pixelize_butchery.cjs` `CEPH_SRC`(한글 파일명 → `squid_*` 키 명시 매핑, 원본 복제 없음)로 굽고
  `SQUID_STAGE_SPRITE`로 14스테이지에 배정. `CephalopodFish`는 **실사 우선 · 파라메트릭 폴백**
  (백로그 1b "시트 도트 추출" 해소 — SVG는 나머지 3종 참고자료로 잔존).
- **좌표계** — `subjectRect` 신설(유도선·입력·F9 공유). **두족류만** 스프라이트 실점유 rect,
  어류는 생선 rect 그대로. 뷰 종횡비가 0.27~2.6으로 흔들려 도마 기준으로는 tolerance 의미가 뷰마다 달라진다.
- **부산물 아이콘** — `ceph_head_mass`(머리+다리+내장 덩어리)가 내장 주변부 대체본을 쓰던 것 →
  사용자 제공 전용 에셋 `trim_ceph_head_mass`로 교체.
- 검증: 굽기 회귀 **0 삭제**(기존 20 스프라이트 바이트 동일) · 실렌더 15/15 배선·좌표오차 0·도마 이탈 0 ·
  **어류 회귀 cov 1.00** · pageerror 0 · 빌드 4/4 · typecheck 0.
- ⚠ 잔여: `ceph_skin_done`·`ceph_gill_wash`는 전용 사진이 없어 근사 배정(사용자 확인 대상) ·
  **두족류 가이드 좌표는 좌표계 변경으로 전량 재측정 대상**.
- 상세: [워크로그 089](../docs/wiki/03-WORKLOG/2026-08-10-089-squid-photo-stage-sprites.md)

### ✅ 이전 완료 (88차, 2026-08-10) — 검은 화면 방어 4종 + 무늬오징어 무게 실측 정합

- **검은 화면 전수검사** — 사용자 케이스(NEW GAME 시 전면 검정·무에러)는 입력 경로 4종 재현 실패.
  대신 잠복 결함 4건 확정·수정: WebGL 컨텍스트 유실 오버레이(`game.ts installCrashGuards`) ·
  전역 에러 배너 · `RegionFieldScene` 맵 JSON 캐시 미스 가드(`bootFailed`) · `quitGame` 소프트락 해제.
  재발 시 오버레이/배너 문구가 원인을 자가 보고한다.
- **무늬오징어 무게** — 전장 기준·wf 0.015 확정(캡처 정합: 30cm 405g / 45cm 1,367g / 1kg = 40.5cm+).
  오라클·dev 지급·도감 3곳 정렬. 한치·갑오징어·문어는 실측 캡처 제공 시(백로그 3d).
- 부수: 한치 아이콘 public 동기화 · PLAN 가독성 재구성 · 스킬 `doc-readability` 신설(12종).
- 검증: 실렌더 6/6 PASS(quit 복구·연타·에러 배너·컨텍스트 유실/복구) · 곡선 수치 검증 · 빌드 4/4 · typecheck 0.
- 상세: [워크로그 088](../docs/wiki/03-WORKLOG/2026-08-10-088-blackscreen-guards-squid-balance.md)

### ✅ 이전 완료 (87차, 2026-08-09) — 두족류 잠금 해제: 무늬오징어 14스테이지 완주
- ⚠ **잠금은 플래그가 아니었다** — 80차는 재료 준비였고 트리·판정·렌더러가 전부 0건. 해제 = 구현.
- **core**: `CephalopodStages.ts`(14스테이지) · `SQUID_SECTIONS`(6섹션 14작업) · `ButcheryProcess` 두족류 분기
  (뷰 자동 스냅·`evalNerveCut`) · **`primitiveInput()`**(프리미티브 19종 → 조작 5종) · **`canButcherSpecies`**(squid만 개방).
- **client**: `ui/CephalopodFish.ts` 6뷰 렌더 · 패널 분기 15곳 정규화 · 두족류 부산물/체크포인트 배선.
- **부수**: 어종별 도마 온마리 10키(`gen_species_sprites.cjs`) · 돌돔 성별 배선(`InvItem.sex` 신설) · 두족류 dev 지급 4종.
- 검증: core 12/12 ALL PASS · 실렌더 완주 · 부산물 10종 지급(몸통 순살 362g) · pageerror 0 · 빌드 4/4.
- 상세: [워크로그 087](../docs/wiki/03-WORKLOG/2026-08-09-087-cephalopod-squid-tree.md)

### ✅ 이전 완료 (86차, 2026-08-07) — 광어 박피 F9 실측 + 회썰기 유도선 dev 편집기
- **박피 좌표 실측 반영**: 넙치류 트리 `peel_grip` 7점 · `peel_insert` — cov 1.00 검증, 돔류 무변경.
- **회썰기 유도선 편집기(F9)**: SashimiPanel 핸들 드래그 + [복사] → core `SASHIMI_CUT_OVERRIDES`(신설) 반영
  체계 — 엔가와·순수 필렛(bream/amberjack/halibut)·고급 전 레이아웃 공통. **오버라이드 값은 사용자 측정 대기.**
- 상세: [워크로그 086](../docs/wiki/03-WORKLOG/2026-08-07-086-peel-coords-sashimi-editor.md)

### ✅ 이전 완료 (85차, 2026-08-07) — 개방 범위 머리쪽 확장 + 필렛 아이콘 반쪽 에셋
- **개방 확장**: 렌더 엔벌로프가 `bodyL 0.28` 세로선에서 잘려 머리 S컷 절단면과의 쐐기가 안 열리던 것 →
  `cutXatY`/`clipSpan`(S컷 곡선 컬럼별 클리핑)으로 절단면까지 확장 — 4개 포 공통, 렌더 층만(F9 좌표 무변경).
- **필렛 아이콘**: `skinned_upper/under_pillet_halibut` 교체(upper=내장 없는 쪽 / under=내장쪽) —
  `buildYieldRows` taskId 스레딩, 내장쪽 반 = `t_flb_upper`·`t_flk_lower`. 구 `_1/_2` 키는 구세이브 호환 유지.
- **후속**: 엔가와 분리 **도마 슬랩**도 같은 에셋의 도트화(`fillet_upper/under_halibut` + `flatFilletKind` —
  필렛 1·4=내장쪽 under / 2·3=upper, 재장착은 원물 아이콘 판정)로 교체 — 구 단일 `pure_fillet_halibut` 슬랩 대체.
- 검증: 아이콘 4매핑·실지급 3건·등/배/중간 실렌더 스크린샷·pageerror 0. 빌드 4/4·typecheck 0.
- 상세: [워크로그 085](../docs/wiki/03-WORKLOG/2026-08-07-085-flat-open-extent-fillet-assets.md)

### ✅ 이전 완료 (84차, 2026-08-07) — 광어 포 뜨기 공정 재정의: 3단계 × 4작업 + 칼 팔로우 연출
- 사용자 공정 명세(캡처 3장) 반영 — 포 뜨기 = 반쪽마다 **독립 경로 3단계**(경계 칼길 꼬리→머리 / 분리1·2 머리→꼬리),
  `flat_gut_scoop` 폐지(**내장 = 머리 S컷 동반 지급**), 82차 실사 도마 직접 렌더 폐지(도마 = 자체 픽셀 + 파라메트릭).
- **칼 팔로우**: 자체 픽셀 야나기바(칼끝 파묻힘 crop = 드래그 접점) 딜레이·제한속도 추종 + **이동 힌지 벌어짐**
  (경계→중앙선 — 칼이 지나간 구간만 들림). tuning 4키(F8). 검증: 30스테이지 무결·3단계 실드래그 완주(0.18→0.55→1→필렛 팝업)·
  머리 팝업 = 머리+내장·pageerror 0.
- 상세: [워크로그 084](../docs/wiki/03-WORKLOG/2026-08-07-084-flat-lift-3stage-knife.md)

### ✅ 이전 완료 (83차, 2026-08-06) — 90° 회전 재설계: 도마 가로 고정·생선만 회전
- 사용자 리포트 3건(도마가 세로로 서는 설계 / 세로로 그은 자국이 가로로 남음 / HUD 겹침·튀어나감) 해소 —
  **board rect(가로 고정)/fish rect(회전 반영) 분리**, 트레이스·F9 핸들 `toPanelPx` 경유, 레이아웃 앵커 9곳 board 전환,
  `jumpTo` 방향·회전 동시 스냅. 후속: 세로 생선 **도마 밖 허용·창 안 제한**(PANEL_H 656 + ROT_SCALE 0.55 — 생선 141~449).
  검증: 유도선 세로 스팬·드래그 cov 통과·F9 핸들 오차 0·devExpand 박스 내·실사 도마 걸침 렌더·pageerror 0.
- 상세: [워크로그 083](../docs/wiki/03-WORKLOG/2026-08-06-083-rotation-fish-only.md)

### ✅ 이전 완료 (82차, 2026-08-06) — 광어 포 뜨기 실사 5단계 픽셀화·배선
- 실사 투명본 5장(= 한 필렛의 연속 공정) → 6키(`fin_score`·`lift_a_up/dn` 합성·`lift_b`·`lift_done`·`gut_lift`) +
  `drawFlatFish` 진행도 기반 선택 + 파이프라인 `ROTATE_KEYS`/`ERASE_POLY`. 세로 배치 실렌더는 83차에서 확인 완료.
- 상세: [워크로그 082](../docs/wiki/03-WORKLOG/2026-08-06-082-halibut-photo-stages.md)

### ✅ 이전 완료 (80차, 2026-08-06) — 도마 90° 회전 + 포 뜨기 벌어짐 연출 + 배쪽 단면 실사 + 두족류 정합/에셋
- **90° 회전 = 독립 축**(`BoardRotation`): core(`rotationRequired`·`rotate()`·`rotationOk()`·canAct 편입·jumpTo 스냅) + client(2×2 버튼·R/Shift+R·세로 도마 프레임·`rotNorm`/`unrotNorm`·캔버스 변환 렌더·회전 우선 힌트). 넙치류 포 뜨기 11스테이지에 `rotationRequired: 90` + 칼길 **꼬리→머리** 반전.
- **벌어짐 연출**: 칼질 성공 → **액션 연출 완료 후** `flatOpenMs`(650ms) 보간으로 **바닥 고정·위쪽 덩어리만** 중앙선 힌지로 열림(이산 단계 → 연속 보간).
- **배쪽 단면 실사**: 사진 3번 투명본 → `halibut_belly_open`(128×93) → `gutsExposed` 상태에 렌더. 나머지 4장은 `butchery/reference/`로 보존.
- **`pixelize_butchery.cjs` 누끼 사문 수정 3건**: bgTol 미전달로 **TH가 undefined**(배경 제거 무력화) · 배경색 평균→**중앙값**(피사체가 프레임에 닿으면 평균이 밀림) · **알파 경로**+순백 매트 제거.
- **두족류**: 스펙 **정합 v3.1 §0.5**(speciesId 4건 치환·프리미티브 실재 6종 확인·심볼 대조표·구조 결정·부록 3건 해소) + core 1단계(뷰 9·프리미티브 11 추가·프로필 4·부산물 19·가이드 좌표·tuning 29키) + **에셋 15키** + **부리 공정 `ceph_beak_out`·`ceph_beak` 신설**(오징어류 스테이지 13→14/14→15/12→13).
- 검증: 회전 4방향 왕복 변환 오차 0·유도선 정착·세로 렌더 / 벌어짐 보간 / 실사 렌더(흰 매트 없음) / 텍스처 15/15 · 아이콘 분기 6케이스 / pageerror 0. 빌드 4/4·typecheck 0.
- ⚠ **두족류 트리 자체는 미착수** — 위 "🚧 다음 착수" 1번 참조.

### ✅ 이전 완료 (79차, 2026-08-05) — 한치(창꼴뚜기) 어종 4계층 등록 (두족류 손질 선행)
- **오라클** `swordtip_squid` — Uroteuthis edulis(2026-08 리서치: 외투장 35~40cm · 서식 30~170m · 제주/남해 여름 밤 집어등 · 공략 20~30m · 18~24℃): `['open','structure']` 15~120m · **layers `['mid','surface']`**(야간 부상) · lure 95 · **egiOnly** · 12~40cm(외투장 기준) · wf 0.016 · **nightBonus 1.9**(두족류 최고) · 금지체장/금어기 없음.
- **도감**(제철 5~9월 · 회 35,000원/kg — 오징어류 최고가) · **텍스처 배선**(`FISH_TEXTURE`) · **경락 3곳**(Economy 32,000원/kg / **MAFRA `'한치'` 오매칭 정정** + `'창꼴뚜기'` / KOSIS 전용 항목 선착순 앞배치) · **에기 spawnBinding 2종**.
- 검증: 야간 표층 12m **83.6%** / 중층 25m 44.1% / 바닥 60m 12.3% = 수심층 분리 · 생미끼 채비 0%(egiOnly) · 판매가 단조 증가 · MAFRA 6케이스 · 도감 실렌더(5/5페이지 실사 카드) · pageerror 0. 빌드 4/4·typecheck 0.
- 부수: 오라클 주석 "49종" → **56종** 정정(실제와 어긋나 있었음. FISH_DATABASE 57 = 56 + 야간 참돔).
- ⚠ 손질은 여전히 `cephalopod` 스텁(정상) — 트리·렌더·에셋이 다음. dev 지급도 손질 구현 시 함께(74차 광어 전례).

### ✅ 이전 완료 (78차, 2026-08-05) — 광어 F9 실측 1차 + 배쪽 포 뜨기 3D 렌더 + 작업 패널 2열
- FLAT_GUIDE 실측 7종(시메·방혈·S커브·비늘 base/flip 분리·꼬리·**중앙선 전장 연장**·경계 칼길) + 포 뜨기 4스테이지 3회화.
- drawFlatFish 개편(사용자 스케치): 떠진 자리 = 척추+갈비뼈 부챗살 / 뜬 살 = **중앙선 힌지 플랩이 반대편 흰 배를 덮음**(완료 시 소멸) / 경계 검정선 제거 / 경계 곡선 = 실측 보간. + 작업 패널 4작업 이상 2×2 그리드(도마 침범 해소).
- 검증: 실측 구간 cov 1.00·35스테이지 완주·lift 3회·실렌더 4단계. **잔여: upLift/dnScore/dnLift/내장/엔가와 F9 재실측(사용자 재진입 예정 — 새 렌더 기준)**.

### ✅ 이전 완료 (77차, 2026-08-05) — 레포 파일 정리 + 프로젝트 스킬 10종
- `.claude/skills/` 10종 도입(작업별 절차·함정 노하우 추출 — 목록은 CLAUDE.md) + AGENTS §4 중복 정책 스킬 포인터 축약.
- 루트 잡파일 정리: 폐기 6건 삭제(sashimi_guide_fix/board_dnd svg·상태 덱·chum_guide_popup·webglmap 중복 2) / `docs/reference/`(API 가이드·수심 zip·경락 CSV) · `docs/mockups/` · `assets/{branding,characters,guide}/`로 git mv(구 packages/man·girl 포함). 도구 경로 2건 갱신(ZIP_PATH·SVG_PATH) — 생성기 재실행 diff 0·빌드 4/4.

### ✅ 이전 완료 (76차, 2026-08-05) — 팝업 z-order 포커스 시스템 + ESC 정합 + 광어 온마리 신규 에셋
- **클릭 = 최상단**: `DraggablePanel.bringSelfToTop()` 재작성 — 구 `children.bringToTop()`은 depth가 다른 패널 간 완전 무력(사문)이던 것을 **depth 기반 동적 최상단**으로. 밴드 규칙(일반 [800,899) 자유 전환 / 모달 [900~) — 일반은 모달 침범 금지·포화 시 재정규화) + **씬 레벨 pointerdown 포커스 캡처**(자식 요소 클릭도 raise·겹침은 최상단만·모달 dim 뒤는 차단) → 인벤 우클릭 메뉴가 장비창에 가려지던 사용자 리포트 해소(메뉴 = 패널 자식이라 자동 해결).
- **ESC = 시각적 최상단부터**: `closeTopPopup`이 스택 LIFO 대신 depth 최고 팝업을 닫음(동률 = 기존과 동일).
- **광어 온마리**: 사용자 교체 halibut.png(고품질) → public 교체 + `gen_flatfish_sprites.cjs` 재생성(도마 스프라이트는 구운 스냅샷이라 재생성 필수 — fish_halibut 텍스처는 자동 반영). dev 테스트 어획에 광어(40~80cm) 추가.
- 검증: 실마우스 5시나리오(초기 서열→클릭 전환→우클릭 메뉴 최상단 렌더→상호 전환→모달 가드) + 온마리 2면 렌더. 빌드 4/4·typecheck 0. ⚠ FP 씬 ESC 하드코딩 순서는 스코프 제외.

### ✅ 이전 완료 (75차, 2026-08-05) — 광어 회썰기·도마 필렛 실사 에셋 마무리 (bream 폴백 해소)
- **에셋 2종 활용** (사용자 지정): 순수 필렛 = `skinned_pillet_without_engawa_halibut.png`(skinned 네이밍이지만 순살과 공용 — 껍질 분리 연출이 돔류·방어류와 동일하기 때문) / 엔가와 = `engawa_halibut.png`(74차 기배선 유지).
- **회썰기 3뷰** — `gen_sashimi_fillet.cjs`에 halibut 계열 추가(`flipX/flipTop: true` — 원본 머리 왼쪽 → **머리 오른쪽 규칙** 정렬) → `fillet_top/side_halibut.png` + `piece_halibut.png` + SashimiFilletProfiles `SashimiFilletFamily` 3군 확장. BootScene 3텍스처.
- **어종군 판정 일원화** — `PixelButcherFish.butcherFamilyOf(speciesId)` 신설(amberjack/halibut/bream) — SashimiPanel(구 로컬 AMBERJACK 셋)·UtilizationPanel 조각 스테이징이 공유. 광어 필렛 → fam 'halibut' → 전용 뷰/조각 아이콘 자동.
- **도마 실사 슬랩** — `pixelize_butchery.cjs`에 `pure_fillet_halibut` 투입 + **MIRROR_KEYS 신설**(도마 필렛 방향 규칙 = **꼬리 왼쪽·머리 오른쪽** — 박피 peel_grip 꼬리 칼집·회썰기 컷 순서와 동일 컨벤션. 원본이 머리 왼쪽인 에셋은 굽는 시점 반전). drawFlatFish FLESH_UP 슬랩·drawPeelTop(skin_fillet 폴백)이 실사 픽업 — 구 bream 폴백 해소.
- **도구 이식성** — `gen_sashimi_fillet.cjs` playwright 경로가 특정 사용자 계정 하드코딩이던 것 → 로컬 설치 → npx 캐시 자동 탐색.
- 검증: 실렌더 4스크린샷(회썰기 탑뷰 14컷·측면 16컷 — fam/texKey/fr 정합 / 도마 엔가와 슬랩·박피 당김 뷰 — 미러 후 꼬리 왼쪽·껍질 바 정합) + pageerror 0. 빌드 4/4·typecheck 0.

### ✅ 이전 완료 (74차, 2026-08-05) — 넙치류 다섯장뜨기 손질 (필렛 4장 + 중골 = 5장)
- **core**: `buildFlatStages` 27스테이지(머리 **S자 절단**·개복 없는 내장 긁기·배쪽 먼저 위/아래 포 뜨기·등쪽 동일·엔가와 ×4·박피 id 공유) + `FLAT_FISH_SECTIONS` 7섹션(`sectionsForBodyShape`) + yield 3종(flatFillet/engawaSkin/flatSkinFillet) + 게이트 해제(광어·도다리 3종 finfish) + filletCount 4 고정(구 ≥45cm 5필렛 분기 폐기 — 5장 = 4필렛+중골).
- **client**: `PixelFishFlat.ts`(신규 `gen_flatfish_sprites.cjs` — 신 halibut.png 등면 추출 + 배면 크림 파생) + `drawFlatFish`(탑뷰·S컷 erase·면별 포뜨기 진행 오버레이·내장 노출) + ButcheryPanel flat 배선(섹션 선택·orientLabel·checkpoint·대체 지급 회수·showResult 필렛4+엔가와4) + 재장착 3체인(engw→엔가와 / skin→박피 / engwskin→**순수 엔가와**) + halibut trimmings 9키.
- **박피 연출**(전 어종): peel_pull 게이지 완료 시 선 따라 칼 스윕 제거 (사용자 지시 — 톱질 연출과 중복).
- **후속 — 엔가와 회썰기**: 순수 필렛 아이콘 = skinned_pillet_without_engawa 확정 + **순수 엔가와 회썰기 = 총 2컷 → 3조각**(core ENGAWA_CUTS/PIECES · SashimiPanel 엔가와 분기 — 실사 trim_engawa 스트립·탑뷰·3등분 유도선·조각 = 접시 재료 인정). 검증: 39g → 13g×3 특 PASS.
- 검증: core 정합·FSM 완주·실 스토어 완주(2.2kg → 특 필렛 251g×4 + 엔가와 39g×4 + 부산물 4종)·재장착 3체인·실렌더 스크린샷 — 전 PASS. 빌드 4/4·typecheck 0.

> ⚠ **방어류/돔류 손질은 마감** (사용자 판단 2026-08-05): 기존 단계 스프라이트가 **원래 방어 실사 기반**이라
> 방어류 잔여 2건(실사 4장 투입·후속 좌표 재실측)은 그대로 둔다. 어종별로 실제 다른 것은 **밑손질(체형)뿐**이고
> 70차 어종군 분리로 해결됨. 손본다면 오히려 돔류 쪽.

### ✅ 이전 완료 (73차, 2026-08-05) — 외부 QA 버그 수정 2건 (접시 회전 렌더 + 씬 전환 검은 화면 전수조사)
- **접시 돌리기 실버그** — `drawPlate` 조각 배치 수식이 하단 방위(좌하/우하)를 상단 위치에 겹쳐 그려 3번째 회전부터 "안 돌아가는" 것처럼 보임 → 방위별 호 시작각 테이블로 재작성. + 가득 찬 방위 회전 시 토스트·접시 아래 상시 안내, 녹색 호/'배치 방위' 라벨 삭제 (사용자 지시).
- **씬 전환 전수조사** — ① `WorldMapScene.isTransitioning` 재진입 미리셋(출조 1회 후 '집으로 돌아가기'·출조 먹통 + 요금 이중 차감) → create() 리셋 + 요금 차감 전 가드 ② '집으로 가기' 확인 팝업이 일시정지 메뉴 뒤에 가려짐 → 메뉴 먼저 닫기 + depth 1200 ③ **신규 `scenes/SceneFade.ts`** 공용 안전망(폴백 타이머 + WeakSet 이중 실행 가드)을 bare `camerafadeoutcomplete` 대기 17곳(14파일 — FP 낚시 종료·실내·레거시 Field 포함)에 일괄 적용.
- 검증: 접시 4방위 [4,4,4,4] 실렌더 / 출조 연타 요금 1회분 / 월드맵 2회차 goHome 정상 / 실내 60ms ESC·2연타 복귀 — 전 PASS.
- ⚠ **QA 테스트 URL은 69차 배포본** — 도감 잘림·캐스팅 힌트 리포트는 71·72차 기수정. **gh-pages 재배포 필요** (로컬 worktree를 origin/gh-pages로 먼저 동기화).

### ✅ 이전 완료 (72차, 2026-08-05) — 장비 시스템 개편 (착용품 인벤 이탈 + 인체 배치형 장비창 + 캐스팅 게이트)
- **캐스팅 힌트 게이트** — 물가 판정보다 **장비 게이팅 우선**: 낚싯대 미착용 = 무반응(퀵슬롯에 낚싯대면 착용 안내만) / 착용 + 뭍 = "바다 가까이" / 착용 + 물가 = 차지. **퀵슬롯 선택 요건 폐지**(캐스팅 = 손 착용 단일 기준). 사용자 캡처 버그(빈 퀵슬롯 4 + 미착용인데 힌트) 해소.
- **착용품 그리드 이탈** — `SLOT_EQUIPPED(-1)` + 전 착용 API `EquipResult{ok, reason}` 전환. 해제 시 빈 소켓 필요("아이템 창 공간이 부족합니다."), 시드/freeSlotCount/**세이브 마이그레이션**(구세이브 착용품 소켓 자동 반납)/상점 판매 목록 제외.
- **EquipmentPanel 재작성(382×674)** — 사용자 도식: 중앙 캐릭터 렌더 + 상단 머리 4칸 / 좌·우 팔 6칸(손 좌/우 포함) / 하단 다리 좌·우 3칸(pair 장비 양쪽 표시). **우클릭 해제 · 호버 툴팁 · 인벤토리 드래그 장착**(`inventory-drop` 씬 이벤트 — 슬롯 히트/자동 부위 배정). 예약 슬롯 6종(마스크·넥워머·어깨·팔토시·반지·양말)은 흐린 실루엣.
- **검증(실마우스)** — 게이트 5케이스·우클릭 해제·25칸 만석 사유 안내·실드래그 장착 전부 PASS. **`__GS` dev 전역 신설**(하네스 GameState도 별개 인스턴스 함정 실측 확정 — 이후 하네스는 `__GS` 필수).

### ✅ 이전 완료 (71차, 2026-08-05) — 도마 도움말 팝업 + 도감 전면 정비 + 메인메뉴 전환 안전망
- **도마 [?] 도움말** — 버튼을 도마 **바깥 우상단**으로 이동 + 팝업을 공통 `DraggablePanel`(520×268·**화면 중앙**·depth 900)로 교체: 헤더 드래그·✕ 닫기·재클릭 토글, **ESC LIFO(도움말→회썰기→손질)** 편입. 실렌더: 중앙 (380,226)·드래그 +60/+60·ESC 1회 = 도움말만 닫힘.
- **도감 하단 잘림(실버그)** — 어종 도감 탭이 **페이징 없이 전 종을 렌더**해 y≈2200까지 나가 잘렸다. `renderPageNav` 공용 추출 + **12종/페이지(3행×4열)·5페이지** + `switchTab` 페이지 리셋. 카드 개편: **어종 실사 이미지**(FISH_TEXTURE 폴백) + 이름/학명 clamp + 스탯 4행(최대어·누적·제철·kg당 횟값) + `발견 N / M종`.
- **조과 기록 탭** — 1페이지 5 → **7행**, 어종명/상세줄 clamp. **헤더 라벨↔버튼 겹침 수정**(필터 라벨 우측 102 vs 구 버튼 좌측 85 → 라벨 실측 폭 기반 배치 114 / 정렬 버튼은 우측 끝에서 역산).
- **메인메뉴 씬 전환 안전망** — 5개 전환이 전부 `camerafadeoutcomplete` 단독 대기라 **fadeIn 중 재진입 시 검정 화면 멈춤** 가능(46·52차 동일 패턴) → `fadeOutThen`(폴백 타이머 + 이중 실행 가드) + `onReturnFadeIn`. 재진입·연타 모두 정상 복귀 검증.

### ✅ 이전 완료 (70차, 2026-08-04) — 방어류 손질 좌표·제거 영역 + 타이틀 로고 배치
- **가이드 좌표 어종군 분리** — `GUIDE_COORDS{bream|amberjack}` 신설(시메/방혈/머리 2면/비늘 2면/지느러미 3선/꼬리 2면 = 9세트). 구 구조는 돔류 값을 전 어종에 하드코딩. 두 어종군 cov 1.00.
- **방어류 제거 영역** — 실측 지느러미 선이 몸통 윤곽을 따라가는 성질을 이용해 `FIN_ERASE.amberjack`(등·배, 꼬리 보존) + `HEAD_EXTRA_ERASE`(머리와 함께 떨어지는 목덜미·가슴지느러미, 아래로 테이퍼) 신설.
- **타이틀 로고** — `TITLE_LOGO` 튜닝 블록 + 좌측 90px 이동(메뉴 패널 겹침 해소) + 배킹 0.85배. 하한 ≈0.83.
- ⏳ 잔여: 방어류 **실사 단계 스프라이트 4장 투입 대기** · 배따기~박피 좌표는 아직 돔류 공용(재실측 필요).

### ✅ 이전 완료 (69차, 2026-08-04) — 사용자 피드백 8건
- 지아이/완료 뷰 = 실사 순수 필렛 · 2면 좌표 F9 반영 · 탑뷰 꼬리 = 왼쪽(컷은 머리부터) · **회썰기 완료 → 도마 조각 진열 → 접시 드래그**(스테이징) · 회 조각 아이콘 = 한 점 슬라이스 · 모듬회 신규 사진(256²) 적용 · 타이틀 로고 반투명 배킹.

### ✅ 이전 완료 (68차, 2026-08-04) — 단품 사시미 횟집 괴리율 보정식
- `판매가 = 원물 kg시세 × (회중량 ÷ 실수율) × 인분마진` — core `SINGLE_SASHIMI_PRICING`+`singleSashimiPlatePrice` (구 KG_FACTOR 폐기). 일반 실수율 0.35·마진 1.60~1.25 / 고급 0.28·1.85~1.45.
- 캘리브레이션: 참돔 단품 소 59,400(실측 목표 55k)·벵에돔 소 66,720(70k) 정합. 조각/필렛 = 원가 승계, 마진은 접시 완성 시 실현. 모듬 고정표 불변.

### ✅ 이전 완료 (67차, 2026-08-04) — 갈빗대 재장착 세션 필렛 소실 수정
- **원인**: 재장착 세션의 필렛 = **원물(source)**인데 모프가 grantedLog만 순회 → 대상 0건 → 정산이 원물 소모 → 필렛 증발.
- **수정**: 모프 대체 지급(`inv_filletpin_` — 가치 승계) + sec_pin 재장착 매핑 + 연속 진행 시 대체 회수 + 재장착 순살 필렛 **원물 실중량/가치 반분할**(880g→431g×2, 구 97g 결함). 통짜 세션 회귀 없음. 검증 4항목 PASS.

### ✅ 이전 완료 (66차, 2026-08-04) — 사시미 접시 플레이팅 + 가격 개편 + 요리 탭 재배치
- **가격**: 필렛 합가 = **원물 판매가 분할** / 회썰기 산출 = **회 조각**(스택) / 완성 = 접시에서 — **모듬**(2종+) 고정표(35k/55k/70k/110k + g 하한, 고급 40k/60k/80k/125k) / **단품** = 원물 kg 시세 × 계수(1.0~2.75kg). 완성 접시는 0.6 매입 할인 없이 책정가 판매.
- **요리 탭**: 안내문 → [?] 도움말 / 도마 아래 **[사시미 만들기(접시 필요)]·[불요리(미구현 비활성)]** 2버튼 + 확장 연출(우측 축소 스트립).
- **플레이팅**: 접시 4종(시드 소1+식자재마트) 드래그 장착 → 크기별 접시 렌더 → **4방위·접시 돌리기·방위당 소4/중5/대6/특대7점** 조각 배치 → [사시미 완성](전 방위 만석·모듬 g 하한) → `inv_sashimi_plate_*`. 빼기/파괴 = 접시+조각 반환.
- **상세보기 겹침 수정**(긴 이름 — 제목 wrap 172 + hShift 흐름 배치). 검증 7항목 PASS.
- 잔여: 불요리 실구현 · 스시(고급 조각/접시 '요리하기' 스텁) · 배치 상태 세이브 미포함(세션) · 도마 인라인 썰기 통합 검토.

### ✅ 이전 완료 (65차, 2026-08-03) — 회썰기(사시미) 미니게임 (사용자 요청 7번 완결)
- **필렛 스프라이트 2뷰** = `tools/gen_sashimi_fillet.cjs` — **일반 = 탑뷰**(원본 사진 직접 다운샘플 — 리매핑 없음) / **고급 = 완만한 슬랩 측면 뷰**(원본 컬럼 리매핑, 사용자 스케치 실루엣) → `public/sashimi/fillet_{top,side}_{fam}.png` + `data/SashimiFilletProfiles.ts`(뷰별 윤곽 프로필). (사용자 정정 반영 — 일반 = y plane 위에서 본 뷰.)
- **core `SashimiSlicing.ts`** — 일반 14컷 세로 / 고급 16컷 사선(야나기바 게이트), 크기 소<200/중<400/대<700/특대(g), 등급 특/상/중/하, `buildSashimiCutPaths`(실루엣 자동 배치 — F9 불필요).
- **`SashimiPanel`** — 순수 필렛 도마 드롭 → [일반 회뜨기]/[고급 사시미 뜨기](야나기바 시만 활성) → 머리쪽(우)부터 유도선 드래그 → 조각 팬아웃 → `{어종} {고급 }사시미 ({크기}·{등급})` 지급(고급 = `inv_sashimi_adv_` — 우클릭 **'요리하기'** 스시 스텁). 중단 = 필렛 보존.
- 검증: 일반/고급 완주·실패 케이스·산출가·스프라이트 전 PASS (실 스토어 __INV + 실렌더 스크린샷).
- 잔여: 스시 실구현·회 접시 플레이팅/판매·사시미 전용 아이콘. **실플레이 확인 권장**: 도마 실마우스 드래그·버튼 게이팅.

### ✅ 이전 완료 (64차, 2026-08-03) — 브랜딩 적용
- **메인 메뉴 타이틀 로고** = 구 텍스트 2단 로고 → `public/ui/title_logo.png`(투명 PNG 991×359) 이미지(`BootScene` load + `MainMenuScene.drawTitle` add.image 중앙·폭 500).
- **exe/윈도우 아이콘** = `icon_rectangle.png`(930² 정사각)에서 `tauri icon` 전 아이콘 재생성(`apps/tauri-wrapper/src-tauri/icons/`) + 브라우저 파비콘(`index.html`). exe 실반영은 `tauri build`(Phase 9).
- ⚠ `Image`엔 `setShadow` 없음(Text 전용). 아이콘 재생성 = `tauri icon "<정사각>.png" -o …/icons` 후 딸린 `android/ios` 폴더 제거.

### ✅ 이전 완료 (63차, 2026-08-03)
- **부산물 = 팝업 확인 즉시 지급** (레저 정산 폐지 — 사용자 제안 채택). 체크포인트 전 이탈은 `grantedLog` 회수로 원물 복구 규칙 유지. 갈빗대 morph/지아이 분할/최종 완료 시 중간 필렛 회수로 중복 방지.
- **팝업 [보관/버리기] 함정 UI 교체** — 단일 상태 토글(누르면 뒤집힘)이 정상 플레이에서 부산물을 버리게 만들던 원인 → **선택형 2버튼**.
- 검증: 머리 팝업 확인 → 즉시 지급 / 전 이탈 → 회수+원물 보존 / 2면 종료 → 5종 잔존 / dev 경로 유지.

### ✅ 이전 완료 (62차, 2026-08-03)
- **부산물 미지급 잔여 원인** — dev [섹션 건너뛰기]/[개별 작업] 점프가 부산물 팝업을 건너뛰어 **레저 적립을 안 함**(원물만 소모, 부산물 0 — 사용자가 본 증상). `accrueYields()`로 dev 항법도 자동 [보관] 적립 + `grantPending` dev 콘솔 진단.
  검증: dev 건너뛰기 ×8 → 이탈 → 5종 지급(머리·내장·필렛×2·척추뼈) ✅. 게임 그래프 = `.ts` URL 확인(`reassigned:false`) — 61차까지의 스토어 검증도 유효했음.

### ✅ 이전 완료 (61차, 2026-07-31)
- **부산물 소실 최종 원인** — 씬 ESC → UtilizationPanel.destroy → `butcheryPanel.destroy()` **직행**(requestClose 우회).
  → ① **destroy 지급 안전망**(체크포인트 이후면 어떤 파괴 경로에서도 지급) ② **ESC 인터셉트**(`onEscIntercept` — 손질부터 LIFO로 닫기) ③ 팝업 [보관] 흡수 공용화.
  검증: destroy 직접 호출(구 ESC 경로) → 5종 지급 + 원물 소모 ✅ (실 스토어 __INV 기준, 세 이탈 경로 전부).

### ✅ 이전 완료 (60차, 2026-07-31)
- **부산물 미지급 핵심 원인** — 체크포인트가 `advanceSection`에서야 설정돼 **부산물 팝업 단계에서 ESC = 레저 전량 폐기**.
  → `exitAfter` 섹션은 **완료 즉시 체크포인트 확정** + `requestClose`가 팝업 [보관]분을 적립 후 정산. (검증: ESC → `checkpoint 'fillets'`·정산 도달)
- **2면 연출 플래시** — 연결부 끊기 단계 벌어짐 3 고정 + 2면은 앞 단계 누적 계산 + 배쪽 판정 `'belly'` 포함.
- **갈빗대 필렛 재장착** — `inv_filletribs_*` 도마 드롭 → `sec_rib`부터 재개, 1장 세션(부산물 수량 축소).
- **검증 하네스 함정 해소** — dev에서 `globalThis.__INV`로 게임 실싱글턴 노출(`import('/src/…')`는 별개 인스턴스).
  실 스토어 기준 검증 ✅: 2면 뜨기 종료(버튼/ESC 모두) → 머리·내장·껍질갈빗대 필렛 ×2·척추뼈 지급 / 갈빗대 재장착 → 갈빗대 ×1.

### ✅ 이전 완료 (59차, 2026-07-31)
- **부산물 소실 버그 수정** — `onStageComplete`의 섹션 desync 자가복구(구: 현재 섹션에 없는 스테이지는 조용히 무시 → 이후 팝업·정산 전부 소실) + `pendingAfterAction` 체이닝 + `addItem` 무음 실패를 결과 화면에 경고.
  검증: 완주 시 부산물 팝업 **7종 전부 발생**(수정 전 4종). ⚠ 인벤 실반영은 하네스 모듈 인스턴스 분리로 확인 불가 — **실플레이 확인 필요**.
- **방어류 껍질 필렛 에셋**(`skinned_pillet_amberjack`) — 아이콘 어종군 분리 + 박피 도마 뷰 픽셀화.

### ✅ 이전 완료 (58차, 2026-07-31)
- **2면 배쪽을 등쪽과 대칭 구조로** — `fillet_1_sever`(2회) + **`fillet_1_bellyribcut` 신설**, 들림 방향 **꼬리쪽부터**로 반전(`liftFrom:'tail'`), `{fam}_bellyspineribs` 신규 뷰.
- **전환 플래시 수정** — `spineOpen`에 ribcut 합산(완료 직후 덜 벌어진 그림으로 되돌아가지 않음).
- **belly 뷰 판정 버그** — id 'sever'만 보던 것을 'belly'도 인정(온마리 측면 폴백 해소).
- **갈빗대 좌표 실측 + 부산물 2개** · **껍질 붙은 필렛 에셋**(`skinned_pillet_bream`) 아이콘·도마 적용 · **박피 톱질 애니**(드래그 중 칼 상하 왕복).

### ~~🚧 다음 착수 — 회썰기(사시미) 미니게임~~ ✅ **완료 (65차, 2026-08-03 — 위 "직전 완료" 참고)**

당시 상태: 순수 필렛의 **아이템창 → 도마 드래그 자체가 막혀 있었음**(도마 수용은 어획물·두족류·껍질필렛만).

- **z plane 정면뷰(기본)**: 총 **14개 컷 위치** → 사시미(소) — 중량에 따라 소/중/대/특대.
- **y plane 정면뷰(고급)**: **고급 사시미칼(야나기바) 이상 장착 시** 사선 썰기, **16개 컷** → 고급 사시미(소).
- 고급 사시미는 추후 **'스시' 만들기** 연계 산출물 — 아이템 우클릭에 '먹기' 외 **'요리하기'** 추가 필요.
- 참고: 손질하기(원물 손질)와 **별도 과정**. 측면 썰기 뷰 에셋 필요.

**✔ 확정 결정 (사용자, 2026-08-03)**: ① 썰기 뷰 = **파라메트릭 먼저**(나중에 실사 pixelize 교체)
② 컷 판정 = **유도선 드래그**(기존 `guided_cut`·`submitCut` 재사용).

**구현 순서 (A~E — 전부 65차에 구현됨)**:

- **A. 순수 필렛 도마 수용** — `UtilizationPanel` draggable·drop 술어에 `inv_fillet_` 추가 →
  도마에 올라오면 [손질 시작] 대신 **[회썰기]** 버튼. `this.sashimiPanel` 필드·destroy·`onEscIntercept` 정리.
- **B. SashimiPanel 신설** — `ButcheryPanel` 모델(DraggablePanel·씬 포인터 판정·`guided_cut` 스트로크).
  z-plane 14 세로 / y-plane 16 사선 유도선(근사 기본 좌표 → 사용자 F9 실측). 파라메트릭 측면 썰기 뷰.
- **C. 야나기바 게이트** — `getBestKnife(손 장착 회칼).tier === 'yanagiba'`일 때만 y-plane(고급) 활성.
- **D. 사시미 산출** — `inv_sashimi_{speciesId}_{seq}`(회(사시미)), 소/중/대/특대 = 필렛 `weightG` 임계,
  등급 = 컷 정확도. `sashimiValuePerKg` 가격식 재사용. 순수 필렛 소모.
- **E. '요리하기' 메뉴** — 순수 필렛/고급 사시미 대상 '요리하기' 추가(스시 연계 — 스텁/안내).
- 검증: 빌드 4/4·typecheck 0 + Playwright 실렌더(드롭 → 회썰기 → 산출·야나기바 게이트 on/off).

### ✅ 이전 완료 (57차, 2026-07-31)
- **박피 2단계 재설계** — ①꼬리 손잡이 칼집 ②껍질 분리(측면 뷰 경계 칼 → 탑뷰 껍질 클릭 후 좌측 지그재그 4~5초).
  뷰 3종: `pure_fillet_{fam}`(실사 픽셀화)·`{fam}_peelcross`(측면 단면)·`drawPeelTop`(껍질이 왼쪽으로 늘어나고 살이 밀림).
- **부산물 재정의** — 지아이 분리 = 지아이뼈 2 + **껍질 붙은 필렛 4장**(exitAfter) / 박피 = 껍질 1 + 순수 필렛 1.
- **필렛 재장착** (백로그 ⓑ 완료) — 껍질 붙은 필렛을 도마에 드롭 → `resumeSectionId: 'sec_peel'`로 박피부터 재개.
- **후속** — 박피 좌표 실측 반영(`peel_grip`·`peel_insert`) · 꼬리 손잡이 단계 회색 바 제거 ·
  당기기 연출 재구성(**z순서 도마→껍질→칼→살코기**, 칼은 x고정·y 위아래, 껍질과 살코기가 함께 왼쪽 이동) ·
  **상세보기 확대 이미지를 픽셀아트 보유 전 아이템으로 일반화**(부산물·필렛 포함).
- ⏳ **사용자 예정**: `peel_pull` 지그재그 좌표 F9 실측 · 칼 실사 에셋 · 박피 추가 연출 구상.

### ✅ 이전 완료 (56차, 2026-07-31)
- **가이드 좌표 실측 반영 6구간**(지느러미 3선·비늘 앞/뒤·배따기·내장·2면 1회차) — 전 구간 판정 cov 1.00.
- **2면 등쪽 3단계 재구성** — `fillet_1_score`(1회차) → **`fillet_1_score2`(신설)** → `fillet_1_ribcut`(갈비뼈·척추 연결부).
  **커버율 부족 원인 = 같은 선 3회 요구 구조**(벌어진 뒤엔 살과 선이 어긋남) → 회차별 실측 경로를 스테이지로 분리해 해소.
  `spineOpen` 신설로 스테이지가 갈려도 벌어짐이 누적 유지.
- **신규 뷰 3종** — `{fam}_spineribs`(갈비뼈 노출) · `{fam}_bellyspine0~3`(2면 전용 배쪽) · `fillet_ribs`(갈빗대 실사 픽셀화).
- **박피 회색 바·갈색 블럭 제거.**
- ⏳ **사용자 예정**: 교체된 이미지 기준 유도 좌표 F9 재실측(갈빗대·3단계·2면 배쪽) + 박피 추가 작업 구상.

### ✅ 이전 완료 (55차, 2026-07-31)
- **밑손질 유도선 틀어짐 수정** — 스프라이트 교체 시 생선이 확대·축소·이동하던 것을(온마리 384px ↔ 머리제거 192px ↔ 지느러미제거 472px) **온마리 원형 틀 고정 + 부위 영역 삭제**로 교체.
  머리는 **실제 절단선(guidePath)을 따라** 삭제되므로 F9로 선을 옮기면 잘린 모양도 따라온다. **적용 = 개복 전 측면 뷰만**(뒷 단계는 기존 동작 유지 — 사용자 수동 점검 예정).
  기존 측정 좌표(지느러미 3선·비늘 스윕)는 **재측정 불필요**(전부 프레임 안 — oob 0).
- **dev 항법** — 좌측 하단 [dev: 섹션 건너뛰기] / [dev: 개별 작업(확장)](11섹션 19작업 전체, 클릭 점프 시 앞 단계 전부 완료 처리).

### 🔧 현재 진행 중 (사용자 수동 검증 — 코드는 대기)
- **가이드 좌표 실측 튜닝** — dev **F9 곡선 편집기**로 유도선 좌표를 눈으로 맞추는 중.
  - 대상: `fillet_1_score`·`fillet_1_ribcut`·`fillet_1_sever`(2면 새 구조) + `vessel_scrub`·`tail_grip_b`·`rib_a/b`·`pin_a/b`.
  - 현재 코드값은 **근사 기본값**. F9 **[복사]** 스니펫을 `ButcheryProcess.ts`에 붙여넣고 **core 리빌드**해야 반영됨.
    (⚠ [복사] 스니펫은 `strokesRequired`/`tolerance` 등 opts가 누락되니 **기존 opts는 항상 보존**.)
- **스프라이트 육안 확인** — 2면 뷰 배선(등쪽/갈비뼈끊기 = 측면 `{fam}_spine` / 배쪽 sever = 정면 `{fam}_belly`)이
  실플레이에서 맞는지 확인 → 어긋나면 **2면 전용 배쪽 뷰**를 따로 뜰지 결정(현재는 장뜨기 belly 뷰 재사용).

### ~~📋 다음 착수 작업 (코드 — 우선순위 순)~~ ✅ **전부 완료 — 최신 계획은 최상단 "🚧 다음 착수" 참고 (2026-08-04)**
1. ~~**UI 전수조사 고순위 4건 수정**~~ ✅ **완료 (54차, 2026-07-31)** — ShopPanel 윈도우드 그리드 스크롤(휠·스크롤바·`n–m / N행`) ·
   EquipmentPanel/LicensePanel 이름 상한(신규 `ui/TextFit.ts`) · ButcheryPanel 결과창 겹침(실측 11px → 여유 12px).
   실렌더 실측 검증 완료. **잔여**: LicensePanel `y > 300` 컷(10번째 면허부터 조용히 누락 — 현재 9개라 무증상) ·
   저순위 팝업 검수(백로그 ⓕ)는 계속.
2. **필렛 재장착 배선 + 껍질 있는 필렛 스프라이트** ← **다음 착수** — 인벤 '껍질·갈빗대 붙은 필렛'(`trim_fillet_ribs`)을
   도마에 재장착 → `ButcheryProcess.jumpTo`로 갈빗대 섹션부터 재개. (스키마 `ButcheryProgress`·`jumpTo`·아이템 발급 완비 — **도마 수용/progress 임베드/resume 배선만 남음**)
   + 갈빗대 제거 단계 스프라이트를 **껍질 있는 필렛**으로 (현재 FLESH_UP은 순수 필렛 폴백이라 "껍질 없는 살"로 보임).
3. **회 썰기(12단계) 미니게임 + 측면 뷰 에셋** — 순수 필렛 재장착 → 세로 9등분(사시미/야나기바) /
   대각 11등분(고급사시미), **소/중/대/특대 = 원물 크기 연동**. 측면 썰기 뷰 에셋 필요.

### 🗺️ 회뜨기 로드맵 (서브시스템 내부 진척도)
| 상태 | 항목 |
|---|---|
| ✅ | 자유 손질 FSM (섹션 순서 + 작업 자유선택·달성도, 부산물 팝업/레저, 체크포인트 종료) — 52차 |
| ✅ | 단계별 스프라이트 (복면/체강/장뜨기/spine·belly 뷰 + 실사 pixelize 파이프라인 훅) — 52차 후속 |
| ✅ | 연출 완료 후에만 스프라이트/작업/팝업 전환 (`pendingAfterAction` defer) — 53차 후속 1·2 |
| ✅ | 2면(fillet_1)만 새 구조 (score+ribcut+sever, 1면 유지) — 53차 후속 10 |
| ✅ | UI 고순위 4건 (상점 그리드 스크롤·이름 오버플로 2건·결과창 겹침) + `ui/TextFit.ts` — 54차 |
| 🔧 | 가이드 좌표 F9 실측 (**진행 중, 사용자**) / 스프라이트 실사 교체 (방어류 사진 대기) |
| ⬜ | 필렛 재장착(도마 resume) → 회 썰기 미니게임 → 회 접시 플레이팅/판매 → 숙성/활어수조 버프 |
| ⬜ | 두족류·복어·광어(flat) 전용 손질 트리 (현재 돔류+방어류만 finfish, 그 외 unsupported) |

### 🎯 회뜨기 완결 후 차기 대과제 (Phase 6 잔여 → 7~9)
- **퀘스트/스토리 라인** — `QuestDatabase`는 있으나 **실플레이 연결·퀘스트 저널 UX·보상 흐름** 확장 필요 (차기 핵심).
- **CookScene 실조리** (현재 U 도마 손질만 — 레시피 조리 플로우) · **경영** (식당 납품·직판 판매·경매 시세).
- **CraftScene(U 제작대)** · **해루질/통발 심화** · **홈타운 확장** (하우스 Tier·농사·수조·벌목/채굴).
- **Phase 7 CraftScene · Phase 8 서버/멀티 · Phase 9 Tauri v2/Steam 패키징.**

---

## ▶ 다음 작업 큐 (2026-07-20 등록 — 여기서부터 이어서 작업할 것)

**진행 현황**

- **Q1 ✅ 완료**(9차) · **Q3 ✅ 완료**(2차 — 해저 지형 프로필 연동 + 뒷줄견제 홀드 재정의 + 가이드 UX). Q4는 Q3 프로필 구조로 대비 완료.
- 3차: 탑다운 수심 타일 + 낮/밤 조명 + 날씨.
- 4차: 1인칭 하늘/수중 실시간 반영(시간대×날씨 매트릭스 + 비/눈/안개 파티클) +
  메인 메뉴 좌하단 API 연동 상태 패널(`ExternalDataStore.getApiStatusList` — 실데이터/Mock/수집 중 점 표시).
- 남은 항목: **Q2 🔶 부분 완료**(23차 — 시각 도해 2종·가이드 일시정지·가이드북 아이콘.
  잔여: 조류 존/침강/정렬도 상관 도해 확장) → **Q5**(신규 API 5종).

**차기 소과제 (2026-07-22 등록)**

1. 사용하기 아이템 효과 적용(HP/피로/버프)
2. 쿨러 어획 '손질하기' 메뉴 → ButcheryPanel 연결
3. 개볼락/창꼴뚜기(한치) DB 등록 + 이미지 매핑(에셋 선로드됨)
4. 회썰기(두께/각도)·두족류 손질 트리
5. 상점 '기타' 매입 신설 시 쿨러 판매 가드 재확인
6. 정식 사운드 에셋 도입(현 WebAudio 플레이스홀더 대체)

**회뜨기 자유 손질 잔여 (2026-07-30 등록 — 사용자 백로그, AGENTS 52차 잔여와 동일)**

- ⓐ **방어류 실사 스프라이트 투입** — 사용자 제공 4장(핏줄 뷰/등쪽 길내기 1~3)을
  `food assets/butchery/amberjack_vessel.png`·`amberjack_fillet1~3.png`로 저장 → `node tools/pixelize_butchery.cjs`
  (배쪽 사진도 제공 예정: `amberjack_belly_score1~3.png` 권장)
- ⓑ **필렛 재장착** — 인벤의 '껍질과 갈빗대가 붙어있는 필렛'을 도마에 올려 갈빗대 섹션부터 재개
  (ButcheryProgress 스키마·아이템 발급은 완비, 도마 수용/재개 배선만)
- ⓒ **회 썰기(12단계)** — 순수 필렛 도마 재장착 → 세로 9등분(일반 회칼)=사시미 / 대각 11등분(야나기바 이상)=고급사시미,
  소/중/대/특대 = 원물 크기 연동, 측면 뷰 에셋 별도
- ~~ⓓ dev 가이드선 곡선 편집~~ ✅ **완료(52차 후속 3)** — F9 편집기에 [곡선 그리기](자유 드래그 → Chaikin ×2 + 호길이
  리샘플 7점) · [+ 점](중점 삽입 세분화) · [스무딩] · 핸들 우클릭 삭제. core 실판정 검증(곡선 1.00 통과 / 직선 0.44 실패)
- ~~ⓔ 회뜨기 유도 연출 개정 — VENTRAL 뷰~~ 🔶 **부분 완료(후속 8)** — 복면(뱃살 정면) 뷰 + 체강 탑뷰 신설·
  배따기/내장/핏줄/세척 배선. 잔여: 영역 유도·홀드 게이지
- ⓕ **UI 전수조사**(텍스트 오버플로/스크롤 미비/HUD 겹침 — 전 팝업 별도 패스)
- ⓖ **신규 스테이지 가이드선 곡선 실측 튜닝**(vessel/tail_b/rib/pin — 직선 기본값을 곡선 편집기로 생선 실루엣에 맞춤) ·
  광어(flat) 섹션 트리 분리
- ⓗ **뷰 스프라이트 실사 교체** — 복면/체강 뷰는 현재 파라메트릭 생성분(`tools/gen_butchery_views.cjs`).
  실사 사진을 `food assets/butchery/{bream,amberjack}_ventral.png`·`_cavity.png`로 저장 후 파이프라인 실행 시 자동 우선 적용
- ⓘ **새 뷰 기준 유도 좌표 실측** — 배따기(gut_open 정중선)·내장(gut_scoop)·핏줄(vessel_scrub) 스윕을 F9 편집기로
  복면/체강 뷰에 맞춰 재측정([복사] 스니펫이 `sweepPath: [...]` 형식으로 출력됨)
- ⓙ **장뜨기 뷰 실사 교체 + 좌표 미세조정** — `{bream,amberjack}_dorsal0~3`·`_belly0~3`은 파라메트릭 생성분(실사 3장
  기준 근사). 사진을 같은 이름으로 넣어 파이프라인 실행하면 교체, 등쪽 3점·배쪽 5점(3구간 분리) 경로는 F9로 미세조정

**24차 잔여 튜닝 (FP 회수·밑밥 — dev 실관찰 후)**

- ⓐ RETRIEVE_ANCHOR_Y(0.70~0.75H)·세트 배율(1.8~2.2)·원줄 굵기·alpha 시작값
- ⓑ 투척점 수/간격(현 13개·1.5m)·sinkRate·spread·동조 시그마·동조→입질 배율
- ⓒ 밑밥 종류별 sinkRateMps 차등(현재 전 종 0.22 고정 — CoolerStore 배합 재료 연동)
- ⓓ 예측 고스트를 실제 stepChum 궤적과 대조하는 dev 오버레이

### ~~Q1~~. 입질 1·2단계 유도 메커니즘 (✅ 완료 2026-07-20 9차 — `BiteSequenceEngine.provoke()`)
- 1~2단계(또는 직후 공백) 중 릴링 1초 유지 or H 견제 → **70% 확률로 다음 단계 3단계 승격**(공백 0.6~1.6s).
  시퀀스당 1회 판정, 실패 시 원 패턴 지속. 성공 시 "입질 유도 성공" 안내. 2000회 시뮬 70.0% + 브라우저 실관측 검증.

### Q2. 인게임 낚시 가이드 고도화 (요소 간 상관관계 설명) — 🔶 부분 완료 (2026-07-22 23차)
> 완료분: 챔질 타이밍 **입질 타임라인 도해**(3단계 융기+골든존+펴짐=실패) · 파이팅 **텐션 게이지 구간도** ·
> 가이드 열람 중 낚시 일시정지 · 가이드북(?) 아이콘. "낚싯대 휨새 도해가 가장 이해된다" 피드백 기반.
> 잔여: 아래 후보 주제 중 조류 존/침강·면사매듭/정렬도/뒷줄견제 도해.
- 물리 수치 나열이 아니라 **"어떤 상황에서 어떻게 되는지"** 요소 간 상관 중심 설명.
  예: "던지는 위치마다 조류가 다르게 흐르며, 조류에 따라 찌·루어·미끼의 침강 속도와 채비 정렬 속도가 달라집니다."
- 예시 이미지(가능하면 짧은 애니메이션/동영상 프레임) 포함 — 기존 F1 가이드 4페이지를 확장.
- 후보 주제: 조류 존(조경지대/반탄류/본류) 유불리 · 침강과 면사매듭 · 정렬도와 입질 · 뒷줄견제 효과 ·
  물때와 활성도 · 지형(여밭/모래)과 밑걸림 · 채비 무게와 원투.

### ~~Q3~~. 우측 수심 모식도 — 동적 바닥 지형 (✅ 완료 2026-07-20 2차 — 아래 설계대로 구현)
- 착수 지점~유저 사이의 **거리별 바닥 지형이 연속적으로**(끊김 없이) 구성되어 릴링으로 채비가
  이동하면 모식도의 지형이 함께 흘러가야 함 (채비 위치 중심 거리 창 스크롤).
- 지형 요소: 암초 높낮이(단차 있는 암초 지대), 암초가 끝나며 모래 바닥이 시작되는 경계, 수초 군락,
  높낮이 있는 모래 바닥 (참고 스케치: 상층/중층/바닥층 구분 + 수초 낀 암초 실루엣).
- **지형 지도 연동 소스** (씬이 이미 보유한 지도 기반 데이터를 프로필 생성에 주입):
  ① 착수 타일 해시 시드(`reefSeed`) — 같은 포인트는 항상 같은 바닥 ② 실측 수심(`zMaxM`,
  연안정보도) — 프로필 원거리 기준 수심 ③ 발밑 지형(`shoreKind` — 모래/잔디/자갈) — 근거리
  기준 수심 ④ 낚시터 특성(`RegionAreaNode.snagRisk`) — 암초 비율(high 감천항 ≈ 암초 50%+,
  low 모래 위주).
- 구현: `core/simulation/SeabedProfile.ts` — 결정적 PRNG + 값 노이즈 기반 1D 프로필
  `generateSeabedProfile(seed, lengthM, shoreDepthM, landingDepthM, rockRatio)` →
  `{distM, bedDepthM, kind('rock'|'sand'), weed}` 샘플 배열 + `bedDepthAt`/`bedKindAt` 보간.
  암초/모래는 **연속 구간**(세그먼트)으로 전환되고 경계에서 바닥 높이는 이어진다.
- 게임 연동: 침강 바닥 한계 = `min(Z_limit, bedDepthAt(distM))` (릴링 중 채비가 암초 단차를
  타고 오름) / 여밭 판정 `isReefAt`(측면 X 해시) → **거리 기반 `bedKindAt`으로 대체** /
  수심 모식도 게이지 박스에 거리 창(±12m) 단면 실루엣 + 수초 렌더.
- Q4의 어탐 레이더(추후)가 같은 프로필을 조회하는 구조.

### Q4. 유저-투척 지점 주변 지형 기반 구조 (어탐 레이더 대비)
- **어탐 레이더 기능 자체는 지금 구현하지 않음** — 다만 "내가 선 지형에서 일정 거리 이상은 특정
  지형/수심"을 조회할 수 있는 데이터 구조(Q3의 지형 프로필 + 아래 API)를 지금부터 고려해 설계.

### Q5. 신규 해양 API 연동 (승인 완료 — 키는 data.go.kr 공용키 사용)
공용 인증키: `VITE_DATA_GO_KR_API_KEY` (4b17...13e7 — .env)

1) **해양수산부_어초정보** (인공어초 위치 — 여밭/구조물 포인트 소스)
   - Base: `https://api.odcloud.kr/api` / GET `/15127275/v1/uddi:0be633ef-11f9-434d-a93f-fe557d9081d5`
   - JSON+XML, odcloud 표준(serviceKey, page, perPage)
2) **해양수산부_해수유동 3분 서비스** (격자별 유향·유속 — TidalCurrentEngine 실데이터화)
   - End Point: `https://apis.data.go.kr/1192000/apVhdService_ContOc3` / `getOpnContOc3`
   - 파라미터: serviceKey, numOfRows, pageNo, analsYmd(분석일자), gridCd(격자번호 예: GR3_G1E23_P), analsHhs(분석시간), type
   - 참고문서: 루트 `격자3단계_격자번호.xlsx` (격자번호 매핑)
3) **KHOA 수치조류도 예측 유향·유속** (영역 검색 — 간격 자동조절 1~10km)
   - URL: `https://khoa.go.kr/oceandata/api/tidalCurrentArea/search.do`
   - 파라미터: ServiceKey, Date(YYYYMMDD), Hour(00~23), Minute(00~59), MaxX/MinX/MaxY/MinY(경위도 영역), ResultType(json/xml)
   - 응답: result.data[] = { pre_lon, pre_lat, current_speed(cm/s), current_dir(deg) }, result.meta.obs_last_req_cnt(잔여 요청수)
   - ⚠️ KHOA 자체 키 필요 여부 확인 (샘플 키 형식이 data.go.kr과 다름)
4) **국립해양조사원_자연과학용 수심정보** (해양수치모델 수심 — 광역 수심 그리드)
   - End Point: `https://apis.data.go.kr/1192136/waterDepth` / `GetWaterDepthApiService`
   - 파라미터: serviceKey, type(json/xml), ymin/ymax/xmin/xmax(위경도 박스), pageNo, numOfRows(최대 300), include/exclude
   - 참고문서: 루트 `오픈API 활용가이드_자연과학용 수심정보.hwp`
5) **국립해양조사원_조석예보(고·저조)** (일별 만조/간조 조위·시각 — TideCalculator 실데이터 보정)
   - End Point: `https://apis.data.go.kr/1192136/tideFcstHghLw` / `GetTideFcstHghLwApiService`
   - 파라미터: serviceKey, pageNo, numOfRows(최대 300), type, obsCode(예보지점 코드 예: DT_0018), reqDate(yyyyMMdd), include/exclude
   - 참고문서: 루트 `오픈API 활용가이드_조석예보(고, 저조).hwp`

연동 방침: 기존 `ExternalApiService` 패턴(클라이언트 클래스 + Mock 폴백 + 스타트업 캐시) 준수.
활용 매핑 — 어초정보→여밭/구조물 스폰 포인트(Q3 프로필 시드), 해수유동/수치조류도→`TidalCurrentEngine`
V_tide 실데이터, 자연과학용 수심→`resolveCastDepth` 광역 폴백(연안정보도 프로필 없는 지역),
조석예보→`TideCalculator` 고·저조 시각 보정.

---

## 전체 구현 단계 현황

```
Phase 1: Core 엔진 & 타입 정의            ✅ 완료
Phase 2: DB 스키마 & 데이터               ✅ 완료
Phase 3: Phaser 씬 기본 구조              ✅ 완료
Phase 4: GameState 연동 & 빌드 안정화     ✅ 완료
Phase 5: FieldScene UI 고도화             ✅ 완료
Phase 6: 게임플레이 심화 (낚시·퀘스트)    🚧 진행 중
Phase 7: CraftScene 구현                  ⬜ 대기
Phase 8: 서버 & 멀티플레이                ⬜ 대기
Phase 9: Tauri v2 통합 & Steam 패키징     ⬜ 대기
```

---

## ✅ 완료된 작업 전체 목록

### Phase 1–3: 기반 구조

| 항목 | 파일 |
|---|---|
| 물때 계산 엔진 | `core/src/simulation/TideCalculator.ts` |
| 낚시 입질 엔진 | `core/src/simulation/FishBiteEngine.ts` |
| 줄 물리 | `core/src/simulation/LinePhysics.ts` |
| 캐스팅 모델 | `core/src/simulation/CastingModel.ts` |
| 날씨 모델 | `core/src/simulation/WeatherModel.ts` |
| 해루질 엔진 | `core/src/simulation/NightHuntingEngine.ts` |
| 통발 시스템 | `core/src/simulation/TrapSystem.ts` |
| 어종 DB | `core/src/db-schema/FishDatabase.ts` |
| 장비 DB | `core/src/db-schema/GearSpecs.ts` |
| 스팟 DB | `core/src/db-schema/SpotDatabase.ts` |
| 미끼 DB | `core/src/db-schema/BaitDatabase.ts` |
| 해루질 생물 DB | `core/src/db-schema/ShoreCreatureDatabase.ts` |
| 통발 DB | `core/src/db-schema/TrapDatabase.ts` |
| 레시피 DB | `core/src/db-schema/RecipeDatabase.ts` |
| 퀘스트 DB | `core/src/db-schema/QuestDatabase.ts` |
| 어신앱 스팟 DB | `core/src/db-schema/AnglerAppSpots.ts` |
| 활동 타입 | `core/src/types/Activities.ts` |
| 라이선스 타입 + DB | `core/src/types/License.ts` |

### Phase 3: Phaser 씬

| 씬 | 파일 | 비고 |
|---|---|---|
| BootScene | `client-pc/src/scenes/BootScene.ts` | 완료 |
| MainMenuScene | `client-pc/src/scenes/MainMenuScene.ts` | 네온 타이틀 로고 강화 |
| WorldMapScene | `client-pc/src/scenes/WorldMapScene.ts` | 완료 |
| FieldScene | `client-pc/src/scenes/FieldScene.ts` | 탑다운 재설계 완료 |
| FishingScene | `client-pc/src/scenes/FishingScene.ts` | 완료 |
| TackleRoomScene | `client-pc/src/scenes/TackleRoomScene.ts` | 완료 |
| TideChartScene | `client-pc/src/scenes/TideChartScene.ts` | 완료 |
| AnglerLogScene | `client-pc/src/scenes/AnglerLogScene.ts` | GameState 연동 미완 |
| NightHuntingScene | `client-pc/src/scenes/NightHuntingScene.ts` | 완료 |
| TrapScene | `client-pc/src/scenes/TrapScene.ts` | 완료 |
| RestaurantScene | `client-pc/src/scenes/RestaurantScene.ts` | 완료 |
| CondoScene | `client-pc/src/scenes/CondoScene.ts` | 완료 |
| CookScene | `client-pc/src/scenes/CookScene.ts` | 완료 |

### Phase 4–5: GameState 연동 & UI 고도화

| 항목 | 결과 |
|---|---|
| GameState 확장 (stamina, fatigue, activeQuickslotIndex) | ✅ |
| TrapScene / CookScene / NightHuntingScene ↔ GameState 연동 | ✅ |
| LicensePanel ↔ GameState 연동 | ✅ |
| HUD: 좌상단 STATUS (Stamina/Fatigue/Coins) | ✅ |
| HUD: 중앙하단 퀵슬롯 8개 (단축키 1~8) | ✅ |
| HUD: 좌하단 커뮤니티 채팅 플레이스홀더 | ✅ |
| MiniMap: 우상단 직사각형 배치 + M키 3단계 크기 변환 | ✅ |
| InfoOverlayPanel: 인벤토리(I) / 퀘스트(Q) 팝업 | ✅ |
| ESC LIFO 팝업 스택 시스템 | ✅ |
| 마우스 클릭 자동 이동 | ✅ |
| WASD ↔ 방향키 완전 분리 (이동은 방향키 전용) | ✅ |
| U 키: 제작대(CraftScene) 단축키 등록 | ✅ |
| noUnusedLocals 전면 준수 (미사용 변수 정리) | ✅ |
| 단축키 팝업 창 (Inventory, Quest, Status, License) 드래그앤드롭 및 토글 해제 | ✅ |
| 팝업 창 우측 상단 ✕ 네모 닫기 단추 구현 및 화면비 절대좌표 뷰포트 배치 | ✅ |
| 팝업 본문 텍스트 GeometryMask 기반 마우스 휠 스크롤 지원 | ✅ |
| 낚싯대 캐스팅 시 파워 차지 게이지(스킬샷) 인터랙션 추가 및 낚시 씬 연동 | ✅ |
| 팝업·미니맵·HUD 활성화 시 클릭이동 차단 및 퀵슬롯 단축키(1~8) HUD 즉시 갱신 | ✅ |
| 울릉도 핀포인트 좌표 최적화 및 독도 노드(지도 동단 노란 체크위치) 신규 추가 | ✅ |
| 모든 서브 씬(Cook, Condo, Restaurant, NightHunting, Trap) ESC 단축키 & fadeOut 복귀 일관성 적용 | ✅ |

---

## 🚧 Phase 6: 게임플레이 심화 & 실데이터 연동

### 6-0. 실데이터 기반 기반 구조 및 시장 경제 (✅ 완료)

| 항목 | 결과 |
|---|---|
| `FishingSpotInfo` 타입에 `dotMapX/Y`, `tideStationCode`, `kmaGridX/Y`, `seasonalSpecies` 확장 | ✅ |
| `CoordinateUtils.ts`: 위경도 ↔ 도트맵/기상청 격자 변환 유틸 | ✅ |
| `FishBehaviorDatabase.ts`: 10종 고증 입질 프로파일 | ✅ |
| `Economy.ts` & `MarketPriceEvaluator.ts` : 수산물 경락 시세 및 kg단가/중량/등급 기반 가변 수매가 책정 | ✅ |
| `Item.ts` & `UniversalItemDatabase.ts` : 신선도/부패(live~fresh~spoiled) 및 쿨러 보관(chilled) 통합 아이템 시스템 | ✅ |
| `AuctionEngine.ts` : 선어(01~03시) 및 활어(03~07시) 시간대별 경매 개폐장, NPC 카운터 경쟁 입찰 | ✅ |
| `FieldScene.ts` : 기존 고정 수식 어판장 수매 로직을 고증 경락가 산정 엔진(`evaluateFishSellPrice`)으로 마이그레이션 | ✅ |
| `index.ts` export 추가 | ✅ |

### 6-0b. 포항 영일만 맵 & 조류 시각화 시스템 (✅ 완료, 2026-07-08)

| 항목 | 결과 |
|---|---|
| `YoilBayFieldMap.ts`: 실제 영일만 지형을 2048×1536 픽셀 격자로 추상화 | ✅ |
| 낚시 포인트 7곳 (북방파제 끝단/중단, 홈통, 남방파제 끝단/내항, 내만, 갯벌 통발) | ✅ |
| 지형 구역(Zone) 9개: 외해/북방파제/홈통/남방파제/내만/조간대/마을 | ✅ |
| 건물 7개 (민박, 낚시점, 면허사무소, 하나로마트, 횟집, 어판장, 화장실) | ✅ |
| 조류 타입별 포인트 속성 (MAIN/COUNTER/EDDY/CONVERGENCE/NONE) | ✅ |
| 수심 및 바닥 지형(reef/gravel/sand/mud) 속성 | ✅ |
| `HydroCurrentRenderer.ts`: Phaser Graphics 기반 조류/수심 픽셀 렌더러 | ✅ |
| 조류 화살표 (방향+길이로 속도 표현, 색상 코딩) | ✅ |
| 수심 색조 배경 레이어 (얕음→깊음 청색 그라데이션) | ✅ |
| 낚시 포인트 마커 (원형 + 이름 + 수심 텍스트) | ✅ |
| 조경지대(CONVERGENCE) 황금색 테두리 강조 | ✅ |
| `FieldScene.ts`: 조류 렌더러 통합 (V 키 토글, 30초 물때 연동 자동 갱신) | ✅ |

### 6-1. FishBiteEngine V2 — 고증 알고리즘 강화 (✅ 완료)

**파일**: `packages/core/src/simulation/FishBiteEngine.ts`

현재 단순 가중합산 구조를 아래 단계로 고도화:
- `getSeasonScore()`: 현재 월 기반 `seasonActivity` + 영등철/회유 시즌 보정
- `getTideTimingScore()`: 만조/간조 시각과 현재 시각 차이로 `highTideWindow` 내 여부 판단 (단순 tideStrength 배수 대비 훨씬 현실적)
- `getTempScoreV2()`: `tempActivityCurve` + `interpolateTempActivity()` 선형 보간
- `getRigBonus()`: 전유동/반유동/루어/바닥 채비별 어종 보너스
- `getHabitatScore()`: 스팟 타입 vs `preferredHabitat` 매칭
- `isClosedSeason()` 체크: 금어기 자동 경고

### 6-1b. 채집 시스템, 야간 볼락류 DB 확장, 드랙 파이팅 & 상층/보일링 그림자 연출 (✅ 완료)

| 항목 | 결과 |
|---|---|
| 위험/엣지 타일 감지 및 30% 슬립 판정 (`checkSlipHazard`) | ✅ |
| 낙수 패널티 처리 (체력 50% 차감, 피로도 50% 가산, 이전 안전 타일 강제 후퇴) | ✅ |
| 우측 채집 선택지 UI & 도구별 채집 연동 (`attemptGather` / 성게·뜰채, 거북손·칼, 갯강구·맨손) | ✅ |
| 볼락류 야간 어종 4종 추가 (`korean_rockfish` 우럭, `yellow_rockfish` 황볼락, `red_snapper_rockfish` 열기, `night_seabream` 야간참돔) | ✅ |
| 어종별 swimmingLayer('surface'/'mid'/'bottom') 및 isBoilingSpecies 필드 정의 및 특정화 | ✅ |
| 찌 흘림/어신 대기 상태 시 상층/보일링 타겟 어종 존재 시 동적 그림자(Shadows) 표현 | ✅ |
| 보일링 어종 존재 시 여러 마리의 군집 그림자가 빠르게 요동치며 지나가는 시각 연출 | ✅ |
| 실시간 드랙 조절 (`adjustDrag` / F/G 단축키 및 방향키 위아래 연동) 및 kg단위 변환 피드백 | ✅ |
| 파이팅 틱 시뮬레이션 (`simulateFightTick` / 릴링 시 장력 가산 및 드랙 속도 줄풀림/줄감기 물리 반영) | ✅ |
| 장력 90% 임계점 초과 시 릴링 락업(잠김) 처리 및 경고 피드백 | ✅ |

### 6-1c. GIS 맵 빌더 패키지 및 파이프라인 자동화 (✅ 완료)

| 항목 | 결과 |
|---|---|
| `packages/map-builder` 신규 패키지 및 tsconfig/package.json 모노레포 연동 | ✅ |
| `MapRegistry.ts`: 대한민국(512), 포항(1024), 임곡항(2048), 방파제(4096) 맵 계층 및 해상도 명세 정의 | ✅ |
| `TileExporter.ts`: 16px 정밀 타일 지형 분류(land, water, breakwater_edge, safe_zone 등) 및 콜리전 매핑 익스포터 구축 | ✅ |
| `download_tiles.py`: Bounding Box 및 Zoom 레벨별 VWorld/KHOA 타일 지도 이미지 다운로드 스크립트 | ✅ |
| `merge_tiles.py`: 조각 세그먼트 타일들을 단일 대형 위성지도로 자동 합성 및 결합 | ✅ |
| `preprocess.py`: 자질구레한 노이즈 억제를 위한 미디언 필터 및 16색 제한 대표색 전처리 단순화 필터 | ✅ |
| `pixelize.py`: PyTorch PixelOE 모델 및 PIL Nearest Neighbor 도트화 대체 Fallback | ✅ |
| `export.py`: 완성된 도트 맵 복사 및 색상 기반 자동 타일링과 충돌 격자 JSON 내보내기 | ✅ |
| `pipeline.py` & `tools/build_map.py`: 루트 및 빌더 내에서 GIS 픽셀화 맵 배포 통합 기동 래퍼 스크립트 | ✅ |

### 6-1d. 수온 고증 구조화 및 브랜드명 우회 (✅ 완료)

| 항목 | 결과 |
|---|---|
| `WaterTemperatureData` 인터페이스 구조화 (surface, mid, bottom 수층별 수온 및 coldWaterShockIndex, trend 정의) | ✅ |
| `WeatherData` 타입에 고도화된 수온 정보 연동 | ✅ |
| `GearSpecs.ts` 저작권 우회 브랜드명 개편 (다이와->다이오, 시마노->소마노, 선라인->솔라인, 요즈리->요즈미, 나이키->쯔리센, 메이저크래프트->마이너크래프트) | ✅ |
| `FishingScene.ts` 하드코딩 기본 채비 브랜드명 우회 변경 | ✅ |

### 6-2. DB 스키마 일관성 보완 및 리팩토링 (차기 예정)

현시점 골격화(Skeletalization) 평가 결과 도출된 데이터 모델 불일치를 해소하기 위한 단계:
- **인벤토리 모델 일원화**: `PlayerState` 내의 `ConsumableItem[]`과 `CoolerSlotItem[]` 구조를 `InventoryItemInstance[]` 형태로 완전 대체.
- **조과기록 메타데이터 통합**: `livewell`에 보관된 `CaughtFishRecord` 정보를 `InventoryItemInstance` 내의 가변 메타데이터(`lengthCm`, `weightGram` 등)로 탑재하여 아이템 인스턴스로 일원화.
- **채비(TackleSetup) 미끼 인스턴스 연동**: `BaitItem` 대신 인벤토리의 특정 `baitInstanceId`를 참조하게 하여 남은 수량 및 부패 상태(spoiled 미끼 사용 시 입질 패널티)가 실제 물리 엔진과 유기적으로 연동되도록 개선.
- **현실 오프라인 시간 보정 유틸**: 게임 세이브/로드 시 `lastSavedAt` 차이를 구하여 오프라인 경과 시간만큼 게임 내 분으로 환산 후 쿨러 외부 생선의 부패 상태를 로드 즉시 벌크 업데이트.

### 6-3. AnglerLogScene 실데이터 연동
**파일**: `packages/client-pc/src/scenes/AnglerLogScene.ts`
- `GameState.player.caughtFishHistory` 실데이터 표시
- 어종별 최대어 기록, 날짜/스팟별 필터 UI

### 6-4. 퀘스트 시스템 완성
- `QuestDatabase` 연동 → 활성 퀘스트 조건 체크 (어종 포획, 라이선스 취득 등)
- 퀘스트 달성 시 알림 팝업 + `GameState` 업데이트

### 6-5. WorldMapScene → 도트 월드맵 렌더링 전환 (✅ 완료, 2026-07-13)
**파일**: `packages/client-pc/src/scenes/WorldMapScene.ts`

| 항목 | 결과 |
|---|---|
| `FishingSpotNode` 인터페이스 및 `WORLD_NODE_DATABASE` (10개 사이트) | ✅ |
| `webglmap_pixel.png` 배경 이미지 배치 (수동 폴리곤 윤곽선 제거) | ✅ |
| `WORLD_NODE_DATABASE` 기반 동적 핀포인트 마커 렌더링 | ✅ |
| 리스트 hover ↔ 지도 마커 양방향 하이라이트 동기화 | ✅ |
| 클릭 시 카메라 줌인 애니메이션 (1.5x) 후 스팟 뷰 전환 | ✅ |
| 스팟 툴팁 (물때/수온/어종, 지역 노드 툴팁) | ✅ |
| 범례 (방파제/갯바위/선상/갯벌/해수욕장) | ✅ |
| 에셋 public 배포 (`client-pc/public/webglmap_pixelazed.png`) | ✅ |

### 6-5b. WorldMapScene 핀 편집 Dev Tool (✅ 완료, 2026-07-14)
**파일**: `packages/client-pc/src/scenes/WorldMapScene.ts`

| 항목 | 결과 |
|---|---|
| P 키 토글 핀 편집 모드 진입/종료 | ✅ |
| **개발자 도구 진입용 UI 버튼 추가** (`🛠️ Dev Tool (P)` 버튼 구현 및 상태 실시간 갱신) | ✅ |
| 핀 드래그 → 지도 pixelX/Y 좌표 실시간 표시 (라벨 + 하단 패널) | ✅ |
| dragend 시 클립보드 자동 복사 (`pixelX: N, pixelY: N, // nodeId`) | ✅ |
| 배너 우측: 선택 핀 ID 강조 표시 + 드래그 확정 좌표 업데이트 | ✅ |
| 지도 빈 공간 클릭 → 새 핀 좌표 캡처 + 클립보드 복사 (TS 코드 형태) | ✅ |
| 마우스 이동 시 현재 지도 픽셀 좌표 실시간 표시 | ✅ |
| 전체 덤프 버튼: `WORLD_NODE_DATABASE` 전체 좌표 콘솔/클립보드 출력 | ✅ |
| `_editSelectedId` TS6133 빌드 오류 수정 | ✅ |

### 6-1e. 캐릭터 스프라이트 교체 (✅ 완료, 2026-07-13)

| 항목 | 결과 |
|---|---|
| `packages/man/` 12개 PNG → `client-pc/public/characters/man/` 복사 | ✅ |
| `packages/girl/` 12개 PNG → `client-pc/public/characters/girl/` 복사 | ✅ |
| `BootScene.ts` preload()에 25개 이미지 로드 (남자 12 + 여자 12 + 지도 1) | ✅ |
| `FieldScene.ts`: `player: Graphics` → `playerSprite: Image` 교체 | ✅ |
| 4방향(front/back/left/right) × idle/move × 2프레임 걷기 애니메이션 (200ms 주기) | ✅ |
| 발 아래 반투명 그림자 타원 (`registry '_playerShadow'` 동기화) | ✅ |

### 6-5c. WorldMap 핀 재배치·여수 추가·지역 줌인 진입 (✅ 완료, 2026-07-14)
**파일**: `core/src/types/WorldMap.ts`, `core/src/db-schema/RegionDatabase.ts`, `client-pc/src/scenes/WorldMapScene.ts`, `client-pc/src/scenes/BootScene.ts`

| 항목 | 결과 |
|---|---|
| 11개 핀포인트 좌표 재배치 (사용자 지정 좌표) | ✅ |
| `FishingSpotNode.mapSlug` 필드 추가 (지역 상세 지도 파일 매핑) | ✅ |
| 전남 여수(`jeonnam_yeosu`) 노드 + 지역 DB 추가 | ✅ |
| 지역 클릭 → `zoom_{slug}` 텍스처로 확대 줌인 진입 (`renderRegionMapView`, `regionmap` 뷰) | ✅ |
| 지도 미준비 지역 '준비중' 플레이스홀더 | ✅ |
| 11개 지역 상세 지도(`pixelazed/*_2_pixelazed.png`) BootScene 로드 + loaderror 안전처리 | ✅ |

### 6-5d. 캐릭터 렌더링 수정 (✅ 완료, 2026-07-14)
**파일**: `client-pc/src/scenes/FieldScene.ts`

| 항목 | 결과 |
|---|---|
| 스프라이트 원점 발밑(0.5,1) + 그림자 발밑 정렬 (공중 뜸 해소) | ✅ |
| 표시 높이 `PLAYER_DISPLAY_H=60`px 정규화 (크기 확대) | ✅ |
| idle/move 종횡비 유지하며 크기 통일 (`applyPlayerSpriteSize`) | ✅ |

### 6-5e. 실지형 기반 지역 타일맵 시스템 — 속초 (✅ 완료 v1, 2026-07-14)
**파일**: `tools/build_region_maps.py`, `core/src/types/RegionMap.ts`, `client-pc/src/scenes/RegionFieldScene.ts`, `client-pc/public/data/sokcho/*.json`

| 항목 | 결과 |
|---|---|
| PNG 지형 지도 → 색상 분류(바다/육지/건물/잔디) 타일 그리드 변환 도구 (표준 라이브러리만) | ✅ |
| 작은 물 얼룩 제거 + 대각 연결성 보정(`bridge_diagonals`) 후처리 | ✅ |
| POI(식당 아이콘) 자동 추출 | ✅ |
| `RegionMapData`/`RegionMapGraph` 타입 + 속초 7개 맵 연결 그래프 | ✅ |
| 타일 RenderTexture 베이킹 렌더링 + 모래톱/모래사장/건물 외곽선 디테일 | ✅ |
| 바다·건물 병합 정적 바디 충돌 (이동 불가) | ✅ |
| 방향키 이동 + 카메라 팔로우 + 캐릭터 스프라이트(발밑/크기 통일) | ✅ |
| 맵 간 엣지 전환 (속초항3↔2↔1↔연결로↔동명항1↔2↔3) | ✅ |
| 바다 인접 시 좌클릭 차지 캐스팅 연출 (찌 투척·파문·회수) | ✅ |
| WorldMap 속초 뷰 '필드 입장' 버튼 → RegionFieldScene | ✅ |

### 6-5f. WorldMap 구역 선택 + RegionField HUD/인벤토리 (✅ 완료 v1, 2026-07-15)
**파일**: `core/src/types/WorldMap.ts`, `client-pc/src/scenes/WorldMapScene.ts`, `client-pc/src/scenes/RegionFieldScene.ts`, `client-pc/src/ui/RegionHud.ts`, `client-pc/src/ui/InventoryPanel.ts`, `client-pc/src/store/InventoryStore.ts`

| 항목 | 결과 |
|---|---|
| 전국→지역 이음새 없는 줌인 (전국 지도 페이드아웃 → 지역 지도 확대 연속 연출) | ✅ |
| 지역 확대 지도 출조 구역 핀 (속초항 258,60 / 동명항 221,49) + 좌측 리스트 hover 연동 | ✅ |
| 출조 확인 팝업 (예/아니오) → RegionFieldScene 해당 구역 맵 입장 | ✅ |
| 속초 외 전 지역 잠금 (`isRegionUnlocked` — REGION_AREA_NODES 데이터 존재 여부) | ✅ |
| 맵 간 이동 스폰: 진입 엣지 밴드 한정 + 엣지 통로 연결 검증(`edgeSpawnTile`) | ✅ |
| ESC 일시정지 메뉴 (계속하기/전국 지도/타이틀, 목재 도트 패널) | ✅ |
| HUD: HP/피로도 바 + 시계 + 날씨 (좌상단) | ✅ |
| HUD: 미니맵 — 지형 그리드 축소 렌더, M 키 3단계 크기 | ✅ |
| HUD: 퀵슬롯 8칸 (1~8 키/클릭, InventoryStore 배정 연동) | ✅ |
| HUD: 이벤트 로그 + 커뮤니티 채팅 목업 (좌하단, 멀티플레이 대비) | ✅ |
| 인벤토리(I): 카테고리 탭 5종 × 5x5 소켓, 신선도 배지, 착용 표시 | ✅ |
| 인벤토리 우클릭 액션 (착용/해제, 퀵슬롯 등록, 전환(준비중), 버리기, 완전제거) | ✅ |
| 인벤토리 하단 보유 재화(원) 표시 | ✅ |

### 6-5g. 팝업 UI 체계 + 상점/건물 시스템 (✅ 완료 v1, 2026-07-15 2차)
**파일**: `ui/DraggablePanel.ts`, `ui/ItemDetailPanel.ts`, `ui/StatusPanel.ts`, `ui/EquipmentPanel.ts`, `ui/UtilizationPanel.ts`, `ui/ShopPanel.ts`, `ui/Dialogs.ts`, `data/ShopCatalog.ts`, `core/src/types/AnglerStats.ts`

| 항목 | 결과 |
|---|---|
| 클릭 판정 어긋남 수정 (컨테이너 자식 scrollFactor 재귀 적용 `applyScreenFixed`) | ✅ |
| 팝업 공통 베이스: 드래그 이동 / X 닫기 / ESC 최상단 LIFO | ✅ |
| 아이템 우클릭 상세보기 (종류별 추론 물리 스펙 목업) | ✅ |
| 인벤토리 탭별 독립 5x5 소켓 + 드래그 앤 드랍 이동/교환 | ✅ |
| 낚싯대 우클릭 채비하기 → U 창 채비 탭 전환 | ✅ |
| Status(S) — 물리 스탯 4종 + 기여 설명 | ✅ |
| Equipment(E) — 부위별 착용/해제 + 물리 파라미터 요약 | ✅ |
| Utilization(U) — 요리하기/채비하기 탭, 채비 소켓 조립 + Z_limit 조절 + 스펙 합산 | ✅ |
| 건물 도트 텍스처 6종 (편의점/마트/직판장/식당/카페/주점) + POI 배치 | ✅ |
| 거래 확인 팝업 → 상점(좌) + 인벤토리(우) 동시 오픈 | ✅ |
| 상점 구매/판매 탭 + 수량 팝업(-/+/직접입력) + 확인 메시지 + 재화 정산 | ✅ |
| core AnglerStats/Zone 수심 프로필(Z_max) 기초 타입 | ✅ |

### 6-5h. 1인칭 낚시 물리 파이프라인 (✅ 완료 v1, 2026-07-15 3차)
**파일**: `core/src/simulation/CastingPhysicsEngine.ts`, `UnderwaterSinkPhysics.ts`, `LineTensionPhysics.ts`, `ChumPhysics.ts`, `BiteProbabilityEngine.ts`, `FishSpawningOracle.ts`, `FightingPhase.ts`, `client-pc/src/scenes/FirstPersonFishingScene.ts`

| 항목 | 결과 |
|---|---|
| 1단계: 3D 탄도 캐스팅 (조준각·완력·바람, 그림자/찌 이원화, z≤0 착수) | ✅ |
| 캐스팅 조준 UI (조준선 + 실시간 탄도 점선 + 착수 예상 마커) | ✅ |
| 2단계: 1인칭 씬 전환 + 의사 3D 레이어 (하늘/파도/수중/해저/실루엣/로드 뷰) | ✅ |
| 3단계: 침강 V_sink + 조류 드리프트 + H 뒷줄견제(제동/양력/정렬도 A) | ✅ |
| 입질 커널: P_base×M_terrain(여밭 Hold 2.5)×(1+k·A)×M_action×M_chum + 밑걸림 | ✅ |
| Phase 1: 밑밥 투척/확산/3차원 동조율 `getChumSyncRate` + 게이지 UI | ✅ |
| Phase 3: 어종 오라클 (물때 1~15 활성도·수심층·규제, 가우시안 개체 생성) | ✅ |
| Phase 4: 파이팅 텐션 상태 머신 (바늘털이/여박기 패턴, P_escape 공식) | ✅ |
| 1인칭 UI: 2분할 쿨러(어획/밑밥), 그만하기 버튼, 퀵슬롯 미표시→복귀 복원 | ✅ |
| 손 좌/우 착용 시스템 + 캐스팅 장비 게이팅 (퀵슬롯+착용 이중 조건) | ✅ |

### 6-5i. 메인 메뉴 개편 + 저장 슬롯 시스템 (✅ 완료 v1, 2026-07-15 4차)
**파일**: `client-pc/src/scenes/MainMenuScene.ts`, `client-pc/src/store/GameState.ts`, `client-pc/src/scenes/AnglerLogScene.ts`

| 항목 | 결과 |
|---|---|
| 로고 잘림/이중 겹침 수정 + 태그라인·구 도트 배경 제거 | ✅ |
| 시간대 연동 배경 (하늘·바다 그라데이션/별/반사광/등대/배/찌) | ✅ |
| 메뉴 뷰 스택: 게임 시작 → NEW/LOAD → 슬롯 3개 선택 | ✅ |
| 저장 슬롯 3개 (메타 표시·덮어쓰기 확인·레거시 호환) | ✅ |
| 인게임 일시정지 메뉴 '저장하기' | ✅ |
| 도감 복귀 씬 파라미터화 (메인 메뉴 흑백 화면 버그 수정) | ✅ |
| 키보드 내비 폰트 흔들림 수정 (선택 = 색+바만 변경) | ✅ |

**차기 (메인 메뉴 고도화)**:
- 슬롯 삭제 버튼, 슬롯별 스크린샷 썸네일
- 설정에 해상도/키 리맵, 언어 영문 번역 실적용
- Tauri 종료 API 연동 (`window.close()` → appWindow.close())
- BGM/파도 앰비언트 사운드 도입 (설정 볼륨 연동)

### 6-5j. 어종 마스터 21종 + 채비 손실 루프 + FP 뷰 개선 (✅ 완료 v1, 2026-07-15 5차)
**파일**: `core/simulation/FishSpawningOracle.ts`, `core/simulation/FightingPhase.ts`, `client-pc/src/store/InventoryStore.ts`, `client-pc/src/scenes/FirstPersonFishingScene.ts`, `RegionFieldScene.ts`

| 항목 | 결과 |
|---|---|
| 어종 마스터 DB 21종 (서식지/수심/미끼 선호도/성전환/규제/물때/야간) | ✅ |
| 미끼 친화도 `getBaitAffinity` → 입질 확률 연동 | ✅ |
| 파이팅 패턴 3종 (바늘털이/여박기/횡이동) 어종별 가중치 | ✅ |
| 난이도 완화 (패턴 빈도↓, 릴링 진행 1.2배) + 입 연약도/목줄 절단 | ✅ |
| 실패 유형별 채비 손실 → 즉시 1인칭 해제 → 재장착 후 캐스팅 | ✅ |
| 캐스팅 필수 채비 게이트 (원줄/찌/목줄/바늘·미끼) | ✅ |
| FP 뷰: 해저 렌더 제거 + 육지 전경(shoreKind) + 우측 상단 수심 패널 | ✅ |
| dev 기본 장비 (용상 파조기/다이오 2500L/반유동 프리셋) | ✅ |
| 팝업 클릭 관통 → 캐스팅 힌트 버그 수정 | ✅ |

**추가 (2026-07-15 6차)**: 맵 전환 인식 최외곽 1타일로 축소 + 건물 근접 시 전환 억제 / 아이템 이미지 아이콘 시스템(`ItemIcon.ts`) / 음식·생선 에셋 배치(`public/food/`, `public/fish/`) / 감성돔·광어 어획 팝업·상세 실사 이미지 / 회(사시미) 네이밍 규칙 및 식당 품목 추가.

### 6-5k. 1인칭 낚시 전면 개편 — 챔질/조류/조법/편대 (✅ 완료 v1, 2026-07-19)
**파일**: `core/simulation/BiteSequenceEngine.ts`(신규), `core/simulation/TidalCurrentEngine.ts`(신규), `client-pc/scenes/FirstPersonFishingScene.ts`, `store/InventoryStore.ts`, `ui/UtilizationPanel.ts`

| 항목 | 결과 |
|---|---|
| 초릿대 3단계 구부러짐 (30/45/60° 프로파일) + 낚싯대 연장 렌더 | ✅ 수치+실렌더 검증 |
| 우클릭 챔질 (1단계 5%/2단계 20%/3단계 100%, 릴리즈 늦챔질 실패) | ✅ 1단계 실측 5.2% |
| 입질 패턴 7종 확률 분포 + 간격 1~180s + 확률 비례 반복(최대 5) | ✅ 1만회 분포 검증 |
| 어종 mock: 광어 [3] / 감성돔 [1→3] | ✅ |
| 조류 5존 (조수/반탄/조경 Hit Zone/횡/본류) + 포말 + 수면거리 변동 | ✅ |
| 릴링 방향성(화면 좌/우) + 조류 순/역방향 속도차 + 리프트(↑) | ✅ |
| 루어 액션: 리트리브/리프트앤폴/트위칭(더블탭)/호핑(싱글탭)/드래깅 | ✅ |
| 텐션 저항 (릴링 미끄러짐 + 과부하 줄터짐) | ✅ |
| 조법: 전유동(면사 제거 토글·H 침강 정지)/잠길찌 안내 | ✅ |
| 원투 편대 (T자/카드 3종/학꽁치/갈치 + 멀티훅 + 과부하 + 다관점 히트) | ✅ |
| 우측 수심 모식도 (거리축+자세 아이콘+훅 도트+존 라벨) | ✅ |

**추가 (2026-07-19 2차) — 온보딩 가이드 + 이펙트**: ✅ 첫 진입 튜토리얼(3열 카드+초릿대 도해, localStorage 영속, F1/? 재열람, 열림 중 입력 차단) / ✅ 상태별 하단 조작 바(drift/입질/파이팅 자동 전환) / ✅ 입질 단계 이펙트(파문+느낌표+쉐이크, 3단계 "지금 챔질!" 배너) / ✅ HOOK UP! 배너+플래시 / ✅ 텐션 85+ 붉은 비네트 / ✅ 조류 존 전환 토스트.

**추가 (2026-07-19 3차) — 1인칭 UI 다듬기**: ✅ 낚싯대 높이 축소+팁 화면 내 가시화(패널 왼쪽 바깥 배치) / ✅ 수심 패널 전용 레이어(z순서 — 낚싯대 위) / ✅ 수심 모식도 2배 확장(넓은 게이지 박스+채비 좌우 편차+우측 텍스트 열) / ✅ 가이드 4페이지 스텝 재구성(입질 읽기→챔질 타이밍→채비→파이팅, 단계별 히트 가능성 문구).

**차기**: 어종별 입질 형태 전체 확장(SPECIES_PATTERN → FishMasterSpec 이관) / 조경지대 시각 마커 고도화 / 잠길찌 아이템 시드 추가 / 편대 채비별 엉킴 확률(T자 -50%) 실연동 / 파이팅 중 릴링 게이지 시각화 / 사운드 이펙트(입질 알림음·드랙음 — 오디오 에셋 확보 시).

**차기 (1인칭 낚시 고도화)**:
- 어종별 실사 픽셀 이미지 확충 (현재 감성돔/광어 2종 → 43종. 사용자가 차근차근 추가 예정 — 신규 12종 `spriteKey`는 `fish_{id}` 규칙으로 이미 예약됨)
- 어종별 단일 회 아이콘 분리 (현재 모듬회 이미지로 통일)
- 어종 DB API 연동 매칭 (현재 core 하드코딩 43종)
- 기상 API 실연동 (바람/파고/조류 벡터를 목업 → 실데이터로)
- 여 밭 배치를 해시 시드 → 실제 해저 지형 데이터 기반으로
- 채비 부품별 무게/부력 DB 정식 연동 (현재 이름 기반 목업 수치)
- 어종 오라클 ↔ FISH_DATABASE/조과첩 도감 통합 (성전환 생태 노트 표시)
- 붕장어 줄꼬임/복어 와이어 바늘 대응 등 특수 채비 카운터
- 파이팅 릴링 사운드/로드 진동 연출, 뜰채(왼손) 랜딩 미니게임
- 물고기 실루엣 원근 접근 연출 고도화 (스케일/알파 + 수평선 스폰)

**차기 (RegionField 고도화)**:
- 세부 낚시 포인트 지정 + 캐스팅 → FishingScene 정식 연동 (다차원 캐스팅 물리: 스탯/기상 벡터/Z_max 그라디언트 적용)
- 요리하기 탭 정식 구현 (도마 + 삼면뜨기 손질, 회칼 장비 필요, 생선 이미지 에셋 업로드 후)
- 채비 모딩 고도화 (부품별 무게/부력 DB 연동, 채비 세트 저장)
- 인벤토리/상점 ↔ @tra/core UniversalItemDatabase 정식 연동 (현재 클라이언트 목업 시드)
- 상점 재고/시세 API 연동 (농정원 경락 시세 등 — API 키 확보 후)
- 해류/수심 정보 표시 (API 연동 기반, 바다 타일에서만 — 단축키 토글 아님)
- 건물 위치를 지도 데이터 기반으로 정밀 배치 (현재 POI 순환 배정)
- 나머지 지역(여수 등) 타일 데이터 생성 및 그래프 확장 + 잠금 해제
- 방파제 좁은 통로 통행성 세밀 튜닝, 물결 애니메이션

### 6-5l. 2026-07-20 종합 — 지형 연동·시각 개편·원투/루어 체계·추천 (✅ 완료, 2~8차)
**핵심 파일**: `core/simulation/SeabedProfile.ts`·`LineTensionPhysics.ts`·`BiteSequenceEngine.ts`·`RigRecommender.ts`·`LureRig.ts`, `core/db-schema/SinkerDatabase.ts`·`LuresCatalogDB.ts`, `core/types/Lure.ts`, `client-pc/scenes/FirstPersonFishingScene.ts`·`RegionFieldScene.ts`·`MainMenuScene.ts`·`SettingsScene.ts`, `store/ExternalDataStore.ts`·`RecommendationStore.ts`, `ui/UtilizationPanel.ts`

| 차수 | 항목 | 결과 |
|---|---|---|
| 2차 | SeabedProfile 인게임 연동 (snagRisk→암초 비율, 침강 한계=실지형, 여밭 판정 일원화, 모식도 해저 단면 ±12m 창) | ✅ |
| 2차 | 뒷줄견제(H) 재정의 — 그 지점 홀드(0.2m 원샷 상승+정지, 정렬도만 진행) | ✅ |
| 2차 | 1인칭 가이드 UX (도해 좌우 반전·키 배지 행 구조·패널 520px) | ✅ |
| 3차 | 탑다운 바다 수심 6단계 타일 + 암초 노이즈 + 낮/밤 조명(네온·가로등) + 날씨 파티클 | ✅ |
| 4차 | 1인칭 실시간 하늘(시간대×날씨 매트릭스) + 수중 밝기 + 날씨 파티클 / 메인 메뉴 API 상태 패널(5소스 점 색) | ✅ |
| 5차 | 메인 메뉴 하단 순환 티커 (서비스 지역별 환경·7단계 낚시등급/시세 TOP5/어획량/입질 전망 — 8초 페이드) | ✅ |
| 6차 | 낚싯대 5절+스피닝릴 재설계(물 쪽 벤딩 보장) + 설정 '낚시' 탭(로드 좌/우·릴 핸들) + 입질 지속 +0.5s | ✅ |
| 7차 | 입질 단계 형태 차별화(톡/끄덕끄덕/실림) + 채비 당겨짐 연출 + **원투 체계**(찌 필수 해제·무게추 봉돌 13종 DB·초릿대 입질) + **채비 추천 알고리즘**(RigRecommender — U창 배너/소켓·상점 추천 배지) | ✅ |
| 8차 | **루어 채비 시스템** — 카탈로그 7종 15변종(웜/저크베이트/미노우/스푼/스피너/에기/메탈지그) + 지그헤드, `rigMode: 'bait'\|'lure'` 병렬 소켓, 침강 프로파일·Cd·타겟 가중 데이터 소비, 두족류(egiOnly) 스폰 바인딩, 루어 손실 규칙(목줄째만) | ✅ |
| 9차 | **채비 회수**(전 조법 distM≤0.5m → 배너+탑다운 복귀, 드리프트 하한 1→0.3m 실버그 수정) + **루어 액션 반응형 입질**(방치 0.15 / 리트리브 2.2 / 폴 2.6 / 트위칭 3.0 / 지깅 ×1.3, 게이지 표기) + **입질 유도 provoke**(릴링 1s/H → 70% 3단계 승격, 시퀀스당 1회) + **오라클 주야간/수심층 전수 검토**(43종 nightBonus 전수 부여 — 주행성 0.1~0.7 억제·야행성 1.2~1.8 추가, 층 페널티 인접 0.15/2단계 0.03) | ✅ |
| 10차 | **쿨러(어창) 시스템** — CoolerStore 3x3 활어 보관(신선도 정지) + 어획 [쿨러에 넣기]/[방생] → [계속]/[그만] 흐름 + CoolerPanel 팝업(1인칭 클릭·탑다운 B, 우클릭 상세/방생 확인/손질 예정) + 종료 시 인벤 이송·부족 시 **강제 방생(ESC 차단)** + **밑밥 배합 시스템**(U '밑밥 품질' 탭 — 재료 6종 드래그 투입 연출·물 넣기·섞기 → 100 충전, 1인칭 C 25씩 소모, 추천 배합 코멘트) | ✅ |

**차기**: 낚시점 전용 상점(루어 판매 등록) / 어탐 레이더(SeabedProfile 조회) / 어종별 입질 패턴 전체 이관 / 사운드 이펙트 / 프로덕션 서버 프록시.

### 6-6. WeatherEventEmitter — 돌발 기상 이벤트
**신규 파일**: `packages/core/src/services/WeatherEventEmitter.ts`
- `sudden_wind`, `passing_rain`, `tide_reversal`, `baitfish_school` 등 이벤트 타입
- 출조 중 시간 경과에 따라 확률적 발동
- 기상 예보와 실제 출조 간 '리얼한 변수' 제공

### 6-6b. 공공 OpenAPI 통합 수집 + 엔진 연동 (✅ 완료 v1, 2026-07-15 7차)
**파일**: `core/src/api-client/{ExternalApiService,FishingIndexApiClient,AuctionPriceApiClient,KosisCatchApiClient}.ts`, `client-pc/src/store/ExternalDataStore.ts`

| 항목 | 결과 |
|---|---|
| 바다낚시지수 API (fcstFishingv2) — 실 응답 검증 및 파서 확정 | ✅ |
| 경락가격 클라이언트 (품목 코드 CSV 매핑, End Point 주입형) | ✅ (엔드포인트 확정 대기) |
| KOSIS 어획량 클라이언트 (시도×어종, 월간) | ✅ (KOSIS 전용 키 필요 — Mock 폴백 중) |
| 스타트업 1회 수집 → 캐시 싱글톤 → 엔진은 캐시만 참조 | ✅ |
| 낚시지수 → 입질 P_base 0.7~1.4배 / 경락가 → 직판장 매입가 0.5~2.0배 / 어획량 → 스폰 가중 0.7~1.8배 | ✅ |
| 전 API Mock 폴백 (오프라인/트래픽 초과 안정성) | ✅ |

**추가 (2026-07-16 1차)**: ✅ MAFRA 수산물 경락가격 2종 정식 연동(Grid 621/623, 실호출 검증, 2023 동월동일 계절 시세 + 7일 누적) / ✅ KOSIS 새 인증키 검증(1,140행, 총중량 필터) / ✅ 어종 DB 단일화(레거시 ID 개명 + FISH_DATABASE 17종 추가 + 오라클 8종 역편입 = 29종) / ✅ MAFRA 품목명·KOSIS 분류명 → 어종 ID 매칭 테이블.

**추가 (2026-07-16 2차)**: ✅ 벵에돔 실측 보정(금지체장 없음 확정) / ✅ 긴꼬리벵에돔·가숭어 신규 (총 31종) / ✅ `bread` 미끼 분류 + 빵가루 경단 아이템 — 기존 TODO 2건 해소.

**추가 (2026-07-16 3차)**:

- ✅ 신규 12종 — 개서대/갯장어/꽁치/눈볼대(금태)/눈퉁멸/대구/덕대/병어/도다리/강도다리/도루묵/말쥐치
  (**오라클 43종 / FISH_DATABASE 42종**)
- ✅ 기존 4종 실측 보정 — 갈치(학명·금어기 7월·**금지체장 항문장 18cm → 전장 환산 47cm**),
  고등어(금어기 [5]→[4,5,6]), **광어(금지체장 21→35cm 오류 정정)**, 문치가자미→'참도다리' 개명(도다리·강도다리 분리)
- ✅ 오라클↔FISH_DATABASE 드리프트 4건 정렬(볼락·황볼락 금지체장, 조피볼락·열기 표기)
- ✅ MAFRA·KOSIS 매칭 확장(21건 매칭 검증 통과)
- ✅ 학명 4건 확정(참도다리 `Pseudopleuronectes`, 강도다리 `Platichthys`, 덕대 `P. echinogaster`, 병어 `P. argenteus`)

> **금지체장 단위 규칙**: 게임의 금지체장 판정은 **전장(`SpawnedFish.lengthCm`)** 기준. 법정 기준이 항문장·체장 등 다른 단위인 어종은 **반드시 전장으로 환산해서** 입력할 것 (갈치: 항문장 18cm → 전장 47cm). 환산 없이 넣으면 스폰 최소 크기에 막혀 규칙이 조용히 무력해진다.

> ⚠️ **매칭 테이블 순서 규칙**: `MAFRA_ITEM_TO_SPECIES`/`KOSIS_SPECIES_MATCH`는 부분 일치(`includes`)+선착순(`find`) 구조 — 품목명이 포함 관계면 **더 긴 쪽을 먼저** 둘 것 (`'말쥐치'⊃'쥐치'`, `'강도다리'⊃'도다리'`, `'개서대'⊃'서대'`). 어종 추가 시 반드시 확인.
>
> **사용자 확인 대기**: 학명 4건(참도다리·강도다리 속명, 덕대·병어가 동일 학명 `Pampus argenteus`) — 제공 데이터 그대로 입력함. 전갱이·쥐치는 오라클에만 존재(FISH_DATABASE 미등록 → 도감 조회 불가, 기존 이슈).

**차기**: 낚시지수 placeName↔게임 지역 매핑 정밀화(속초·동명항 인근 포인트 지정) / 수집 주기(일 1회 갱신) 스케줄링 / MAFRA·NMPNT HTTP 엔드포인트 → HTTPS 배포 시 프록시 + **브라우저 CORS 프록시**(NMPNT/MAFRA/KOSIS는 브라우저에서 CORS 차단 → 현재 Mock 폴백. 기상청 apis.data.go.kr만 CORS 통과).

### 6-6e. 채비 분리 + 부산 구역 + 팩토리 (✅ 완료, 2026-07-17 1차)
**파일**: `client-pc/src/store/InventoryStore.ts`, `ui/UtilizationPanel.ts`, `scenes/FirstPersonFishingScene.ts`, `core/types/WorldMap.ts`, `scenes/WorldMapScene.ts`, `src/game.ts`(신규), `src/main.ts`

| 항목 | 결과 |
|---|---|
| 채비 `hookBait` → `hook`/`bait` 소켓 분리 (8소켓 체인) | ✅ |
| 루어(바늘 일체형) 분류 + 장착 시 미끼 소켓 비활성 + 미끼 없이 캐스팅 | ✅ |
| 입질 소모/실패 손실 규칙 세분화 (루어 무손실, 바늘빠짐은 미끼만) | ✅ |
| `RegionAreaNode` 특성 확장 (details/depthRangeM/snagRisk/enterable) | ✅ |
| 부산 4구역 핀 (좌표는 핀 이미지 픽셀 추출) + 특성 카드 + 출조 차단 | ✅ |
| 속초 2구역 특성 상세 보강 | ✅ |
| main.ts → game.ts 팩토리 (싱글턴 가드 — 하네스 이중 생성 해소) | ✅ |

**차기**: ~~부산 4구역 타일맵 제작 후 enterable 해제~~ ✅ / ~~snagRisk 1인칭 실연동~~ ✅ (6-6f) / `depthRangeM`을 RegionFieldScene 수심 프로필 폴백에 연동(잔여).

### 6-6f. 부산 필드 + snagRisk 연동 + CORS 프록시 (✅ 완료, 2026-07-17 2차)
**파일**: `tools/build_region_maps.py`, `core/types/RegionMap.ts`(BUSAN_MAP_GRAPH·depthProfileUrl), `core/types/WorldMap.ts`, `core/simulation/BiteProbabilityEngine.ts`, `client-pc/vite.config.ts`, `store/ExternalDataStore.ts`, `public/data/busan/`(8종)

| 항목 | 결과 |
|---|---|
| 부산 타일맵 8종 생성 (감천서2/감천동3/암남1/백운포2) | ✅ |
| BUSAN_MAP_GRAPH — 명세 링크 6건 (동방파제1↔E↔암남 포함) | ✅ 브라우저 검증 |
| 출조 진입 중앙 스폰 (편차 1~3타일) / 맵 전환 엣지 스폰 유지 | ✅ 기존 로직이 명세 충족 |
| 부산 4구역 출조 개방 (enterable 해제) | ✅ |
| snagRisk→밑걸림 배율 (low 14.9s/mid 8.9s/high 5.8s, 1000회 시뮬) | ✅ |
| CORS 프록시 — NMPNT/MAFRA/KOSIS dev 실데이터화 (차단 0건) | ✅ |
| 부산 실측 수온 매핑 (감천항유도등부표 994401579) | ✅ |

**부산 맵 체인**:
```
[감천항 서방파제]  감천동(west_1) ↕ 방파제(west_2)
[감천항 동방파제]  제3부두(east_1) ↕ 수산시장(east_2) ↕ 방파제(east_3)
                   제3부두(east_1) ↔E↔ 암남공원 주차장(amnam_1)
[백운포]           체육공원(baegunpo_1) ↕ 방파제(baegunpo_2)
```

**차기**: 프로덕션 서버 프록시(배포 시 필수 — vite 프록시는 dev 전용) / 부산 수심 프로필(`build_depth_profiles.py` 부산 앵커) / depthRangeM 폴백 연동.

### 6-6c. 실측 연안 수심 프로필 연동 (✅ 완료 v1, 2026-07-15 8차)
**파일**: `tools/build_depth_profiles.py`, `core/src/types/DepthProfile.ts`, `client-pc/public/data/depth/gangwon_sokcho.json`, `RegionFieldScene.resolveCastDepth`

| 항목 | 결과 |
|---|---|
| 연안정보도 수심 SHP 파싱 (표준 라이브러리 — SHP/DBF + UTM-K 역변환) | ✅ (전국 46,270 포인트) |
| 속초항/동명항 앵커 100m 구간 수심 프로필 (0~2.5km) | ✅ |
| 캐스팅 거리 → 실측 수심 보간 + 범위 초과 거리 비례 외삽 | ✅ |
| 프로필 미존재 지역 그라디언트 폴백 | ✅ |

**차기**: 지역 확장 시 `REGIONS`에 앵커 추가 후 재실행 / 방향별(방위각) 프로필 분리(현재 반경 평균) / 여 밭 배치를 수심 급변 구간과 연계.

### 6-7. 환경 데이터 실연동 (API 키 확보 후)
**파일**: `packages/core/src/api-client/`

**진행 순서**:
1. 공공데이터포털 `PUBLIC_DATA_API_KEY` 발급 → `PublicDataClient.parseSpotApiResponse()` 구현
2. KHOA `KHOA_API_KEY` 발급 → `OceanApiClient.parseTideResponse()` 구현
3. KMA `KMA_API_KEY` 발급 → `WeatherApiClient.parseWeatherResponse()` 구현
4. 수집 스크립트 작성: `packages/core/src/scripts/crawl-fishing-spots.ts`
5. 스크립트 실행 → `SpotDatabase.ts` 자동 확장 (실제 낚시터 수백 곳 좌표 추가)

---

## ⬜ Phase 7: CraftScene (제작대)

> **단축키**: `U` | **참조 게임**: Green Hell 제작 시스템

### UX 기조

- 인벤토리에서 재료 아이템을 **드래그 앤 드롭** 으로 제작대 슬롯 위에 올리거나,
  아이템을 **클릭(선택)** 하면 제작대 슬롯으로 자동 이동
- 슬롯에 올려진 재료가 레시피와 **완전 매칭** 되면 제작 버튼 활성화 → 결과물 생성
- 재료 조합이 틀리면 빨간 테두리 피드백

### 제작대 종류

| 제작대 | 설명 | 생산물 예시 |
|---|---|---|
| **도마 (조리대)** | 음식 재료 가공 | 손질된 생선, 포, 건어물 |
| **낚시 채비 조합대** | 채비·루어 제작 | 목줄 채비, 지그헤드, 카로리나 리그 |
| *(추후 확장)* | 수산물 가공, 통발 수리 등 | — |

### 구현 목록

- [ ] `packages/client-pc/src/scenes/CraftScene.ts` 신규 작성
- [ ] `main.ts`에 CraftScene 등록
- [ ] `RecipeDatabase.ts` 확장: `craftRecipes` 테이블 추가
- [ ] 드래그 앤 드롭 인터랙션 (Phaser `setInteractive({ draggable: true })` + `on('drag/drop')`)
- [ ] FieldScene `U` 키 → `'CookScene'`에서 `'CraftScene'`으로 교체

---

## ⬜ Phase 8: 서버 & 멀티플레이

### 8-1. 낚시터 실시간 공유
**파일**: `packages/server/src/socket/PlayerSync.ts`
- 같은 스팟 접속 플레이어 도트 표시 (FieldScene 미니맵 연동)
- 채팅 메시지 전달 (FieldScene 좌하단 커뮤니티 UI와 연동)

### 8-2. 날씨/물때 서버 브로드캐스트
- 서버에서 KMA·KHOA API 호출 후 Socket.IO로 브로드캐스트
- 클라이언트 Mock 데이터 → 서버 실데이터 수신으로 전환

### 8-3. 토너먼트 시스템 (TournamentScene)
**신규 파일**: `packages/client-pc/src/scenes/TournamentScene.ts`
**서버 파일**: `packages/server/src/socket/TournamentManager.ts`
- 실시간 어획량 랭킹 브로드캐스트
- 제한 시간 내 최다 점수 방식

---

## ⬜ Phase 9: Tauri v2 통합 & Steam 패키징

### 9-1. Tauri v2 앱 설정
- `apps/tauri-wrapper/src-tauri/` 완성
- 윈도우 해상도 1280×720 고정 (`resizable: false`)
- 앱 아이콘, 번들 메타데이터 설정

### 9-2. 로컬 파일 세이브 마이그레이션
- `localStorage` → Tauri `fs` 플러그인으로 전환
- 저장 경로: `%APPDATA%/TheRealAngler/save.json`
- 세이브 버전 마이그레이션 로직

### 9-3. Steam SDK 연동 (장기)
- Steamworks SDK 도전과제 연동
- Steam Cloud 세이브 연동
- 리치 프레즌스 (현재 낚시 중인 스팟 표시)

---

## 고려되어야 할 추가 항목

### 지형 & 타일 시스템
- **기획 방향**: 구글 기반 대한민국 지도 API 연동을 베이스로, 실제 낚시 스팟 지형을 기반으로 직접 타일을 제작할 예정
- 행동(낚시, 방파제, 갯벌, 마을 등)은 특정 **랜드타일 종류**에서만 수행 가능하도록 타일 속성 부여
- 현재 FieldScene의 절차적 구역(`ZONES` 상수)은 임시 구현 — 실제 타일맵으로 교체 예정
- Phaser `Tilemaps` 또는 직접 제작 타일셋 + `SpotType` enum 연동 설계 필요

### 인벤토리 시스템 실체화
- 현재 `InfoOverlayPanel`의 인벤토리는 읽기 전용 목록 표시 수준
- **향후**: 아이템 상세 보기, 장비 장착/해제, 퀵슬롯 드래그 배정, 아이템 정렬/필터링 기능 필요

### 퀵슬롯 아이템 배정 시스템
- 현재 퀵슬롯 1~8은 인덱스 번호만 관리 (실제 아이템 연결 없음)
- 인벤토리 아이템을 퀵슬롯에 드래그 앤 드롭으로 배정하는 UI 구현 필요
- 퀵슬롯 활성화 시 실제로 해당 장비를 장착하는 GameState 반영

### 플레이어 스프라이트 & 애니메이션
- 현재 플레이어는 `Graphics`로 직접 드로우한 픽셀 캐릭터 (임시)
- 향후: 실제 픽셀 아트 스프라이트시트 + `AnimationManager` 4방향 걷기 애니메이션

### 세이브/로드 시스템 강화
- 현재: `localStorage` 기반 JSON 저장
- 슬롯 저장 (3~5 세이브 슬롯), 자동 저장, 건물 진입 시 세이브 안내

---

## 씬 전환 아키텍처

```
MainMenuScene
    ↓ scene.start (페이드)
WorldMapScene
    ├─ scene.start → RegionFieldScene  (실지형 타일맵, top-level)
    │      ↕ scene.restart (맵 간 엣지 전환)
    │      ↑ ESC → scene.start('WorldMapScene')
    ↓ scene.start (페이드)
FieldScene  ← 탑다운 월드 허브 (pause 상태 유지)
    ↓ scene.pause + scene.launch (페이드)
    ├─ FishingScene         → stop + resume('FieldScene')
    ├─ NightHuntingScene    → stop + resume('FieldScene')
    ├─ TrapScene            → stop + resume('FieldScene')
    ├─ CookScene            → stop + resume('FieldScene')
    ├─ CraftScene [예정]    → stop + resume('FieldScene')  [U 키]
    ├─ TackleRoomScene      → stop + resume('FieldScene')
    ├─ AnglerLogScene       → stop + resume('FieldScene')
    ├─ RestaurantScene      → stop + resume('FieldScene')
    ├─ CondoScene           → stop + resume('FieldScene')
    └─ TournamentScene [예정] → stop + resume('FieldScene')
```

> ⚠️ **절대 규칙**: 하위 씬에서 `scene.start('FieldScene')` 사용 금지
> 반드시 `this.scene.stop()` + `this.scene.resume('FieldScene')` 사용

---

## 현재 빌드 상태 (2026-07-20)

```bash
npx pnpm run build                              → ✅ 4/4 패키지 성공
npx pnpm --filter @tra/client-pc run typecheck  → ✅ 0 오류
```

**1차 테스트 배포**: https://sarsah93.github.io/Pixel-Angler-The-Real/ (gh-pages 브랜치 — 재배포는 AGENTS.md §최근 변경 2026-07-17 4차 절차 참고)

> ⚠️ **지도 데이터 재생성**: `pixelazed/<region>/` 지형 지도를 수정/추가하면
> `py tools/build_region_maps.py <region>` 로 `public/data/<region>/*.json` 을 다시 생성해야 함.

### FieldScene 단축키 목록 (업데이트)

> 1인칭 낚시(FirstPersonFishingScene) 조작은 **AGENTS.md §6c** 참고 — 우클릭 챔질 · 좌클릭 릴링(방향 당김)/탭 호핑/더블탭 트위칭 · ↑ 리프트 · H 홀드 · C 밑밥 · F1 가이드.

| 키 | 기능 |
|---|---|
| `방향키` | 캐릭터 이동 (전용) |
| `SPACE` / `ENTER` | 낚시 포인트 진입 |
| `E` | 건물/NPC 근접 상호작용 |
| `H` | 해루질 (NightHuntingScene) |
| `T` | 통발 관리 (TrapScene) |
| `C` | 요리 (CookScene) |
| `U` | 제작대 (CraftScene 예정) |
| `L` | 면허 패널 토글 |
| `I` | 인벤토리 패널 토글 |
| `Q` | 퀘스트 저널 패널 토글 |
| `M` | 미니맵 크기 순환 |
| **`V`** | **조류/수심 오버레이 토글 (신규)** |
| `1`~`8` | 퀵슬롯 선택 |
| `ESC` | 열린 팝업 LIFO 닫기 → 월드맵 복귀 |

---

## 파일 작성 템플릿

### 새 Phaser 씬

```typescript
/**
 * @file XxxScene.ts
 * @description [씬 설명 — 한국어]
 */

import Phaser from 'phaser';
import { GameState } from '../store/GameState.js';

export class XxxScene extends Phaser.Scene {
  constructor() {
    super({ key: 'XxxScene' }); // 파일명과 동일
  }

  create(): void {
    this.cameras.main.fadeIn(250, 0, 10, 20); // 항상 페이드인
  }

  // 나가기 버튼 패턴
  private onBack(): void {
    this.cameras.main.fadeOut(220, 0, 10, 20);
    this.cameras.main.once('camerafadeoutcomplete', () => {
      this.scene.stop();
      this.scene.resume('FieldScene');
    });
  }
}
```

---

## 주요 참고 자료

- **어신앱 스팟 정보**: `core/src/db-schema/AnglerAppSpots.ts`
- **KHOA (해양조사원) API**: `core/src/api-client/OceanApiClient.ts`
- **기상청 API**: `core/src/api-client/WeatherApiClient.ts`
- **공공데이터포털**: `core/src/api-client/PublicDataClient.ts`
- **레시피 DB**: `core/src/db-schema/RecipeDatabase.ts` (CraftScene 확장 예정)
- **퀘스트 DB**: `core/src/db-schema/QuestDatabase.ts`
